# 办公机工程交付（2026-09-15）

## 范围与集成

在阶段提交 a4e5b811a4573ac3cc7fdd088e4ed799469a446c 基础上完成下表工程接线，使用独立工作树验证后合入 main。合并包含 main 已有提交；本批工程不改写生产提示词、评级或用户作品。最终提交、源/构建哈希与执行日志由 `runtime/delivery-evidence/` 中的版本化交付记录关联，原始日志位于 `scripts/archive/office-completion/`。这些目录不入 Git，不能把缺失本地证据的其他机器自动视为已验收。

| 范围 | 已实现行为 |
| --- | --- |
| 场景/蓝图维护 | 精确变更集、独立全量导入、影响预览、sc1000+ ID、409 草稿及在途编辑保护；保存包含分片、聚合、压缩、版本及备份回滚 |
| 进程故障恢复 | PID/nonce 租约、登记子进程、签名 journal、跨进程读取屏障、显式恢复计划和恢复失败回滚；未知或活进程保持阻塞 |
| 资源库 | 可信配置及批准来源、完整/增量包、下载/导入/取消/恢复/回退、实际字节核验、网关和控制室接线、Live2D 依赖整组回退 |
| 安装包分层 | 默认 full；显式 base 只处理新暂存中的基础展示图，保持 URL、缩略图、品牌和完整 Live2D，记录基础/可选字节与必要下载量 |
| 引用审核发布 | 候选与人工决定绑定确切源/配方/请求/图片版本；发布不可变新目录；正式 outfitId 投影通过 schema 回归，审核证据留在封存清单；索引/图像共同失效 |
| 历史与证据 | 从本地旧/新记录判断关系影响；可证明时增量执行，否则完整结构检查并继续要求 full；源/构建/日志自动失效及最终提交绑定 |

## 验证记录

最终验证使用合并 main `3888e06fe3532db6ca1409661dd6e4ad27856e28` 后的受控源码，执行前 HEAD 为 `a4e5b811a4573ac3cc7fdd088e4ed799469a446c`。提交后的关联通过 finalize 保留这一原始执行身份，并逐文件核对最终提交；不把提交时间当作执行时间。

| 检查 | 本次最终结果 |
| --- | --- |
| full 门禁命令 | 退出 0，PASS；北京时间 13:52:48–13:59:29，包含下列质量、前端、Node、契约及构建阶段 |
| 质量检查 | 22/22，通过类型、Lint、双主题静态对比度、动画、体量及数据一致性检查 |
| 前端测试 | 109 个文件、783/783 通过 |
| Node 单测 | 1,032 通过、2 跳过、0 失败，共 1,034 项 |
| 契约套件 | 33 组通过、0 组失败 |
| 生产构建及预算 | 18 条路由通过硬预算；工作台 CSS、Live2D 懒块和 Companion 静态闭包仍有接近预算警告 |
| 浏览器 | 35/35 通过，13:53:59–13:55:02；覆盖维护变更集、冲突/草稿保护、资源管理及图片失败恢复 |
| 画面复核 | 已打开最终场景影响预览与资源库面板的深/浅主题、1440/390 宽度截图，复核文字、布局和控件状态；范围限所列页面 |

两项跳过分别为 Windows 缺少文件 symlink 创建权限，以及工作树没有 AI/SceneShowcase 真实素材。目录 junction 拒绝测试已执行。命令 PASS 与跳过项分别记录：机器证据的 `checks.fullGate` 保持 pending 并明确 `exitCode: 0`、`skipped: 2`，不将它转换成零跳过的完全通过。

本次证据链前缀为 `runtime/delivery-evidence/office-completion-20260915055246916-`，包括 baseline、results、office 及提交后生成的 final JSON。快照绑定 1,379 个源码文件、dist 和 data 树的实际字节，包含未入 Git 的聚合与压缩产物。最终日志为 `scripts/archive/office-completion/gate-20260915055246916.log` 和 `browser-20260915055246916.log`；截图在同目录的 `browser-20260915055246916/`。

首次合并验收曾因本地蓝图聚合产物陈旧而失败；使用现有自愈构建刷新聚合及压缩后，核对全部已跟踪源码字节未变，再捕获新基线并重跑 full。失败日志 `gate-20260915054320021.log` 保留，不用于最终通过声明。

收尾定向结果：恢复/读取屏障 44/44；正式引用契约、候选安全、版本路由及内容证据合计 74 通过、1 跳过、0 失败。跳过项为 Windows 无文件 symlink 创建权限；目录 junction 拒绝与路径边界测试另有通过证据。早期资源 108/108、浏览器 35/35 属于各自执行阶段，不冒充最终合并源码重跑。

## 验收边界

测试数据为中性临时夹具；上游为本地模拟服务，未调用真实模型、下载外部资源、安装桌面程序或迁移作品。参考门禁采用 `AICS_REFERENCE_AUDIT_MODE=structure`，不证明参考图存在、画质或人工审核真实性。HTTP 事务隔离测试中的内容校验器为明确替身，实际文件写入、压缩、锁和恢复执行；完整生产内容规则另由 full 门禁检查。

check:impact 的 passed-scoped 不等于完整语义覆盖：DATA_VERSION、压缩伴随文件、未导出语义规则和全部字段读写依赖仍要求完整门禁或继续保留 unknown。引用发布保持人工决定与显式激活；下载不自动安装；打包源维护保持 501；远程/隧道/未知来源继续 fail-closed。

进程 SIGKILL 恢复不是物理断电证明。Windows 目录 fsync、磁盘故障、非协作外部写入器及原生文件占用需按实际环境验证。读取屏障完整缓冲受保护响应，上限 64 MiB，超限拒绝。

主力机仍需按 `deploy-desktop.bat` 执行安装及必要 UAC，验收真实模型/GPU、实际资源与 Live2D、WebView2、4K/150% DPI、多屏/休眠和升级迁移；这些字段在交付记录中保持 pending。暂停内容任务、安装版自建源内容和未授权新产品接线不在本批恢复范围内。后续任务仅维护于 [roadmap](../../roadmap.md)。
