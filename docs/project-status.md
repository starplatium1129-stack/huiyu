# 项目状态

> 更新于 2026-09-20，当前版本 1.7.1。此页汇总已实现范围；剩余任务只维护在 [未来规划](roadmap.md)。9 月 12 日发布修复见 [发布说明](releases/v1.7.1.md)，后续 UI、导航及清理见下文。不同阶段的测试和安装状态分别记录。

## 当前数据

以下为 2026-09-12 登记快照；9 月 15 日旧模块及文档清理没有重算内容或资产数量，不将其作为最新实物盘点结果。

| 项目 | 数量 | 口径 |
| --- | ---: | --- |
| 角色档案 | 160 | `data/characters.json` 数组 |
| 热门角色 / 服装形态 | 158 / 971 | `data/popular-characters.json` 的 characters 与 outfits |
| 通用场景 / 场景蓝图 | 302 / 1,692 | `data/scenes.json` 与 `data/scene-blueprints.json` |
| 参考库角色 / 已登记形态 | 160 / 644 | `data/character-reference-view.json` 的角色和 outfits |
| 参考条目 | 4,508 | 全部 references |
| 非 pending 且有 URL / pending | 2,534 / 1,974 | 索引登记统计；未在本机验证实际文件与图片质量 |

聚合数据有相应分片源，门禁检查源与产物一致性。六份空白喜好/投入条目已按来源补充，旧身份、所属与语气字段已恢复显示，见 [档案补全记录](archive/audits/scene-scroll-character-profiles-2026-09-12.md)。字段中的资格、URL 或非 pending 标记不等于原作设定或真实图片审核通过。

## 已实现范围

### 2026-09-20 人物粒子展示回归：角色档案整幅展台

人物点云不再作为场景页的小块装饰，改为角色档案首屏的整幅形象展台：内容区全宽、桌面 65dvh（440–760px）画面，左侧角色目录继续用于切换；人物介绍放在下方，同一区域可在粒子形象与人物原画之间切换。保留同一 canvas 的人物重组、指针交互，新增重新聚拢与暂停；缺少点云时明确提示并回退原画，不将抽象占位计为人物。系统/应用减少动态效果均采用清晰静态人物，保留至少 2400 个采样点，暂停停止调度并清除残影。原画加载/回退、人物链接和参考素材行为保留。

双主题 1600/390 展台与原有档案/原画恢复共 20 项浏览器回归通过；最终显示调整后定向重跑 6 项通过，包括暂停像素稳定、重播像素变化、同一画布切角色、系统/应用减少动态效果、缺点云回退及键盘原画切换。应用/测试类型、定向 ESLint、样式/对比度/动效/单体、粒子偏好与可读性单测、构建/原预算/预压通过。截图和日志在本任务工作区 `runtime/particle-stage-review/`。未修改点云资产或角色提示词。

### 2026-09-20 美术审计收尾：阅读层级、画布与光色手帖

五项剩余前端工作已落地：统一小字阶梯（装饰编号 12px、元数据 13px、控件 14px、说明 15px、正文 16px）与阅读行距；作品册标题、筛选阅读底色和公共空/错/加载状态统一。绘制画布新增当前场景参考、镜头/构图/光照/画幅说明，明确参考不是生成结果；真实清单核验、未知评级关闭与本机模糊边界保留，完整展示构图，不改场景/服装/提示词及草稿。光色手帖以六张已核实的 All 样张呈现暖冷、日夜和轮廓/点光三组观察，保留色板带入。视频三模式整理输入与步骤、上传首帧、任务状态和按需展开信息，成片只保留一个播放器。首页保留氛围 CG、档案保留人物辨识画面、聊天采用另外两张已核实的陪伴样张并标明参考用途，缺图仍可聊天；未新增 pending 资产。

当次应用/测试类型、定向 ESLint（保留原 v-html 警告）、样式/双主题对比度/动效/单体、25 条组件与工作区测试通过；生产构建、原包体预算及预压通过，绘制页 139.8 / 140 KiB。浏览器分轮共 41 个不同用例通过，最终变更后定向重跑 21 项通过，覆盖双主题、390/1280/1440、实际样张、完整参考构图、缺图回退、作品筛选恢复、视频三模式及模拟任务状态；视频模拟不代表真实生成或媒体解码。证据与最终构建标识在本任务隔离工作区 `runtime/artbook-completion/`。首轮定位重复与参考裁切问题已经修复并补验；上一批 ScenarioView 的内联画幅样式改为 CSS 变量以通过现行样式门禁。

通过唯一部署入口同步到 `D:/AI-CG-Studio`；首次遇文件占用中止，重试成功。实际桌面曾连接旧 3000 开发网关，随后关闭该旧服务，确认安装版自带 Node 启动安装目录网关并返回对应构建。真实 WebView2 已查看双主题、光色参考、键盘搜索跳转、应用放大/100%恢复和减少动态效果选择/恢复。最终安装与 HTTP 首页 SHA-256 以本机交付回执为准。该机实际为 2880×1800 与虚拟显示器，4K/Windows 150% 组合未验；无 NVIDIA 显卡、可用 ComfyUI/视频服务，真实生图/视频与设备全矩阵仍待主力机，不能用浏览器模拟替代。原生截图保留在任务工具记录，浏览器截图保留在上述证据目录。

### 2026-09-20 美术审计第三批：故事手帖与镜头首帧总览

第二批已随 `2f997fb` 合入 main，并与 `ce49cbe` 的效率／可靠性改动完成合并构建、类型及 18 项浏览回归。本批在该基线上继续：剧本页以三本带氛围参考封面的故事手帖呈现，封面跟随宁宁／夏目切换，仅显示目录明确全年龄的现有样张；各幕按顺序展示情绪、构图示意和画幅，提示词与参数按需展开。分镜编辑器增加首帧总览，按原比例展示已上传画面，未配图明确占位；支持键盘定位对应镜头，调序和时长变化即时反映在总览中。未修改剧本提示词、角色替换、参数或既有交接逻辑。

最终构建／原预算／预压、应用类型、单体、样式字面值／对比度／动效及定向 ESLint 通过（保留原有 v-html 警告）。最终生产构建 8 项浏览器回归通过，覆盖双主题 1440/390、两位角色全部 10 幕的复制内容对照、三幕交接、隔离模拟上传、调序／时长／焦点、图片缺失和离线脚本弹窗；已修复总览在窄屏撑宽页面的问题并复核截图。证据在本机隔离工作区 `runtime/storyboard-artbook-review/`，4173 预览首页与当前 dist 字节一致。未调用真实模型、未同步桌面安装版；真实视频生成与设备验收不在本批通过范围内。

### 2026-09-20 美术审计续批：场景首屏与响应式角色画集

在 `64621f4` 上使用隔离工作区继续整理浏览页：角色档案／角色场景的宽屏侧栏保留，900px 及以下改为当前角色摘要与肖像画集弹窗；同一目录实例保留搜索、作品筛选及选择，窗口放宽时退出模态并释放滚动锁。角色场景页移除大幅粒子区域和重复介绍，保留角色色板装饰与分级筛选。灵感页压缩页头和主题条，窄屏常用／收藏／隐藏入口统一从“筛选与收藏”展开，搜索说明按实际查询显示。

390px 实屏中，灵感页首张卡片约从 733px 提前至 533px，角色场景封面约从 1324px 提前至 611px，档案主图约从 583px 提前至 368px；这些是本机默认状态的布局测量，不是所有角色／筛选状态的保证。双主题 1440/768/390 视觉、实际摘要文字对比度、键盘选择／Esc／焦点返回、跨断点筛选保留和隐藏／恢复回归分批共 19 项通过；旧导航测试的弹层和标题定位已按当前界面更新。应用类型、定向 ESLint、样式／对比度／动效、单体和构建／原预算／预压通过。证据在该工作区的 `runtime/scene-browse-review/`，4173 预览首页与 dist 字节一致。未纳入主工作区其他会话的并行改动，未同步桌面安装版、未调用真实模型；提示词、场景数据和分级门控未修改。

### 2026-09-20：代码效率与结果可靠性补强

聊天草稿按角色、音量独立持久化，高频输入不再读取或序列化历史；归档仅在变更后写入，失败保留重试，兼容旧记录及新旧备份。视频 HTTP 入口、任务服务、引擎探测与任务类型分离；视频上传和反推图片异步写盘，并补提交期间取消/关闭的迟到任务清理。SD 结果使用提交任务的参数与场景标题，成功任务各自保留 seed/LoRA，自动入册、手动保存与临时恢复共享结果上下文；异步入册前冻结任务字段，显式空标题与无 LoRA 不再被当前表单覆盖。

搜索面板首次唤起时加载，保留快捷键、焦点返回及加载期间取消；液态玻璃渲染器仅在开启效果时加载，SD 成片后处理按需加载。与本轮检查开始时的本机既有 dist 相比，入口 JavaScript 静态依赖闭包从 363.9 降至 241.2 KiB，入口 CSS 从 97.8 降至 94.9 KiB；最大路由闭包从 575.6 降至 557.2 KiB。原预算未放宽，绘制页仍为 139.9 / 140 KiB，需继续留意余量。这些是构建体积数据，不是整页加载耗时或 GPU 性能测量；生成配置去重后的参数值保持一致，未修改内容数据或提示词编译。

最终 `AICS_REFERENCE_AUDIT_MODE=structure npm run wf -- gate:full --all` 通过（8m33s），含质量/类型/样式检查、前端测试、1,073 项 Node 测试、34 组契约及构建/预算/预压。浏览器首轮 38 项通过，4 个旧 UI 定位修正后连同作品搜索复开用例补验 5 项通过，共 43 个不同用例；已复核搜索与液态效果双主题截图。浏览器所测构建与最终构建的 dist 首页及 manifest 哈希一致。日志、截图与构建绑定记录在本机 `runtime/code-health-2026-09-20/`。首轮门禁发现的换行格式与旧开关结构断言已修复，不以旧日志冒充最终通过。

首轮反推路由测试因旧夹具回退本机模型，实际触发至少一次 WD14 CPU 推理；没有模型下载或外部模型请求。随后修复夹具隔离，引擎真实推理改为显式 `AICS_TEST_REAL_WD14=1`，最终门禁默认不执行真实模型。未同步桌面安装版，structure 检查不代表参考素材实物验收；原有多窗口归档覆盖风险与真机性能验收继续在 roadmap 保留。

### 2026-09-20 美术审计首批：工作室肖像与光色手帖

延续二次元画集方向：绘制台的宁宁／夏目使用现有肖像卡，双人选项同时展示两位，放大角色名称、特征和入册说明；推荐配方、配音与字幕按需展开，基础模式的编译摘要保留计数和健康状态。画风页改为六张氛围参考组成的响应式画集，保留原配色带入逻辑；只展示当前样张目录中明确全年龄且 ID 唯一的参考，目录失效、未知分级或图片失败均保留占位。色彩情绪的色号移到稳定阅读底色上，修复深色色块压深色字。未修改提示词、场景、服装或生成参数。

最终生产构建、原包体预算与预压通过，绘制页体积仍接近上限；应用类型、单体、样式字面值、双主题对比度、动效和定向 ESLint 通过（色彩页原有 v-html 警告保留）。独立预览的 17 项浏览器回归通过，覆盖双主题、1440/1280/390 宽度、全部 60 个色号的实际对比度、肖像键盘选择、缺图与分级拦截、专家布局及草稿保留。已复核深浅主题截图，证据在本机 `runtime/artbook-polish-review/`。既有 3000 服务提供的是另一份旧构建，最终验收使用与当前 dist 首页字节一致的 4173 预览；未同步桌面安装版、未调用真实模型，不代表其余审计建议已全部完成。

### 2026-09-20 创作参数视觉：引擎卡片与参数手帖

继续角色画集的视觉方向：参数侧栏增加角色强调色页眉，引擎选择改为带手绘图标和说明的纵向卡片，当前引擎使用侧边强调线；基础模型、SD/Anima 数值输入统一阅读底色与间距，随机种子独占一行，避免窄栏挤压。参数名称、数值、事件与生成逻辑未修改。

当次构建/预算/预压、双主题对比度、动效与定向 ESLint 通过。桌面/手机双主题 4 项草稿保持回归通过，覆盖引擎与模式切换、参数值保留、禁用态和横向溢出；旧 CFG 定位改为当前界面的“相关性 (CFG)”。已查看双主题截图，本机留证 `runtime/inspector-notebook-review/`。未调用真实模型或同步桌面安装版。

### 2026-09-20 角色选择视觉：肖像画集与造型手帖

按用户以二次元视觉为主的方向，热门角色选择弹窗改为大肖像卡片，名称和作品名放在图片下方；桌面四列、手机两列，保留作品筛选、搜索、分页与选择后焦点返回。当前角色卡放大肖像，服装区改为造型手帖列表，以图标和侧边强调线标记选中项。展示使用现有肖像，pending/加载失败沿用占位，不将头像冒充服装预览；移除该区域重复的引擎推荐与无需 LoRA 徽章，角色来源入口仍保留无需 LoRA 说明。

角色/服装选择事件、数据与编译参数未修改。1440/1280/390 宽度的双主题目录回归 6 项通过，截图留在本机 `runtime/character-artbook-review/`；8 位角色双主题实际控件对比度回归 16 项通过（旧服装标题断言已更新为造型手帖和唯一选中项）；双主题令牌对比度、动效与定向 ESLint 通过，构建/预算/预压通过。后续创作参数面板的视觉整理尚未实施，本批未同步桌面安装版。

### 2026-09-20 Reka UI 迁移续批：开关与绘图模式

在 `b99c85f` 的 Combobox / Tabs / Popover 接入上继续迁移：公共 `ToggleSwitch` 使用 Reka Switch，覆盖生成参数、语音输入、场景隐藏管理与控制页共 14 处调用；保留布尔 v-model 和 change 回调，分享偏好仍通过原保存入口处理。绘图模式封装为按需加载的 `DirectorModeSwitch`，使用 ToggleGroup 的方向键/Home/End 导航，重复点击当前模式不会清空选择。保留项目视觉令牌，开关滑块仅做 transform 动画，并支持减少动态效果与系统强制颜色。

当次验证：2 项开关单测、应用类型检查、定向 ESLint、单体/动效/双主题对比度检查通过；最终 6 项浏览器回归通过（双主题开关及模式组、模拟上游生成参数提交与 CUDA OOM 恢复），另有隐藏场景筛选回归通过。已人工检查控制页开关、分享开关与模式组的双主题截图。旧测试的 checkbox/祖先 label 定位已按 switch 语义更新。

生产构建、预算与预压通过，未调整预算。首轮 Switch 接入使创作页预加载依赖增加、超过上限 73 字节；迁移并拆出模式组后恢复通过。创作页及部分静态依赖闭包仍接近现有预算，后续迁移须继续监测。本批未调用真实模型或设备、未安装桌面版；其余自建弹窗、滑杆与选择控件仍需按实际交互风险分批评估，不记为全站迁移完成。

### 2026-09-20：可选轻盈与液态玻璃

「更多 → 外观与动态效果」增加两档材质：轻盈玻璃默认开启，无背景滤镜或折射贴图；液态玻璃手动开启，导航/工具条按需使用边缘折射，并在切回、隐藏或辅助显示设置生效时释放资源。偏好在设备内保存、跨窗口同步，系统高对比度和减少透明度优先。修复旧氛围层选择器误隐藏根页面的问题。

UI 门禁、样式/双主题对比度、类型检查、原包体预算和 19 项浏览器检查通过；独立提交版本另行构建并重复 19/19。资源及 RAF 采样仅代表本机 Headless Edge，不代表真实 GPU、办公本或桌面安装验收。详见 [材质实现与验证](research/engineering/glass-material-modes-2026-09-20.md)。

### 2026-09-20：生成与工作台维护性整理

WAI HTTP 路由与校验、资源、工作流、WebUI 传输及业务编排分离；工作台的草稿、筛选和作品存储各有独立适配，保留公开接口。相关目录经过运行时校验，保存作品先复制上下文与默认值再等待存储，避免保存中切换故事/项目影响记录。WebUI 探测与任务资源归属服务实例，保存实现按需加载，架构依赖及类型规则已有回归门禁。

实施基线 `db0b9a1`，未使用子代理，其他会话的四个页面/样式改动未纳入。structure 模式完整入口通过：19 项质量检查、1,016 项前端、1,073 项 Node（另 2 项环境跳过）、34 组契约及构建；完整入口后追加的上游地址缓存修正已定向编译/契约复验。浏览器 49 项首轮通过、2 项定位器修正后通过，共 51 项分批覆盖。验证、原包体预算和保留边界见 [维护性整理记录](research/engineering/architecture-maintenance-2026-09-20.md)。不迁移用户数据、不安装桌面版、不调用真实模型；保存时快照仍不替代所有生成入口的完整任务提交快照。

### 2026-09-20：开放聊天舞台与桌宠界面

主聊天页采用开放角色舞台、可切换的完整对话/沉浸布局；桌宠使用透明角色画面与轻量操作入口，独立聊天窗支持贴靠、解除贴靠、保留阅读位置及草稿接续。取景按角色和展示模式分别保存，浏览器画布适应舞台尺寸，Native 取景与命中坐标保持一致。影的本机 Live2D 入口退役，源文件保留，旧聊天数据在归一化前另存；无音色角色显示文字聊天。

998 项前端、1,072 项 Node、34 组契约、38 项原生单测及构建/包体检查通过；双主题真实模型、窄窗口和聊天流程已进行浏览器检查，后续微调复用已通过证据并定向复验。17 项质量门禁在 `AICS_REFERENCE_AUDIT_MODE=structure` 下通过；默认实物检查仍因本机参考图库未配置而失败，不能称为参考图验收。桌面安装版未同步，Windows 贴靠/穿透/多屏与音频设备仍需安装版实测。说明与截图位置见 [接入与重构记录](research/desktop/live2d-candidate-integration-2026-09-20.md)。本轮无子代理，作品册及快照的其他会话改动未纳入本次范围。

### 2026-09-19：长期架构首批

[长期架构计划](../plans/architecture-evolution.md) 已接入索引和 roadmap；本批基于 `1646c68`，复用主干已迁出的 `types/promptHistory` 和旧数据/API 修复，完成 A01.2 与指定范围 A07.1：结果快照只依赖所需字段接口，可用普通或冻结对象测试；历史快照类型脱离 Vue；增量检查追踪真实别名、再导出、Vue 脚本、类型边和间接依赖，原 ESLint 试点仍保留。

当次 76 项定向回归、19 项架构检查通过；structure 模式完整门禁通过，含 970 项前端、1,069 项 Node（另 2 项环境跳过）、34 组契约和构建。三份生产改动擦除类型后与基线输出一致，未改变 UI、生成请求、存储或设备行为，浏览器/真实模型/安装未执行。改动在独立 worktree 验证，提交阶段复用已绑定的源/构建字节证据；范围、日志、构建标识及回退见 [实施记录](research/engineering/architecture-a01-2026-09-19.md)。A01.3 剩余盘点、A02 及后续任务继续在 roadmap 开放。

### 2026-09-20：A02.1 保存生成作品用例

基于已推送 main 的 `e2c5979`，保存流程已抽到 `src/application/artwork/saveGeneratedArtwork.ts`，独立接收暂存、图片、尺寸、ID、正规化和 Repository 能力；Store 提供兼容默认值，并在原暂存保护内更新历史。字段优先级、旧回退时机、数据格式和 ID 算法保留。A01.3 作品链路盘点复用现有旧列表/新记录/恢复视图区分，未重写既有兼容模型。

当次 52 项定向回归、17 项架构/装配检查和 19 项浏览器回归通过，生产构建及包体预算通过。完整入口中的质量、986 项前端和 1,070 项 Node 通过（另 2 项环境跳过）；既有蓝图事务文件在并发/串行模式均达到 180 秒时限，因此完整门禁仍为 FAIL。该文件单独执行 9/9 通过，其他契约补验后共 34 组分段完成，不据此改写整套入口状态。源码未提高时限、未删断言、未安装或调用真实模型。证据、回退及尚未实施的 A02.2/3 见 [实施记录](research/engineering/architecture-a02-2026-09-19.md)。

### 既有功能范围

| 范围 | 已实现内容 | 交付边界与依据 |
| --- | --- | --- |
| 桌面布局与状态接口 | 布局、尺寸与滚动职责统一，六个面板改为窄接口，工作台公开字段由 156 收至 71 | [005](../plans/005-desktop-architecture-consolidation.md)；存储/恢复只完成计划内评估及隔离原型，生产仍沿用现有行为 |
| UI 细节与材质 | U0–U5 已有分批验收；后续玻璃回退、工作台阅读、工具区层级、窄侧栏和窄窗口已完成浏览器范围验证 | [007 §8–§12](../plans/007-ui-detail-polish-and-review.md)；历史 U6 部署不覆盖后续补丁，原生设备及未覆盖状态仍待验收 |
| 页面导航与动效 | 清理切页变换残留、重复模糊/入场；补慢加载和减少动效反馈；意图预热去重，不因悬停读取场景全库 | [导航审计](archive/audits/ui-navigation-and-unused-2026-09-15.md)；该阶段生产构建与 41 项浏览器回归通过，不代表网络耗时已完成前后基准测量 |
| 工作流与内容治理 | 场景/蓝图保存事务、变更集与影响预览、跨进程恢复、资源生命周期及网关/控制室接线、人工审核与版本发布、交付证据绑定已实现 | [办公机工程交付](archive/audits/office-engineering-completion-2026-09-15.md)；隔离夹具与浏览器验证覆盖工程行为，真实资产、设备及完整语义覆盖的边界单列 |
| 旧模块清理 | 删除未接入运行时的旧草稿仓库及其专属测试、旧 Krea 手动配方选择器 | [清理记录](archive/audits/ui-navigation-and-unused-2026-09-15.md#第二阶段旧模块清理与文档整理)；现行自动保存、自动配方及用户存储键保留 |

菜单/搜索/灯箱焦点、角色选中配色、录音松开、分镜反馈与照片叠字的较早修复见 [9 月 12 日交互细节记录](archive/audits/interaction-detail-polish-2026-09-12.md)，不计为真实模型交付。

## 办公机工程交付（2026-09-15）

在阶段提交基础上，场景管理界面已接入精确变更集、影响预览、409 草稿保护和 sc1000+ 编号；蓝图/场景保存接入持久化事务、跨进程读取屏障及显式恢复。资源库已接入网关与控制室，支持批准版本的下载、离线导入、取消、恢复和回退，安装包可显式选择 base/full 分层。入口及边界见 [工作流阶段说明](workflow.md#办公机工程阶段入口2026-09-15)。

reference:full 已改为候选生成、人工决定与发布预览；明确批准后才能另行发布不可变新版本。引用投影沿用正式 outfitId/视角契约，审核证据保存在封存版本清单中。历史影响检查在可证明边界内执行增量，未知影响转完整结构检查并继续要求完整内容门禁；它不证明全部字段读写覆盖或图像质量。交付证据绑定源、构建、日志及最终提交，主力机验收保持 pending。

本批修改与测试使用中性隔离夹具，不改写生产提示词或评级，不迁移用户作品、不安装程序。最终门禁、浏览器结果和合并范围分别登记在 [交付报告](archive/audits/office-engineering-completion-2026-09-15.md)，不将工程通过当作真实模型或设备通过。

## 当前交付边界

源码沿用 Vue 3 / Vite / TypeScript / Pinia、Express 网关及 Tauri 2 桌面壳，覆盖角色/场景、绘图工作台、作品册、聊天/语音、Live2D 和视频分镜。SD、Anima、Krea 2 保留独立请求与编译路径；深浅主题、响应式界面及页面懒加载已有实现。

A01–A05 的聊天存储、流式合并、远程自配目标边界及绘画工具结果声明已按行为回归修复。角色绘画工具目前只准备草稿，真实任务接线尚未完成。训练页不在当前路由中，粒子肖像继续保留。

9 月 15 日导航优化及后续清理尚未同步安装版；没有修改提示词、分级、场景源数据或迁移用户作品，也没有完成真实出图、WebView2/多屏及 GPU 验收。各批次“通过”只覆盖其记录的环境与文件，不等于全站全部状态或设备交付完成。

## 验证记录（按阶段）

| 阶段 | 已有证据 | 不覆盖的范围 |
| --- | --- | --- |
| 2026-09-12 发布修复 | [1.7.1](releases/v1.7.1.md) 记录 structure 模式下的门禁、475 项 Node 单测、25 组契约及构建；修复前浏览器 456 过 / 19 失败记录保留 | 参考文件实物、实际模型与安装迁移；这些数字不是 9 月 15 日重新运行结果 |
| 007 后续工作台打磨 | [§11–§12](../plans/007-ui-detail-polish-and-review.md) 分别记录玻璃复审与受控源码副本的 42 项浏览器回归 | 当前共享工作区不自动继承受控副本的构建身份；Windows DPI 和原生缩放仍待验收 |
| 2026-09-15 导航优化 | [第一阶段](archive/audits/ui-navigation-and-unused-2026-09-15.md#第一阶段导航与动效验证) 记录 15 项定向单测、41 项浏览器回归、类型/样式/包体和双主题画面检查 | 真实模型、设备、整页性能前后基准及安装同步 |
| 2026-09-15 旧模块清理 | [第二阶段](archive/audits/ui-navigation-and-unused-2026-09-15.md#第二阶段旧模块清理与文档整理) 单列引用核查、受影响测试和文档验证 | 不将前一阶段的 41 项浏览器结果记为本阶段重跑 |
| 2026-09-15 办公机工程交付 | [最终记录](archive/audits/office-engineering-completion-2026-09-15.md#验证记录)：full 命令 PASS，22 项质量检查、783 项前端、1,032 项 Node 单测、33 组契约及构建通过；35 项浏览器通过 | Node 另有 2 项环境跳过；参考库为 structure 模式；真实模型、素材与原生安装/设备仍 pending |

历史 doctor 就绪、依赖审计零已知漏洞与 6 项纯 Rust 节拍测试仍只代表当时环境；本次没有重新审计依赖，不将其作为当前安装、Cubism overlay、设备或闲置功耗验收。

## 历史与未完成工作

旧审计、批次和实验记录保留于 [归档索引](archive/README.md)，只代表当时状态。原场景审计继续暂停；待人工审核候选、参考机位、真实模型与桌面验收均在 [未来规划](roadmap.md) 留存。当前状态不再混用不同日期的“本轮完成”叙述。


## 2026-09-18：作品清理与备份安全修复

本批基于 `cf279ff`，不迁移用户库、不触碰提示词/评级/模型参数，也不安装或发布桌面程序。此前 `cf279ff` 的 Quality 四条执行道及 Summary 已通过（运行 `35312868036`）；该结果不自动覆盖本批存储改动。

- **未引用图片清理**：确认只授权当次候选 ID；真正删除前，在跨窗口保护内重新读取作品、项目、回收站、隔离记录、当前本地/会话草稿与旧版待迁移数据。新增引用和后来创建的图片不按旧名单删除；读失败、损坏草稿或无法取得互斥时不删除。
- **跨窗口与暂存保护**：当前版本应用在挂载前持有共享文档锁；清理要求独占访问，再取得现有作品写锁。其他窗口的私有会话草稿无法枚举，因此有其他绘遇窗口或未完成图片暂存时明确拒绝清理；关窗后可重试。保存原图到发布历史/临时结果/视频引用之间也受保护。过期回收站懒清理复用此保护，并保护项目、隔离记录与草稿引用。普通保存不要求关闭其他窗口。
- **备份导出**：沿用 v2 JSON 格式；导出/导入共用 512 MiB 字节上限，导出前计算 Base64 膨胀与 UTF-8 元数据开销，逐图编码组装 Blob 并再检查实际大小，避免全量 Base64 数组加整份 JSON 字符串的重复峰值。增加图片进度与取消；失败/取消不下载半成品、不改上次备份时间，读取失败不跳过图片。

保护适用于更新后的同源应用窗口及已接入的写入路径；更新后需保存工作并重新打开旧窗口。它不承诺控制仍运行旧代码的窗口、浏览器扩展、外部写入器或物理断电。缺少 Web Locks 的环境不执行清理，页面浏览保留原有回退。大型分卷/流式恢复尚未实现，512 MiB 上限未提高，编码优化不等于总内存恒定；真实大图库容量、桌面多窗口与设备验收仍须单列。

验证入口：`src/storage/artworkSession.spec.ts`、`src/utils/backupExport.spec.ts`、`src/composables/useBackup.spec.ts`、`src/storage/artworkRepository.test.ts`，以及 `tests/e2e/library-concurrency.spec.ts` 的原生 Web Locks/IndexedDB、多窗口和双主题取消回归。最终测试状态按本批受测源码/构建及 GitHub Actions 记录核对，不将测试设计计为已通过。外部参考素材继续按 CI 的 structure 范围检查，不记作实物或图像审核。

## 2026-09-18：009 F4 路由动效降级与清理

本批基于 `97ade972753694b8dbf7560c7667f56ec76350fd`，推进 [009 F4.6](../plans/009-ui-fluidity-and-performance.md) 的路由局部批次（F4.6a），不关闭整个 F4。保留现有不透明页面、6px / 220ms 入场、自有缓动、`inert` 与测量阶段，不以缩短动画冒充性能提升。

- **可选能力失败回退**：`animate()` 存在但调用失败时完成 Vue 回调；`cancel()` 失败时尝试解除 effect，即使可选清理也失败仍完成回调。成功、取消与重复回调共享一次性收尾，不将动效失败升级为导航失败。
- **偏好监听与生命周期**：支持现代与旧版 MediaQueryList 监听；缺少 `matchMedia` 时仍接收应用内减少动态效果事件。保留已有失活/卸载清理，补齐连续导航、离场不可交互、偏好运行中切换和监听移除的回归。
- **验证边界**：新增 `src/composables/useRouteTransition.spec.ts` 的 22 个定向用例。本环境实际执行 Node 隔离 hook 适配器：同一用例集在原实现 14 过 / 8 失败，在修复实现 22 过 / 0 失败；这不是实际 Vitest、Vue 挂载、浏览器或性能测试。源码哈希与方法见 [验证摘要](evidence/009-f4-route-motion-2026-09-18.json)。

基线 Quality 运行 `35317437516` 的静态、单测及契约执行道通过，浏览器执行道失败；该结果不代表本提交验证结果。新提交的完整门禁与浏览器结果以对应 SHA 的 Actions 为准。本批未改 CI 门槛、提示词、模型参数、存储契约或安装程序；F4 其余动效、双主题/键盘浏览器验收及 F7 真机性能仍按原计划保持待验收。

## 2026-09-19：A 批恢复基线与 009/010 同步

本节为 A 批当时的历史快照；009/010 后续实现分别以本页 D/E 批记录为准，未验事项以 roadmap 为准。

本批在干净的 `main@d738590`（与 `origin/main` 一致）核对当前源码与文档。默认实物模式的完整门禁仅因本机未配置外部参考素材根失败：2,534/2,534 个非 pending URL 对应文件均不可见；未改写索引、URL 或 pending 状态来掩盖缺口。按办公机既有边界设置 `AICS_REFERENCE_AUDIT_MODE=structure` 后，`npm run wf -- gate:full` 通过：17 项质量检查、Vitest、1,043 项 Node 单测、34 组契约和构建，总耗时 6 分 33 秒。该结果不覆盖参考图片实物、浏览器、性能、真实模型或 Windows 安装版验收。

009 已有但此前未回填的实现包括：旧 motion 别名映射到语义令牌、浮层选择器集中管理及生命周期清理、选择指示器在失活／后台时停帧、路由氛围在低效果档位关闭；路由 WAAPI 失败与减少动态效果切换的局部批次另见上一节。F4.2–F4.5、全量后台资源盘点、S08 趋势与 F7 真机负载仍未完成，详见 [009 当前状态](../plans/009-ui-fluidity-and-performance.md#执行状态)。

010 已新增内存级陪伴角色注册表、schema v1 Adapter Profile、两份内置映射和隔离单测，可证明身份／外观分离、未知 ID 拒绝及第三夹具角色无需修改核心 View 分支；但生产聊天、语音、记忆与 Live2D 运行时尚未消费注册表，用户数据未迁移，能力编译、模型检查器和两后端真机等价尚未实现。因此只记为 C2/C3 契约起步，不记为通用模型适配完成，详见 [010 执行记录](../plans/010-companion-experience-and-live2d-adapter.md#11-执行记录与文档维护)。

## 2026-09-19：B 批核心正确性

本批基于 `554cf10` 修复三条可复现的对象／请求一致性问题，不修改生产场景、评级、模型参数或用户作品数据。

- **作品册对象锁定**：沉浸查看器从“当前列表下标”改为稳定作品 ID；筛选、收藏或重载改变列表时重新定位同一作品，目标已离开当前筛选则关闭查看器，不再保留 A 的画面却把收藏、沿用配方、下载、复制或删除按钮切到 B。数字／字符串旧 ID 使用同一兼容比较。
- **提示词完整请求**：PromptPlan 增加独立 `palette` 层。导演台“色彩情调”现在同时进入工作室与热门角色路径；Anima 保留为模型原生标签，Krea 2 编译为明确的英文 color-palette 句，并由实际 `/api/anima/jobs`、`/api/creative/jobs` 浏览器请求体断言覆盖。预览与最终请求继续消费同一个统一组装出口。
- **异步任务隔离**：沿用 Anima 会话已有 request serial，并补“提交后切换实时表单”的回归：成功结果仍绑定提交时的完整 prompt、characterId 与 outfitId；迟到提交／旧轮询、引擎切换和作品查看器图片加载的既有 stale guard 一并保留。

验证：办公机 structure 模式下 `npm run check` 的 17 项质量检查全通过，`gate:quick ui` 通过，生产构建与 18 路由包体预算通过；定向 Vitest 18 项、热门角色编译 36 项、提示词编译 17 项、Anima 会话 15 项及 Gallery 契约通过。4 个隔离浏览器用例分别验证作品目标锁定、热门 Anima 配色请求、工作室 Krea 手选配色请求和热门 Krea 蓝图配色请求。浏览器生成链使用模拟上游，只证明最终请求与回写归属；真实模型画面、GPU 和安装版仍在 F 批验收。

## 2026-09-19：C 批核心功能精修

本批基于 `6b7289a` 收拢常用创作闭环，不改生产提示词数据或用户作品。

- **词条与素材应用**：统一互斥契约为“同类别不同组互斥、同组细节可叠加”。`school_uniform + pleated_skirt` 等同套细节不再互删；换成 bikini 等其他服装族时才清理旧族。一键服装包同步执行同一规则，保留表情等无关词条，兑现界面所写的自动防冲突。
- **完整创作蓝图**：导出改用版本化完整工作台快照，包含 studio／popular 身份、角色／服装／蓝图、故事与画面描述、情绪／镜头／光照／构图／配色、手动词、画师、衣装顶替、导演模式、SD 和 Anima/Krea 参数。导入按白名单与数值范围恢复；未知版本、失效角色／服装／蓝图明确拒绝，缺场景和缺模型分别降级并说明，生成中不允许改写当前工作台。旧 v1 无 schema 文件保持可读。
- **随机、反推与跨页**：随机灵感继续保留手动画师、重掷替换和单步撤销，属性测试覆盖三引擎与互斥组；本地反推的身份／姿势／服装冲突合并契约通过，并在本机现有 WD14 上完成隔离测试图真实推理。缓存工作台的 scene／char／mood／popular／scenario 深链增加上下文签名，只有链接真正变化才重放，既能应用 mood-only 新链接，也不覆盖同一链接后的手工编辑。
- **草稿恢复**：只有画面描述或手动词条、没有故事／场景的草稿现在也会恢复；完全空白草稿仍拒绝覆盖当前工作台。

验证：structure 模式 17 项质量检查、UI quick gate、生产构建与 18 路由预算通过；定向 Vitest 17 项、随机组合 17 项、反推合并 8 项、Prompt Builder 模块与 Gallery 契约通过。6 个浏览器用例覆盖蓝图导出→冲突换装→导入恢复、随机灵感双主题重掷／撤销，以及历史、popular→studio、studio→popular 三条跨页链。反推路由与引擎测试通过，WD14 在隔离测试图上返回真实模型结果；尚未对真实用户素材或生成画面做人工视觉验收。

## 2026-09-19：D 批 009 办公机收尾

实现提交为 `9d8d1fe`，详见 [009 F4–F6 证据](evidence/009-f4-f6-office-2026-09-19.json)。缓存路由不再重播首次入场；浮层从可见来源建立空间关系并在来源失效时轻量回退；快速改向结算旧回调且恢复 `inert`。粒子、场景卡与绘图页健康轮询补齐 KeepAlive／低效果生命周期，真实生成任务不因切页取消。Showcase 删除重复滚动锁，S04 基准修正 Playwright 自动滚动污染后为 0/60，另有 20 轮开关回归通过。

009 性能最终主场景以 3×20、单 worker 运行并通过，附加 overhead/decomposition 明确跳过；第一次含附加探针的完整入口在 15 分钟处超时。最终宿主只测得约 54Hz，S01 与 S03 数值明显受负载影响，不能与历史 120Hz 轮次直接比较，S03、四类 KeepAlive 稳态 GPU/纹理/内存和 Windows 真机仍开放。完整门禁也因蓝图事务文件超过 180 秒外层时限未得到总 PASS；该文件随后 9/9 直接通过，质量检查、Vitest、1,045 项 Node 单测、构建和包体预算通过。未运行真实生成、Live2D/语音并发、功耗或安装版验收。

## 2026-09-19：E 批 010 办公机接线

**9 月 20 日后续修订**：LPK 已复用夏目解包工具完成，新增本机角色共 7 个；初音切换为新水墨版。移除单独表情入口，自动情绪映射、竖向舞台与设置浮层已调整。六个 Cubism 3 模型已接通 Native 明确清单解析并完成离屏渲染；蕾姆保留 Cubism 2 浏览器回退。个人桌面部署新增 `-SyncLocalModels`，私有模型独立写入用户目录，不进入安装包。下段首次增量的两项 LPK 阻塞已解除，详见接入记录的后续修订。

**9 月 20 日增量**：初音未来、芙莉莲、菲伦、长离、蕾姆已通过本机导入接入陪伴注册表和浏览器 Live2D；补齐 Cubism 2 参数接口、缺失/未登记表情与动作、中文引用、三档贴图、表情与眨眼参数协调及小窗首次居中。原始候选和导入模型不入 Git/桌面包，远程不提供。本轮五角色真实浏览器画面、22 个表情与大贴图三档已检查；芙宁娜/雷电将军 LPK、新增模型原生与音频设备仍待验，详见 [接入记录](research/desktop/live2d-candidate-integration-2026-09-20.md)。

实现提交 `3c1099f` 完成 E1 内置角色的生产注册表接线：Companion 角色窗、独立聊天窗、完整房间、聊天历史／归档／草稿／长期记忆、音色、服装和 Live2D 外观现共用一份身份投影。旧 `nene`／`natsume` 数据可重入迁移，未知 ID 明确拒绝，不借用宁宁的人格、记忆、口型或互动。

E2 把口型、眨眼、视线、情绪、互动、命中区、入退场和叠层复位收敛到版本化 Adapter Profile。Browser 与 Rust Native 后端消费同一份经校验映射；无口型可继续播放语音而不伪造嘴部动作，无互动不借其他角色动作。聊天页可展开模型能力报告，区分已发现、待实机、已验证、未配置与配置无效。内置宁宁／夏目 Profile 仍标为 `needs-confirmation`，未用模拟桥提升为真机 verified。

E3 办公机范围验证快速切角的 Profile 同步、旧原生模型释放、草稿／服装保持、未知身份保护及可见性／电源／窗口边界状态。structure 模式 17 项质量检查、Vitest 899 项、Node unit 1,045 项、contract 34 组、Rust 定向 10 项、生产构建和包体预算均通过；浏览器 15 项及仓库内置 Live2D 资产 7 项通过。证据见 [010 E 批办公机记录](evidence/010-e-office-2026-09-19.json)。

未完成边界：静态资产检查器、运行时参数枚举与候选识别、用户模型导入／校准、Windows 透明窗／置顶／穿透／拖动／多屏／DPI、真实语音口型和长时资源趋势仍待 C4/C5/C7 与 F2。

## 2026-09-19：GitHub 参考方案落地与既有提交复审

已接入统一词条字典与别名归一、固定种子候选与配置快照、PhotoSwipe 可退出观画试验、图像/视频任务阶段、常驻配方兼容检查，以及语音中断和旧回调清理。复审同时修复恢复参数被初始化推荐覆盖、字符串作品 ID、停止后的任务摘要、图库异步资源及角色清空目标边界。详见 [实施审计](research/engineering/github-reference-implementation-2026-09-19.md)与[当次证据](evidence/github-reference-2026-09-19.json)。

保留默认查看器；未接入 TanStack Virtual、Reka UI 或 fflate。真实模型、音频设备、原生 Live2D/GPU 与桌面安装仍待主力机验收，浏览器模拟与结构模式不能替代。

## 2026-09-19：验证执行提速

契约执行器对 9 个已复核隔离边界的文件默认两路并行，其余 25 个串行；可用 CONTRACT_TEST_JOBS=1 回退原顺序。预压默认两路异步执行，Brotli 11 / gzip 9 与同步接口保持兼容。日志测试已改为等待真实写入完成，减少固定等待引发的偶发失败。

同机对照：34 组契约从 3 分 40 秒降到 2 分 24 秒；预压从 19.353 秒降到 8.470 秒，676 个压缩文件字节一致。最终单次完整门禁 PASS，耗时 4 分 32 秒；详见 [优化记录](research/engineering/validation-performance-2026-09-19.md)与[证据](evidence/validation-performance-2026-09-19.json)。

## 2026-09-19：可维护性增量试点

[M01–M10 审查](research/engineering/maintainability-optimization-2026-09-19.md) 已按增量边界落地：维护入口校准；历史列表与新生成配方分型、只读恢复解析；生成响应逐字段解码；路由 AST 和标题/作品复用行为回归；两域依赖限制；观画 URL 所有权与迟到回调回归；质量元数据、机器可读诊断及 Node 22.18 兼容 lane；PhotoSwipe 的适配/退出约定。没有用户库迁移、提示词或分级字段改写，也没有默认替换查看器。

执行规则见 [可维护性边界](guides/engineering/maintainability-boundaries.md)，当次验证及失败/复验记录见 [交付证据](evidence/maintainability-2026-09-19.json)。同时保留同期 main 的验证/压缩并行优化，将诊断和单文件超时接入既有进程池；未用另一份并行清单覆盖它。验证中发现的隐藏工作区 SPA 入口与粗粒度时间戳测试夹具问题已修复，私有隐藏资产继续拒绝。

真实模型、音频设备、触屏、原生 Live2D/GPU 和安装验收仍按 roadmap；本轮未部署。
