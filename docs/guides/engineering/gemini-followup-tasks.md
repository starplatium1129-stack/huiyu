# Gemini 3.8 Flash 后续任务包

2026-09-13。安排依据是本项目第一次交付：结构统计可复核，但报告存在把假设写成事实的问题。先完成 G0，再按顺序推进 G1—G5；无需逐批等待用户确认。这些是待执行任务，不是已完成清单。

将下面整个代码块发给 Gemini 即可。与 GLM 的工作流盘点、数据字段职责梳理分开，避免重复。

```text
继续 AI-CG-Studio 的资料整理与视觉初筛。本轮不安排业务代码修改。先读 AGENTS.md、git status 与相关 diff，保护其他任务修改。

第一轮复核已写入 docs/research/engineering/gemini-asset-review-2026-09-13.md。先完整阅读，不能直接沿用你上轮报告的结论。

共同交付要求：
1. 每条观察记录对象 ID、实际文件路径、SHA-256/版本、证据位置、观察、推断、待确认事项。统计必须由实际数据算出，不能硬编码历史数。报告叙述必须与 JSON 一致。
2. 图像工具给你的预览可能缩放或转码，原文件格式和体积只依据磁盘魔数、解码器元数据与字节数。视觉质量不得由文件大小推断。
3. 历史截图没有原始提交、视口、DPR 时只作历史观察；不靠截图宣称 WCAG、运行性能、离线恢复或当前版本通过。
4. 优先使用现有工具。可以在 scripts/archive/gemini-followup/ 编写短小只读脚本、保存临时分析产物；不得安装依赖、重建共享 dist、调用模型或运行写生产数据的维护命令。
5. 不改应用代码、场景/蓝图/提示词、原图、分级和打包清单；不删除、改名、压缩生产资源；不改 AGENTS/skill。只创建下列指定报告和 JSON，不改共享索引，不提交或 push。
6. 报告以并列字段和简短证据为主，不使用“实锤”“重大架构缺陷”“视觉无损”等没有验收支持的断言。无问题也可记录通过，不要求每个角色凑一个缺陷。

G0：修正并自校验上一份交付
- 修改 docs/research/engineering/gemini-asset-audit.md/json 及你自己的原审计脚本，逐项回应复核报告。
- 撤回两张伪 PNG、未经实验的压缩收益、无对应证据的粒子哈希验证、HomeView 原图引用等结论；保留原正确统计。
- 外部素材不可访问与物理缺失分开，历史体积与本机实测分开。
- 清理 JSON 中重复复制的元数据：建立独立资产表，以路径或资产键关联角色用途；保留原账本可对照映射，不能丢掉角色或参考项。
- 交付更正清单，明确哪些是错误、哪些是未验证。保存可复跑命令与输出摘要，不仅改措辞。

G1：扩大已有全年龄素材的视觉初筛
- 先根据现有明确分级确定允许检查的角色资产；不明确的列待确认，不自行判为全年龄。检查所有符合范围的已有角色素材，每批 20 位，剩余单独一批。
- 对照实际原图与缩略图，记录面部能否辨认、主体是否截断、背景干扰、缩略图是否对应原图、白边/黑边/拉伸等可观察问题。不存在的问题不推测。
- 联系表只作导航，疑点回到单图复核。问题按“已观察、容器适配风险、需当前页面复现”分类，不把裁切预测当实际 UI 缺陷。
- 输出 gemini-visual-inventory.md/json，包含逐角色覆盖清单和未检查原因；没有图片输入能力则明确停在结构结果。

G2：图片使用场景与裁切建议表
- 检查现有角色列表、资料大图、样张卡片、首页角色横条和粒子来源的实际资源调用与显示方式。
- 把 G1 的疑点对应到具体容器：实际资源 URL、CSS 比例、object-fit、object-position、已有回退。按调用代码判断，不按注释或文件名猜。
- 为已确证风险写简短建议和验收条件，不改图/CSS，不制定全库固定“头顶必须留 5%”规则。
- 输出 gemini-crop-usage.md/json；没有浏览器复现的项目标代码推断。不得启动或操作安装版。

G3：界面文字与状态提示盘点
- 只检查现有角色目录、场景管理、作品库、生成任务状态、导入导出和设置页的用户可见文案。
- 找错别字、同一概念名称不一、含糊错误提示、提示用户操作但页面无入口、空态缺少已有操作指引等。
- 每项附原文、文件/组件、出现条件、用户困惑和建议文案；仅在现有能力范围内改措辞，不建议不存在的按钮或功能。旧代码文本未确认是否仍使用时标疑点。
- 输出 gemini-ui-copy-review.md/json。不给正常专业词强行换名，不修改 UI 或翻译数据。

G4：指定专题文档的时效与证据索引
- 范围仅 docs/guides/characters/、docs/guides/desktop/ 及 docs/desktop-deployment.md；不重复 GLM 的工作流全量盘点/字段职责任务，不动提示词规范。
- 为每篇记录适用版本、历史性质、当前入口、与当前实现是否存在可证明冲突；历史文档保留历史，不机械改成今天日期。
- 检查“已完成、已安装、已通过”等说法是否有对应提交、产物或环境证据。历史 PASS 不代表当前 PASS，也不因旧报告没有新证据就判功能故障。
- 输出 gemini-doc-evidence-index.md/json；每项给证据与更正建议，不直接重写原文档。

G5：资源分发与 PC 体验的人工验收用例
- 根据实际功能整理断网首次进入、原图缺失、缩略图损坏、资源读取失败、窗口缩放、4K 150%、切换深浅主题的操作场景。
- 每项包含前置条件、步骤、应观察结果、要保留的证据和恢复步骤；明显区分当前功能与 R1 尚未实现的未来能力。
- 下载取消、损坏资源包、跨版本资源更新等若尚未实现，标未来测试设计，不能标当前失败。
- 输出 gemini-resource-acceptance.md/json。只写用例，不真的移动素材、断网、清缓存、安装或运行破坏性故障注入。

G1—G5 的所有正式输出放 docs/research/engineering/。另维护该目录下 gemini-followup-progress.json：每个任务 pending/in-progress/completed/blocked，记录范围、结果文件、未验证项。G0 未修正完不扩批；某任务工具不可用时完成独立部分并继续下一任务。

全部完成后，只汇报覆盖数量、最有价值的已确认发现、更正记录、输出路径和验收缺口。不要把一份长报告当成质量证明；不要自动启动修复、全库内容改写或出图。
```
