# 项目未来规划

011 办公机源码与最终集成验证已完成；剩余主力机和保守回收边界见 [执行记录](audits/2026-09-21/011-results.md)。

> 更新于 2026-09-21。只维护未完成、待复验、暂停和待决策事项；已实现范围查 [项目状态](project-status.md)，历史修复过程查 [归档](archive/README.md)。2026-09-19 基线的完整门禁通过是历史证据；A02.1 的当次完整入口仍受蓝图事务超时影响，分段补验与浏览器结果见下，不替代真实模型和设备验收。

## 当前执行顺序（2026-09-19 确认）

### 2026-09-21 新增 011 执行任务

按[详细计划 011](../plans/011-data-compatibility-and-resource-boundaries.md)单主会话完成六批实现与最终集成验证，未使用子代理。剩余：真实长回复/工具流和 GPU；主力机实际大图、磁盘额度及资源峰值；真实升级/回滚数据保护、发行包身份与安装/UAC。未知浏览器会话引用的参考继续保留，不提供自动删除；异常退出上传锁须停网关核查。既有资源安装恢复测试曾出现一次跨进程 ENOENT，原样定向 14 项及最终全量通过，保留间歇竞态线索，不把一次补验当作消除所有并发风险。上一轮内容、素材、设备待验均未自动关闭。

### 2026-09-20 全面审计补充优先项

[本次全面审计](audits/2026-09-20/README.md) 基于 `39d955e`，最初仅交付报告与[落实计划](audits/2026-09-20/implementation-plan.md)。A0–A5 已逐批实施；2026-09-21 接手复核继续修复路径别名、失败补偿、任务删除和拼接取消边界，并推进 A6–A9。原报告的失败记录保留为历史，当前状态按下面逐项说明，不用此前 PASS 关闭设备或素材验收。

本批未恢复暂停的场景出图审计、未补图或同步桌面；下方既有设备/资源待验继续保留。任务完成后在本节更新对应 ID，详细复现与历史证据保留在审计报告中。

2026-09-21 A0 已提交并推送；A1 的 SEC-01 批量来源上下文、SEC-02 私有候选目录全编码拒绝及 SEC-03 参考投影的远程本机边界已实施并通过结构门禁。SEC-03 的完整场景/样张远程投影仍按设计文档单独接入，不将“参考 view 远程拒绝”冒称为完整内容发布投影。

2026-09-21 A2 已完成 STORE-01/02：合并恢复只发布备份涉及且未被并发改动的设置，失败补偿按写入身份回滚；现代备份记录预检与运行时展示契约一致，旧 timestamp-only 记录稳定迁移，缺失原图在恢复前明确提示。隔离恢复、备份核心、前端全量单测与 21 步 structure 门禁通过；真实双窗口 WebView2 与大备份设备验收仍保留在后续边界。

2026-09-21 A3 已完成 BE-01/02：WAI WebUI 与 Comfy 共用四槽有界准入，状态分别报告两类在途任务，排队取消不触发上游全局中断，失败/终态释放槽位；WebUI 与 Comfy 结果 GET 在所有者授权和 TTL 内只读、可重试并带 `no-store`，不再把响应发送完成当作客户端持久化确认。生成路由隔离夹具已覆盖超额入队、跨提供方结果重读、跨身份拒绝、排队取消、失败后恢复和无重复生成；真实模型、磁盘满/浏览器设备与安装版验收仍保留。

2026-09-21 A4 已完成 UX-01/02/03/05 与 PM-02/03：无图换装入口使用可见焦点的原生按钮，Enter/Space 均可打开文件选择；纯角色窗保留默认隐藏工具栏并用右键/Shift+F10 显式打开，事件提醒只提供事件专属导航按钮；各路由设置区分标题；本机角色目录的 HTTP/结构失败释放缓存并可重试，场景数据按文件超时且可选元数据不阻塞主体。前端 154 个测试文件/1078 项、check 19/19、构建预算和关键双主题键盘/标题浏览器用例通过；屏幕阅读器、WebView2/透明原生窗及其他 UX-04 状态仍需设备验收。

2026-09-21 A5 实现已完成 BE-03/04/05、STORE-05/06：同批视频拼接在 I/O 前登记唯一在途操作，20 个并发请求共享一次转码；列表与输出使用操作级临时路径，成功后原子发布，失败/取消清理本次文件；尾帧抽取与拼接共用有界转码队列。任务摘要通过 Web Locks/版本合并与删除墓碑保留跨窗口新增，入册失败返回操作 ID 和补偿状态；桌面自有网关重启先回收旧子进程，健康读取增加总时限与 16 KiB 响应上限。Node/前端门禁、视频契约和存储故障用例通过；本机未安装 Cargo，Rust 原生编译、Windows 进程树和设备验收未执行，保留为交付边界。

接手复核已找到本机实际 Rust 工具链并补全隔离原生验证（上段“未安装 Cargo”是前次 PATH 探测的错误结论）：完整桌面编译检查、11 项网关原生测试与 2 项真实 Windows 测试凭据写读删除通过。入册提交未知先读回、无法确认则保留原图；摘要改为 IndexedDB 同事务合并，删除墓碑也保留。旧摘要删除标记尚未引入带确认水位的压缩，不应直接按数量丢弃后使旧窗口复活记录。

仍未关闭的办公机外出口：CONTENT-01 单条正文漂移的权威来源/真实编译与画面；CONTENT-02 的 357 未登记/282 pending 形态、2,534 URL 实物核验与逐图决定；SEC-03 真实内容发布、完整远程页面浏览与安装资源验收；STORE-03 浏览器跨存储 journal 上线决策和物理断电；STORE-04 512 MiB 包的完整进程峰值/取消；PM-05 主力机 GPU、KeepAlive 长时资源和功耗；A9 完整安装、WebView2 实际导航/升级迁移与既有 V04/V05/V07 设备矩阵。个人密钥进入 Windows 凭据库/网页页面会话，站主服务端托管文件的静态加密不在此迁移范围。

2026-09-20 美术审计剩余五项前端实现与办公机桌面同步已完成，详见[项目状态](project-status.md)。尚待主力机补齐真实生图/视频、4K/Windows 150% 组合及设备全矩阵；办公机 WebView2 的主题、键盘搜索、缩放恢复和减少动态效果检查不替代这些项目。

### 长期架构优化

[实施计划](../plans/architecture-evolution.md) 已接入。A00 核对到主干 `1646c68`：A01.1 类型迁出、部分 A01.3 旧配方兼容和 A03 关键响应校验已随可维护性批次实现，不重复开发。本批 A01.2 窄输入与 A07.1 指定范围依赖检查已完成；办公机 structure 模式完整门禁通过，结果和不适用的模型/设备范围见 [实施记录](research/engineering/architecture-a01-2026-09-19.md)。本计划 A 编号与旧审计、既有 A–F 功能批次互不替代。

| 任务 | 状态 | 下一完成出口 |
| --- | --- | --- |
| A02.2 / A02.3 | SD 结果链路补齐，其他入口继续按需核对 | SD 直出/队列自动入册及共享手动/临时恢复上下文保留提交参数、场景标题与各自结果 seed/LoRA；入册异步加载前复制任务字段。兼容旧输入的默认值仍保留，不能把保存时默认值当作生成时事实 |
| A02 失败与补偿边界 | planned | 在机械抽取之外补清理失败的单独诊断；提交结果不确定时先核对实际适配器/身份契约，不以盲重试或异常后删图冒充恢复 |
| A03 剩余 / A04 后续 | WAI 编排已分离，其他引擎仍按需盘点 | WAI 已接入明确输入/任务类型、上游目录解析与独立业务服务；其他引擎与剩余响应字段继续对照现有实现盘点，不重复建设已完成边界 |
| A05 / A06 | deferred | A02 接口稳定或发现实际存储/生命周期缺口时启动；共享运行时、持久化任务恢复单独决策 |
| A07.2 | planned | 随迁移记录边界指标和兼容入口退役条件，不增加固定全仓审计 |

A01.3 已完成本计划作品链路盘点，复用主干的旧列表/新记录/恢复投影区分；场景加载与网络字段的其他类型边界不在本条完成范围。首批提交为 `e2c5979`；后续入册抽取的行为、兼容默认值和未解决的提交结果核对边界见 [A02.1 记录](research/engineering/architecture-a02-2026-09-19.md)。

2026-09-20 用户授权综合整理后，WAI 路由/业务分离、工作台草稿/筛选/入册适配、工作台目录校验和保存时快照已实施，完整入口已通过（蓝图事务本轮在原时限内完成），移除 A02.1 完整入口复验待办，不改写旧批次失败记录。末次缓存增量另有定向复验；最新范围及验收见 [维护性整理](research/engineering/architecture-maintenance-2026-09-20.md)。此批按当前授权跨原计划阶段处理，未迁移数据库或统一重写各引擎。

### 既有功能与设备批次

| 批次 | 优先级 | 主要任务 | 完成出口 |
| --- | --- | --- | --- |
| A：恢复基线（已完成） | P0 | 修复当前检查阻断；同步 009、010 和项目状态 | 当前版本的通过项与待验项有当次证据，后续不重复开发 |
| B：核心正确性（办公机已完成） | P0 | 作品册操作对象一致；提示词完整进入请求；异步结果不串到其他任务 | 不出现看着 A 却操作 B、界面选了却没生效 |
| C：核心功能精修（办公机已完成） | P1 | 作品册、提示词、随机组合、反推、素材应用和跨页面流程 | 常用功能形成可用闭环，而不只是入口齐全 |
| D：009 收尾（办公机已完成） | P1 | 连续交互、动效一致性、加载与资源占用、性能回归 | 快速操作和长时间使用保持顺滑、稳定 |
| E：010 深化（办公机已接线，设备待验） | P2 | 注册表接入、模型能力适配、桌宠行为与原生边界 | 桌宠行为一致、模型切换可靠且不打扰正常操作 |
| F：真机交付验收 | 交付门槛 | 真实生成、模型推理、语音和 Windows 桌宠验证 | 用真实环境证据证明可用，不以模拟测试代替 |

## 可维护性后续测量

M01–M10 的试点实现和当次证据只维护在 [项目状态](project-status.md#2026-09-19可维护性增量试点)。继续收集各 CI job 的缓存命中、构建耗时、产物上传/下载体积与墙钟时间；证据不足时保留独立构建，不启用跨 job 产物共享。更多模块的依赖规则按真实调用关系再决定扩展范围，不把试点通过当作全仓循环依赖审计。

PhotoSwipe 的实体触屏、Windows/Tauri 与大图集资源趋势，及最低 Node lane 在远端 Linux 上的实际安装运行结果，分别按设备与 CI 证据确认；本机 Node 22.18 烟雾不代替目标设备验收。既有主力机 F 项保持开放。

## GitHub 复用研究的接入边界（2026-09-19）

[GitHub 复用与参考项目调研](research/engineering/github-reference-research-2026-09-19.md) 的本轮实现与既有提交复审见 [实施审计](research/engineering/github-reference-implementation-2026-09-19.md)。P1 统一词条、随机种子/候选/快照、P4 任务阶段、P5 配方恢复及 P6 语音生命周期已落地；PhotoSwipe 5.4.4 为按需加载、可切回的 Spike，未替换默认观画入口。

余下工作按证据推进：

1. **观画试验设备验收**：实体触屏、Windows/Tauri、真实大图集上的缩放、拖拽边界、切图和资源趋势对照后，再决定是否正式替换默认查看器。
2. **009 性能测量**：S03/S08、四类 KeepAlive 的 GPU/纹理/进程内存与高负载趋势。现有 DOM/Blob URL 探针和 3,000 条分页测试不足以授权引入 TanStack Virtual；压缩仍待真实 manifest + 原图包需求。
3. **真实模型与陪伴验收**：固定种子和旧配方的最终编译请求/画面；真实 TTS 打断、换角隔离、原生 Live2D 音画同步。旧作品缺失的生成字段不能由推断补成已复现。

保留 TypeScript／Express／Tauri 架构、角色约束与分级边界；Reka UI、fflate 仍按实际需求与维护/许可/包体评估决定。010 的模型检查器、导入校准与 F 真机验收继续开放。

A 批已完成本地基线核对与文档同步：默认实物模式只被未配置的 2,534 个外部参考文件阻断，办公机 structure 模式下 17 项质量检查、Vitest、1,043 项 Node 单测、34 组契约和构建通过。外部参考文件继续留在 F/V01，不用降级结果宣称实物通过；远端 CI 状态因本机未安装 GitHub CLI 未在本批读取。

B 批已在办公机完成：作品查看器以稳定 ID 绑定操作对象；色彩情调完整进入 Anima 标签和 Krea 自然语言请求；在途 Anima 结果继续使用提交快照而不是后来表单。定向单测、编译测试、质量门、构建预算及 4 个模拟上游浏览器请求／交互用例通过。真实模型画面未生成，不用请求字节正确关闭 F 批。

C 批已在办公机完成：手动词条、反推与一键衣装共用正确的互斥语义；随机灵感与撤销、完整蓝图导入导出、仅画面描述草稿、历史与热门／工作室跨页链均有定向回归。WD14 已在隔离测试图上执行真实本地推理，但用户素材和生成画面的视觉结果仍归 F。

D 批完成 009 的办公机 F4/F6 与 F5 生命周期范围：导航/浮层连续性、低效果粒子、隐藏页健康轮询、S04 正确测量及构建预算均有当次证据。S03 与真实 GPU/纹理/内存/功耗仍需目标 Windows；本轮完整门禁因单契约文件外层 180 秒超时未获总 PASS，但该文件直接执行 9/9 通过。此处为 D 批当时的验收记录；E 批接线已在后续完成，当前实现见项目状态，设备验收仍归 F。

## 2026-09-18 存储修复后的剩余边界

作品清理的重扫、跨窗口/图片暂存保护及备份双向容量限制、逐图编码和取消入口已补入源码，范围见 [项目状态](project-status.md#2026-09-18作品清理与备份安全修复)。剩余为实际大图库/低内存环境验收、分卷或流式恢复设计，以及更新后的桌面多窗口验证。不要把 JSON 上限提高当作大型备份方案，也不要为了自动清理而放开对未入册草稿的保护。角色绘画工具真实任务接线、多引擎能力补齐和素材补全继续按原范围执行，本批未启用或冒算完成。

## PC 桌面架构整理后的验收

UI 精细打磨见 [专项计划 007](../plans/007-ui-detail-polish-and-review.md)。U0–U5 已有分批验收，后续玻璃、工作台阅读/窄窗口及导航动效已完成记录范围内的浏览器验证；不再从角色选择或原始 U0/U1 启动任务重做。剩余项目如下：

| 优先级 / 范围 | 剩余任务 | 完成条件 |
| --- | --- | --- |
| P1 / U6 后续补丁交付 | 将玻璃、工作台与导航收尾的最终受控构建同步到桌面安装版 | 按 [部署指南](desktop-deployment.md) 唯一入口执行；核对安装目录产物与构建身份、实际网关/素材配置及重启结果；历史 U6 部署不替代新补丁交付 |
| 发布前 / 原生体验 | WebView2、4K / Windows 150% DPI、应用缩放与恢复、低效果/系统偏好、真实素材主要流程 | 安装版中完成深浅主题、键盘/焦点、快速切页、缩放与重启验收，保留真实截图和操作证据；对应 V04/V05/V07 |
| P2 / 尚未覆盖的 UI 状态 | 按具体缺口继续检查业务弹窗、图片背景叠字、高密度页面及错误/忙碌状态 | 明确页面与状态后定向改进、双主题复验；不将零横向溢出或已有批次通过当作全站全部状态完成 |

执行与交接见 [专项计划 005](../plans/005-desktop-architecture-consolidation.md)。保留现有技术栈与业务行为，以主力机 4K / Windows 150% 缩放为主要验收环境。

**办公机与主力机分工（用户已确认）**：办公机先完成前后端实现、模拟上游／隔离数据验证、故障注入及提交推送；主力机同步后再做真实模型、GPU／显存并发、模型切换、WebView2、多屏／休眠和安装迁移验收。涉及显存调度、模型卸载或生成参数的性能优化，先在主力机测出瓶颈再改；不能用模拟测试代替真实负载结果。

布局、尺寸、滚动职责及工作台接口整理不再列为开发待办。005 的存储比较与恢复故障注入已完成计划内评估；继续使用 IndexedDB，现有恢复行为保留。005 评估的新增恢复/存储方案仍需另定实施范围，不自动迁移用户库或继续生成。

剩余主力机验收继续按 V04、V05、V07 记录；本机模拟测试不关闭设备缺口。执行证据与验证修正见计划 005 文末。

## 后续工作流与内容数据治理

详见 [专项计划 006](../plans/006-workflow-and-content-governance.md) 文末状态表与 [办公机工程交付](archive/audits/office-engineering-completion-2026-09-15.md)。蓝图/场景事务、变更集界面、资源生命周期及网关、人工审核发布链和跨进程恢复已接通；以下保留实际未覆盖范围。

| 优先级 / 阶段 | 剩余任务 | 完成条件与边界 |
| --- | --- | --- |
| P1 / R1 资源实物验收 | 在目标安装版核对基础/可选资源、真实离线包、升级保留、断网首次打开与画质 | 下载/导入/取消/恢复和 base/full 机制已通过隔离验证；外部来源、实际包体、Live2D 全链与 4K 展示仍需主力机证据，不默认下载或上传 |
| P2 / W1 实际运行条件 | 真实设备、账户及外部服务条件验收 | 注册元数据、零副作用帮助/预览和条件审计继续复用；本机模拟通过不代表外部环境可用 |
| P2 / W2 双机交接 | 主力机接收最终提交与对应构建，执行安装及设备/模型验收并登记新证据 | 源/构建自动失效和最终提交绑定已实现；不能用 HEAD 匹配关闭 installation/deviceAcceptance/modelAcceptance |
| P2 / D1–D2 完整字段覆盖 | 按实际新增或变更字段补齐未导出的语义规则、读写依赖及资产影响 | 当前 check:impact 仅对可证明边界执行增量；否则完整结构检查，DATA_VERSION、压缩及私有语义仍需 full。没有完整性证明的域继续 unknown，不放宽门禁 |
| P2 / D3 真实来源与审核 | 对实际候选逐图执行人工审核、发布与源版本对账 | 记录/哈希/审核时间绑定和不可变发布机制已实现；审核者真实性、图片质量与真实来源仍需人工证据 |
| P2 / D4 硬件故障验收 | 实际安装环境的断电、磁盘故障及外部写入器协作验证 | 双进程争锁、SIGKILL、回滚及恢复中再次中断已通过隔离测试；不能等同物理断电持久性；未知 PID/损坏 journal 保持阻塞 |

[GLM 分批任务包](guides/engineering/glm-flash-next-batches.md) 保留历史拆分线索，实际状态以本页为准，不重复实施已接通的保存链。安装版用户自建源内容、资产存储治理和新编辑器另定范围；先取得实际占用/耗时/失败案例，不默认迁移内容数据库或用户库。

## 叙事 CG 提示词对照验收

补充 [手机竖屏壁纸](guides/prompts/mobile-wallpaper-prompt-standard.md)，以 iPhone 17 Pro（1206×2622）为主并兼容通用长屏，锁屏／主屏分别验收。另有 [成年角色非露骨 CG 研究](research/prompts/adult-nonexplicit-cg-reliability-2026-09-13.md)，仅覆盖明确成年、非露骨创作和中性一致性排查；不包含露骨内容优化或分级边界修改。两者均待实际候选与设备验证。

同等关注 [单人物立绘与特写壁纸](guides/prompts/character-wallpaper-prompt-standard.md)，面向 4K / 16:9 桌面；以人物轮廓、表情、衣装、留白和裁切为独立目标，不强制环境叙事。实验计划分 narrative／wallpaper 两组记录，先分别小样筛查，不自动扩批。

[写作规范](guides/prompts/narrative-cg-prompt-standard.md)与[研究](research/prompts/anima-krea2-narrative-cg-research-2026-09-12.md)已形成，创作建议尚未证明本地画质增益。主力机按[未执行实验计划](research/prompts/anima-krea2-narrative-eval-plan.json)先做单 checkpoint 小样，再比较多场景与 seeds；保留全部输出和硬条件失败。重点核查 Anima 描述筛选／默认尾句、Krea 实际增强链路及字段到最终请求的保真。本轮不改生产场景、定稿或模型参数。

## 后续工具接线

审计 A01–A05 的代码修复与行为回归见 [1.7.1](releases/v1.7.1.md)，原始失败证据继续保留。角色绘画工具当前如实返回草稿；后续若接入真实受控生成队列，须区分 queued/running/succeeded/failed，并在主力机完成真实出图后再启用图片结果与成功奖励。

## 素材与真实设备待验收

以下缺口来自办公机/隔离验证阶段，继续保留，不标为代码失败或本次清理完成。历史数量仅是交接检查点，需要在对应环境重新对账。

| ID / 优先级 | 未完成内容 | 完成条件与入口 |
| --- | --- | --- |
| V01 / 发布前 | 外部参考文件、URL、样张发布清单与素材默认模式验收；9 月 12 日索引快照有 1,974 个 pending，本次未重新盘点 | 实际素材根目录逐文件检查；逐图记录审核与发布决定；主力机不设置 structure 降级。见 [工作流](workflow.md#参考库) |
| V02 / P1 | 角色立绘、服装七视角、剩余样张；新49角色及 16 蓝图/4 服装批次的源证据对账 | 按角色/服装/蓝图 ID 对齐源码、manifest、真实图片和人工决定；登记或合并不能代替交付。见 [批次记录](archive/README.md#batches) |
| V03 / P1 | 11 条新增服装覆盖场景与两处环境词迁移尚待真实出图 | 从 [固定 ID 清单](../scripts/tests/fixtures/scene-coverage-repairs.json) 复核最终编译请求；固定模型/参数生成并人工逐图验收，保留定稿保护 |
| V04 / P1 | 已安装桌面程序的看屏、麦克风/ASR、TTS、工具取消、异常释放与 Web Locks | 在目标安装包、实际设备与兼容 API 上端到端验证；覆盖来源拒绝、资源释放、旧窗口刷新和重启持久化 |
| V05 / P1 | Live2D 完整链接与运行、两角色动作/换装/叠层、三档纹理、隐藏恢复、窗口跟随、DPI/多屏/休眠、音画同步与闲置功耗 | 本次纯 Rust 节拍测试不代表 overlay 或 GPU 验收；需原生启动、逐项操作与 CPU/显存/功耗记录。见 [原生运行时](guides/desktop/live2d-native-runtime.md) |
| V06 / P2 | 视频首尾帧、重复生成/停止、成片、超分/Wan 增强的实际效果与资源成本 | 真实任务从首帧到成片可追踪、失败可恢复；固定 seed/参数对比，不能用模拟上游代替 |
| V07 / 发布前 | 当前依赖与 exe 的完整安装、迁移及实际推理；主力机控制失败契约复验 | 使用 [部署指南](desktop-deployment.md) 的唯一入口，完成安装/UAC、迁移、运行和控制失败回归；doctor 就绪或依赖零告警不代表安装通过 |
| V08 / P2 | 角色设定/记忆的真实 LLM 一致性、召回与延迟；桌宠事件/勿扰/语音演出的实机体验 | 来源可追踪、无角色串设定、记忆可撤销；真实设备验证不打断用户输入与重播 |
| V09 / P2 | sc063、sc087 评级 All→R15（2026-09-13 人工裁定：bottomless 半脱内容原标 All）后的最终编译请求与画面分级核对 | 主力机固定参数真实出图，核对正向 `sensitive` 评级词与负向安全垫（child/loli/underage 替换裸体压制）的实际画面不越 R15 边界；画面越界时回退评级或修提示词。裁定记录见 [人工定级表](../scripts/lib/manual-scene-ratings.js) |

## 内容与性能待完善

**流畅度专项**：执行与证据见 [计划 009](../plans/009-ui-fluidity-and-performance.md) 和 [项目状态](project-status.md)。办公机代码及定向回归已完成；S03/S08、GPU/纹理/进程内存、高负载与 Windows 安装版仍待目标设备验收，关联 V04/V05/V07/U6。

- **P2 / 包体及真实流畅度**：9 月 15 日导航阶段产物仍有预算压力：工作台 CSS 103.6 / 115 KiB、入口 CSS 99.1 / 100 KiB、Live2D 懒块 923.4 / 1000 KiB、CompanionView 静态闭包 533.1 / 580 KiB；未超硬预算。先在同机同负载记录冷/热进入、首次操作、内存与 GPU 成本，再定向优化。220ms 入场是配置时长，不是整页加载性能测量；旧包体余量和历史 PASS 不替代新基准。来源见 [导航构建记录](archive/audits/ui-navigation-and-unused-2026-09-15.md)。
- **剧情与剧本丰富度**：保留内容建设需求；具体题材和批次按新任务确定，不用历史配额机械扩写。
- **P2 / 聊天归档并发**：草稿与音量已独立持久化，未变更归档不再重复写入；归档本身仍需解决多个窗口同时新增/清空时的旧快照覆盖，补充清除版本或等价协议后验证，不能靠盲目合并使已清空记录复活。此项为原有风险，普通聊天消息的跨窗口合并测试不代表归档并发验收。

## 暂停与待决策

| 状态 | 保留事项 | 恢复条件 |
| --- | --- | --- |
| 暂停 | 原场景语义审计与故事/提示词裁决；历史检查点为 467 条记录、290 张待人工审核候选 | 用户明确恢复后按 ID 核对当前源与候选。数字来自 [暂停台账](archive/audits/all-scene-audit-2026-09-08.md)，本次未重算候选库，也未恢复出图 |
| 待决策 | Qwen-Image-Edit / SAM3；Krea 2 高级增强、PromptWeight / StyleReference | 先确认设备、依赖、来源、数据去向、成本和收益，再决定下载/接线；旧权重或显存结论不作本机事实 |
| 待另定范围 | 005 新增恢复方案及用户作品存储迁移 | 005 已完成评估及隔离原型，当前继续使用 IndexedDB并保留既有恢复行为；有明确需求后单独确定迁移、回滚、幂等与取消边界，不自动迁移用户库或重启生成 |
| 暂缓 | [反推满意图入场景（plans/004）](../plans/004-scene-save-from-interrogate.md) | 有新需求或资源条件后重估；保留原暂停决定 |

已完成工程项不再复制为待办。关闭项目须附本次对应环境的证据；混合报告归档、结构门禁通过或只生成候选均不能关闭素材/真机项目。粒子肖像属于保留功能，按 [粒子管线](guides/characters/particle-portrait-pipeline.md) 维护。
