# 011 执行记录

执行环境：Windows、Node 24.18.0、npm 11.16.0；隔离 worktree `3891`，分支 `codex/011-resource-boundaries`，单主会话，无委派。初始 HEAD/远端 main 均为 `1ec0d1d1e364c6ef6b9e74fa4cea0cc31fbc8f15`，暂存及工作区差异为空。按锁文件 npm ci，未升级依赖。A0–A9 保留，不重复实现。

## B1

执行前快照 `runtime/delivery-evidence/011-b1-before.json`；捕获工具首次因未选择 build 被拒绝，纠正后重新运行反例，不补签早期探针。中性探针确认 version 999 → 3 且 sentinel 丢失。新增共享版本校验，支持无版本及 1–3 聊天格式、无版本及 v1 归档/记忆；拒绝未知新版与非法版本，不迁移退休记录、不清理凭据、不回写原件。保存前重读，恢复替换亦校验现存格式。退休数据和个人档案无版本，保持既有字段契约。

首轮单测 28/29：高频偏好不应读取整份历史；修复为独立键写入，已知受保护状态拒写。Node 首轮发现 TS 导入扩展名及旧测试允许非法版本；修复导入，新增拒绝断言，保留合法版本字段清洗断言。定向前端 29 项通过、Node 2 项通过、应用类型检查及生产构建/原预算/预压通过。浏览器 2 项通过，深浅主题、双页、切页和重载后未来数据原字节相同；端口偏移 17000，独立 runtime 与非持久 Edge。日志/截图 `runtime/audit-011/b1-*`；浏览器执行前身份 `runtime/delivery-evidence/011-b1-browser.json`。

本段为 B1 当时的阶段结果；最终集成与交接见 B7。生产数据、真实模型、安装、设备验收始终未执行。

## B2

精确删除集合：主聊天所有 histories/settings.drafts、归档键、事实记忆键、退休角色键、CHAT_DRAFT_PREFIX 全部角色、个人档案；会话语音由现有 voice.stop 清除。保留 API/凭据、aics_chat_api_drafts（连接配置草稿）、音量、主题、行为及作品库。新增不参与备份的清除修订号，旧窗口保存先丢弃旧内存；直接保存旧记忆快照亦拒绝。清除先写墓碑，逐项汇总失败，已删除项不补偿复活，可重试。

原实现遗漏由源码调用链及单类型中性夹具确认；未留存改动前删除反例执行日志，不能冒称满足先行动态反例步骤。最终前端 37 项通过；生产构建/预算/预压、对比度扫描、定向 ESLint 通过。浏览器四项（含 B1）通过；首次主题夹具存在 SPA 覆盖，截图发现后改初始化并增加 html data-theme 断言，重新四项通过。截图/日志在 runtime/audit-011/b2-*，最终主题日志 b2-browser-theme.log。未真实操作生产数据。B1 提交 aa2e51d 已推送。

## B3

执行前回环 HTTP 中性反例确证缺终态仍 onDone 一次，见 b3-before.log。共享 boundedLines 在解码/拼接前计数：单帧 1 MiB，总 16 MiB，包含注释空行；request 可选绝对 deadline 为聊天设 10 分钟，不替代 idle timeout。推理 20,000 字节、每工具参数 4,000 字节、最多 8 个，保守不超过已有续聊输入字符上限。有效 finish_reason 或 DONE 可终结 SSE；非流式须有消息；Ollama EOF 缺 done 拒绝，工具整批 JSON 验证后才发布。

运行时首次构建因 CommonJS import 失败；紧接的旧 JS 测试通过无效，不作为证据。修正并重建后，既有测试的工具参数夹具竟为非法 JSON（尾随点）；改为合法分片并保留拼接断言，未放宽生产检查。最终聊天 4 项（含 UTF-8、缺终态、重复 DONE、帧超限、推理超限、40ms 滴流截止、Ollama 队列释放），HTTP 8 项、队列 2 项及单体门禁通过。日志 runtime/audit-011/b3-*；无真实模型。

## B4

执行前从实际 GlobalSearch.vue 提取 loadWorks 并用 TypeScript 编译执行，301 输入仅索引 300，最旧关键词 false（b4-before.log）。新增轻字段全集索引，复用 parseArtworkRecords/artworkTimestamp，作品册和全局搜索统一标题/角色/场景/项目/提示词与空白分词 AND 匹配；排序稳定，匹配后限制 5 项。读取失败可见，重开重新读取，请求序号阻止旧响应覆盖。

0/1/300/301/1000/10000 七项单测通过，双主题 10k 最旧命中及 Enter 正确 ID 跳转 2 项通过；端口偏移 17000，首次打开到命中 470/123ms（含浏览器等待，不是纯计算）。应用类型、生产构建/预算/预压通过。日志与截图 b4-*。B3 提交 4b0a9a8 已推送。

## B5

真实路由、唯一临时素材根连续上传同一 2×2 PNG，修复前返回两个随机文件名、2 文件；修复后相同身份返回同名、1 文件（b5-before-retest/b5-route-after）。首个探针装配 router 返回值错误，修正后才得到反例。素材按身份+原始字节 SHA-256 命名，重用前核对实际字节；原有名字继续可读。Anima 和视频共用扫描账目、跨进程目录互斥、两项待处理队列、独占临时文件和硬链接原子发布；失败移除本次临时文件。

默认 512 MiB/1024 文件：容纳至少 25 张原有最大 20 MiB 输入，防止无限增长；服务端 AICS_IMAGE_STORAGE_BYTES/FILES 有界配置（非请求参数）。每次准入重新扫描受控文件，重启不丢账目。旧参考和未知归属保留，额度不足拒绝。Sharp 在 8192 边长、32M 像素、单帧和 5 秒限制下真正验证压缩数据，浏览器缩略图在解码前检查头部、两路并发上限，未知格式仅跳过缩略图。

视频完整契约通过，Anima 8 项、浏览器解码预算 1 项、应用类型/运行时/单体/定向 lint 通过。首次旧写盘故障注入依赖文件前缀，临时文件保留操作前缀以继续命中；随机 16 字节文件名断言只对上传更新为 40 位摘要，尾帧仍保留原断言；修改 PNG 尺寸未重编码的旧夹具改为真实 Sharp 中性编码，原画幅断言保持。日志 b5-*。

回收边界：浏览器项目/未保存会话的完整引用无法由网关证明，故不新增自动删除参考入口；先由操作员检查引用再整理。崩溃遗留锁 fail-closed，不根据时间猜测失效；停网关并确认无上传者后检查该专用锁内本次临时文件再清理。该限制及真实磁盘满/大图峰值继续在主力机验收，不以本轮模拟关闭。

## B6

执行前临时 Git 仓库模拟 A 产包→B 同版本干净源码，运行实际 release 主流程（假 wrapper 停止点，零真实签名/上传），旧 payload 仍进入封装，见 b6-before.log。现复用 delivery-identity 的 snapshot/validateSnapshot，不自建哈希算法；runTauri 在既有锁内捕获源码，成功后绑定实际 dist/resources/native/NSIS。no-bundle 不绑定遗留包，bundle-only 必须先验证原生/资源再扩展。封装入口及发布入口都拒绝缺回执或身份漂移，分发 exe 另绑定并复核。仅文档变化允许，相同源码后提交允许。

运行时构建通过，desktop-updates 11 项、desktop-staging 10 项通过，包含同版本陈旧源码、产物篡改/缺失、空回执、文档变化、先捕获后绑定的调用顺序及已有 draft 上传失败策略。日志 b6-*；反例改后在缺回执处拒绝（b6-after.log）。未调用真实签名、Release 或安装。

## B7：最终验证、身份与交接

最终源码提交 `5aff1ff69eff63dc4dd37921d3beca768f890e2e`（后续文档提交不改变该受测内容）。六批提交依次为 aa2e51d、bfd6704、4b0a9a8、7558e5f、cbab4e3、ff2a49a，集成修订 5aff1ff；每次提交均已推送任务分支。主干按同源快进交付，不覆盖其他会话改动。

最终 `AICS_REFERENCE_AUDIT_MODE=structure` 下 `npm run wf -- gate:full --all` **PASS，退出 0，5m27s**：质量编排 21 步、前端 162 文件/1136 项、Node 1077 通过/2 跳过、契约 35 文件、生产构建/原预算/预压。32 个不同受影响 critical 浏览器用例：首轮 31 通过、1 个旧损坏 JSON 自动清空断言失败；保留旧版/凭据迁移断言，将损坏部分改为原件保留和发送禁用后，单项补验通过。最后测试类型检查通过。生产源和 dist 在浏览器期间不变。实际命令、哈希与数量见 [脱敏证据](011-evidence.json)。

执行前主快照 `runtime/delivery-evidence/011-b7-final3.json`；结果绑定并关联源码提交的回执 `011-b7-verified.json`；修改过的旧浏览器用例另有执行前 `011-b7-legacy-browser.json` 与 `011-b7-legacy-verified.json`。源身份 `949aad7373133b98f47eee200f32e28d5c448136734a2d9f46729fe11948d337`，整个 dist 身份 `71b0cb83e1f29caeeeeeb25b5f2e47247ddba4282425ffdf0cbeeb8adff4ccba`。最终构建再次执行后身份仍 fresh。原始日志、截图仅保留本 worktree 的 runtime/audit-011，不放入生产素材或 Git。

### 失败与补验不能合并冒算

- 首轮完整入口失败：本地 CRLF 格式及 4 项旧前端断言；修正换行、把伪图片 Blob 改为合法中性 PNG、损坏 JSON 改为拒写断言，定向 22 项通过。首轮其余通过不当作最终全量结果。
- 第二轮失败：新增浏览器 spec 缺 CI lane 登记，补齐后分组 2 项通过；未改动的资源安装恢复跨进程测试一次 ENOENT，原测试定向 14 项通过，最终全量也通过，历史失败保留。
- 最终轮准备构建首次发现错误码可能为 number 的类型错误，显式字符串化用于匹配，重建通过后才捕获 final3 快照。最终全量入口本身全部通过。
- 早期 B1/B2/B4 浏览器快照误选 `dist/manifest.json`，记录为 missing，未补签成完整构建通过；真实清单为 `dist/.vite/manifest.json`，final3 改为整个 dist，身份 complete。早期截图主题初始化错误已修正并用 html 主题断言重验。
- 最后浏览器损坏 JSON 旧断言失败和 1 项补验分别保存为 b7-browser.log / b7-browser-retest.log，不宣称首轮 32/32。

### 删除对象表

| 对象 / 键 | 写入者及内存副本 | 清除与导出 |
| --- | --- | --- |
| aics_chat_v1 histories、settings.drafts | useChatStorage / histories、drafts | 所有角色清空，API 与其他设置保留；新导出无旧内容 |
| aics_chat_archive_v1 | useChatStorage / archive | 整键清除，旧窗口清掉归档快照；支持版本守卫 |
| aics_chat_memories_v1 | chatMemory / byCharacter | 整键清除；旧快照带清除修订身份，迟到保存拒绝 |
| aics_retired_companion_chat_v1 | retiredCompanionChat | 所有退休角色清除；不因重新注册恢复 |
| aics_chat_draft_v1: 全部后缀 | 主窗/独立聊天窗 | 精确前缀涵盖退休角色；清除事件丢弃旧输入，新输入可保存 |
| aics_user_profile_v1 | chatUserProfile / 档案表单 | 整键清除、跨窗刷新，不再因没有消息提前返回 |
| 本会话语音缓存 | useVoice | 停止请求与声音并释放消息音频；不导出 |
| aics_chat_api_drafts、连接/凭据、主题、音量、行为与作品库 | 原仓储 | 保留；连接配置草稿不是消息草稿 |
| aics_chat_reset_v1 | 清除用例 / 每窗口修订 | 保留墓碑，不参与备份导出/恢复 |

### 集成复核补充

新版事实记忆同样使聊天进入保护状态；损坏 JSON 的兜底改写路径已移除，备份非法版本也拒绝。保护提示持续可见，发送按钮禁用。搜索读取中与失败不再同时显示零匹配。上传加入断线取消，两个独立 Node 进程争最后一个配额只能成功一个；目录私有 32 字节盐＋身份＋原始字节 HMAC-SHA256 命名，避免外部根据已知图片推算本机名称，实际字节仍二次核对。盐是固定元数据，原“1 文件”去重结果指 1 个参考，当前另有 1 个私有盐文件。拒绝动画 PNG/WebP、损坏/截断和超尺寸输入，文件系统错误响应不暴露本机路径。

现有维护租约包含备份事务，桌面构建锁会处理过期所有者，不能直接套用到用户素材根；新增素材排他目录锁采用保守不抢占策略。未知引用没有可信的跨浏览器集中清单，因此不新增自动回收；这一项是有意保留的产品边界，不宣称已实现自动垃圾回收。

### 回退和主力机剩余

出现回归时在隔离分支只回退对应 UI/协议适配差异，保留未知新版拒写、清除墓碑、资源硬上限与发行预检，再运行该批相同定向验收后受控合入。不得把旧本地数据或导出文件恢复到用户已主动清空的库。上传问题先停止新上传，再检查专用锁和本次临时文件；不清空 input 根，不删除参考或私有盐。发行回退选择原源码及匹配回执/原精确 payload，不生成替代密钥，不把同版本重建包冒作原发行。

未执行生产库操作、数据库迁移、真实模型、真实签名/Release、桌面安装/UAC。主力机须验证真实长回复/工具流和取消、真实大图/素材额度/内存峰值、升级与回滚的版本保护、真实安装包身份；上一轮真实素材/出图/GPU/设备验收继续保留。data 与 assets 对初始基线零差异，提示词、蓝图、服装、分级和定稿未改。
