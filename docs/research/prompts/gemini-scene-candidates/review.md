# Gemini 场景候选复核与三条修订草稿

2026-09-13。复核 selection、四批概念与 12 条 shortlist，并检查对应项目源数据。本轮不修改 Gemini 原稿，不写生产场景或定稿，不调用生成服务。

首次归档仅包含本复核与 reviewed-three.json。经过下文 v2 修订复核后，selection、四批概念、catalog 和 shortlist 一并按待编译/待出图草稿归档；未选 48 条仍待完整内容复审。sourceDraftId 用于对照原交付，不表示已有生产场景 ID。

## 验收结论

60 份概念有可保留的具体画面构思，结构检查通过，但 12 份深化稿尚不满足入库条件。尤其不能以自查中的“100% 忠实”“成功率极高”代替源记录核对或实际图像验证。

| 检查 | 结果 |
| --- | --- |
| 四批数量与唯一 draftId | 60 条、60 个唯一 ID、20 位角色 |
| 用途分布 | 叙事、PC、手机各 20 条；shortlist 为 12 条、三类各 4 条 |
| 概念与 shortlist 的角色/服装 ID | 与当前对应源记录匹配；ID 存在不代表描述遵循衣装 |
| 相近记录的引用 | 6 条使用场景 ID，36 条能按 `pc_<characterId>_<blueprintId>` 映射，18 条无法在当前场景/蓝图库中解析，不能计为已完成有效对照 |
| 真正编译与真实出图 | 未执行；不承诺成功率、人物一致性或画质增益 |

未解析的对照集中于后藤一里、芙莉莲、阿尔托莉雅、雷电将军、御坂美琴、明日奈，每位三条。它们可能是历史样张 ID，需补原始清单依据或重新从当前蓝图库选对照；不能直接删掉 pc 前缀猜测实体 ID。

## 主要内容问题

1. `draft_c02_02`：Anima 使用 `natsume_(senren_banka)`，与源身份 `shiki_natsume` 不一致；`casual_knit` 是针织毛衣、围巾、裙与裤袜，原稿却变成外套和插外套口袋。已在独立修订稿纠正。
2. `draft_c10_03`：绑定 modern_clothes，源衣装为现代紫色裙装，双引擎却改为 kimono。场景创意可以成立，但应忠实已绑定衣装，或先选真实匹配衣装，不能原 ID 配另一件衣服。
3. `draft_c13_02`：源 casual_shopping_date 为粉色毛衣等私服，正文改为 puffer coat。`draft_c07_02` 漏掉源针织开衫；`draft_c16_02` 把 knee-length stage dress 扩成大礼服，均需核对衣装。
4. `draft_c11_01`：源身份写 junior-high，Krea 写 high school；源服装包含高马尾，正文写垂落长发。自查不能无条件写“完全吻合”。
5. `draft_c01_01`：把原创光蝶意象写成《魔女的夜宴》的“死神之蝶契约核心意象”，没有对应来源。修订保留原创光蝶构思，撤回该原作断言。
6. 宽泛的 exposed skin 负向可能抑制正常脸部和手部，不应作为全年龄通用配方；多数负向与“极高成功率”判断未经本地测试。
7. 手机稿均以 9:16 编写，缺少面向 1206×2622 长屏的具体裁切验证；并非 9:16 一律不可用，但不能据此直接声称锁屏适配完成。
8. catalog.md 部分衣装名与已修正的 JSON 不一致；原 check-catalog 以 selection 自建清单为主要对照，且会写 catalog，并非纯只读验收入口。本次使用独立读取核对实际源数据。

没有对 60 条做完整原作考据，也没有证明每条与现有内容都存在充分差异。尚未修订的 57 条概念和 9 条深化稿继续保留为待审。

## 三条实际修订

完整内容见 [reviewed-three.json](reviewed-three.json)，保留 sourceDraftId 以便与原稿比较。

| 原稿 | 用途 | 修订重点 |
| --- | --- | --- |
| draft_c01_01 · 旧书架前的一点微光 | 叙事 CG，4:3 | 原创意象与角色事实分开；书本支撑、指尖与光蝶间隔、三层空间和光源关系明确 |
| draft_c02_02 · 初冬站台的等候音符 | PC 壁纸，16:9 | 恢复正确夏目身份和针织衣装；持票与拢围巾动作；人物全身与左侧低对比环境并存 |
| draft_c05_03 · 纸箱边缘的小小招呼 | 手机壁纸，9:20 草稿 | 脸与蓝黄发饰保持可见；手与箱沿分离；吉他包明确放在箱旁；长屏锁屏/主屏分别验证 |

修订稿分别写 Anima 与 Krea，保留身份/衣装源快照；Krea negative 为空。推理尺寸未指定，目标显示像素不冒充模型推理参数。所有结果仍为 draft、未编译、未出图、未视觉验收。

下一步宜对这三条准备真实编译请求、明确模型配置，再按现有实验计划小样出图；办公机不因为候选写完就关闭主力机验收。不要继续用扩批数量替代这一轮效果验证。

## 2026-09-13 再次复核：Gemini v2 修订

上文保留第一轮问题记录。新交付的 60 条 baselineComparison 已改用明确 referenceType/referenceId/referenceTitle，本轮逐项核对当前场景/蓝图库，ID、标题与角色归属均匹配；12 份 sourceSnapshot 与实际身份词、衣装 prose 对应。主要跨作品身份、年龄、发型和衣装漂移已修正，Krea negative 均为空。

Gemini 自带只读检查报告零错误，但未覆盖全文同步与虚假验收文字。独立补检检出 7 处标题未同步、4 处手机比例未同步、对应目录标题陈旧，以及 3 条未出图却写“锁屏验证通过/绝不削顶”的检查项。夏目遴选理由和概念仍有旧大衣插兜动作。

主任务已直接收尾：

- 按 12 份深化稿同步所选概念的标题、比例、动作/衣装概要与目录，保留未选 48 条概念；修正夏目持票/拢围巾及雷电现代紫裙的旧差异说明。
- 统一将桌面/手机验收文字改为待出图检查，明确留白比例不是遮挡保证；右手持零食、两指持针等自查不再与正文冲突。
- shortlist.md 从 JSON 正文生成，避免重复手写带来漂移；reviewed-three.json 和生产 data/assets 均未改动。

验证：`verify-all-candidates-readonly.cjs` 的 60/12 数量、引用、分布检查通过；独立 `scripts/archive/gemini-scene-review/check-v2.cjs` 在收尾前检出问题、收尾后零错误，覆盖源快照、标题/比例同步、空负向和未验收状态。同步脚本为同目录 `sync-v2.cjs`，仅维护候选文档。检查均未调用模型。

当前按**待编译/待出图候选归档**提交，不是生产场景交付。引用存在不证明已经找到语义上最相近的旧场景，未选 48 条仍未做完整内容复审；12 条镜头和接触关系的实际效果留待主力机验证。本次未部署桌面版。
