# 007：UI 细节打磨与分批审核

> 2026-09-15 状态整理：U0–U5 保留各批验收记录；液态玻璃复审见 §11，工作台阅读与窄窗口打磨见 §12，导航与动效收尾见 §13。U6 历史部署记录不变，后续补丁尚未同步安装版，真实设备验收仍未完成。
> §1–§7 保留原始方案、执行与审核约定，不是重新派发已完成批次。后续用户授权的直接修复与清理以各节记录为准；剩余任务及优先级只维护在 [未来规划](../docs/roadmap.md)。
> 初始已提交基线：`e82329e`，不代表当前 HEAD。开始新工作前核对最新提交与工作区。
> U0/U1 交付包：`scripts/archive/ui-polish-007/`（本地、被忽略）——`U0-control-inventory.md`、`DELIVERY-U0-U1.md`、`REWORK-U0-U1.md`、`env-3210.json`、`shots-before-3210/` ↔ `shots-after-3210/`、`video-keyboard-flow.webm`、`e2e-targeted.log`、`reviewer/`。

## 当前状态（截至 2026-09-16）

### 已完成

- [x] U0–U5 已完成记录范围内的批次验收：角色选择、场景/参数、浮层/结果、玻璃材质推广与排版收尾均通过浏览器范围审核。
- [x] §11–§13 的液态玻璃复审、工作台阅读/窄窗口打磨、导航动效收尾和旧模块清理已完成对应源码与浏览器回归。
- [x] 双主题、390/1440 视口、键盘/焦点、减少动效、快速切页、面板草稿和无水平横滚等已覆盖的状态均有记录证据。
- [x] 2026-09-16 关键浏览器回归 312/312 通过；本轮补齐桌宠聊天窗、控制室导航、减少动效加载条、对比弹窗焦点及场景保存测试契约。

### 待完成

- [ ] 将后续玻璃、工作台和导航补丁同步到桌面安装版，完成 U6 交付闭环。
- [ ] 在真实 WebView2 和 Windows 100%/125%/150% DPI、双屏及原生缩放环境完成安装版验收；浏览器模拟不替代实机证据。
- [ ] 按具体缺口继续检查尚未覆盖的业务弹窗、图片背景叠字、高密度页面及错误/忙碌/禁用等控件状态。
- [ ] 补做真实素材流程及必要的模型/设备联动验收；已通过的浏览器回归不等同真实出图交付。

## 1. 目标与已确认方向

沿真实创作流程，把控件一致性、信息层级、浮层布局、材质和操作反馈逐项打磨到可交付：

**选择角色 → 选择场景 → 调整参数 → 提交与等待 → 查看、比较和保存结果。**

用户已经确认的方向，执行者不再重新解释成其他风格：

- **同心圆角**：带胶囊外框的分段切换，选中块也为胶囊，端部与上下等距，内外弧线协调。参照工作台“场景模式 / 专家模式”的关系；不能再用大胶囊包小圆角矩形。
- **形状分工**：普通按钮、输入框、卡片和弹窗用按尺寸分级的规则圆角矩形；圆形图标按钮保持正圆。不能把全部控件改成胶囊。
- **材质与背景独立**：液态玻璃不要求彩色背景。保留现有背景体系，不为突出玻璃新增彩色渐变、光斑或壁纸。
- **内容优先**：玻璃服务于导航、悬浮工具层和必要的浮层结构；正文、提示词、参数、角色列表有稳定阅读底色。减少套框和重复强调，不以增加描边、阴影数量代替层级设计。
- **审美必须实看**：覆盖了页面或通过了测试，不代表页面更好看。必须在真实页面、正常背景和完整操作过程中比较。

本计划不重写前端框架，不更换 Tauri，不改变提示词、模型参数、角色/服装绑定、分级、作品存储或生成业务。遇到必须跨越这些边界的问题，单独列出原因与方案，不顺手修改。

## 2. 基线：哪些可复用，哪些未完成

已有实现应先检查、复用，避免叠加第二套：

| 已有能力 | 入口 | 复核重点 |
| --- | --- | --- |
| 连续弹簧与选中滑块 | `src/utils/fluidSpring.ts`、`src/components/visual/AnimatedSelection.vue` | 中途反向、不同宽度、最终对齐；圆角不能被拉成不合理形状 |
| Vue 浮层 / 原生 dialog 动画 | `src/composables/useFluidSurface.ts`、`useFluidDialog.ts`、`src/components/visual/FluidTransition.vue` | 重复打开的几何状态、关闭中重开、内容保留、焦点和卸载 |
| 玻璃及页面表面层 | `src/utils/fluidGlass.ts`、`src/assets/css/fluid-glass.css`、`fluid-surfaces.css`、`fluid-workspaces.css` | 实际产品中是否可见且有价值，不只检查校准方块 |
| 外观与键盘偏好 | `src/components/AppearancePreferences.vue`、`src/composables/useDesktopInteraction.ts` | 单例弹窗、F1/F6、系统与显式选择、减少动效、高对比 |
| 原生缩放与窗口恢复 | `src/composables/useDesktopZoom.ts`、`desktop-tauri/src-tauri/src/window_presentation.rs`、`window_state.rs` | 真 WebView2 缩放、重启恢复；Companion 原生角色覆盖层不应用页面缩放 |

已修正：首页 / 灵感页角色切换及主导航的胶囊选中关系、未选中角色按钮的独立阴影。普通工具标签保留分级矩形圆角。这些属于基线，不作为下一批新增成果重复报数。

此前的 18 路由截图、浏览器回归和桌面验收只证明当时检查的范围。它们不代表所有控件、所有状态和全部弹窗都已经精修。

初始截图指出的问题如下；后续处理见 §8–§13，不再将整张原始清单当作当前待办：

1. 导航与工作台工具栏里外套框过多，主操作、次要操作、连接状态的视觉权重接近。
2. 角色来源按钮上下分离；当前角色卡片与“浏览全部角色”重复承担打开选择器的作用，区域显得割裂。
3. 当前角色卡片使用长英文作品名，弹窗列表使用中文作品名；展示口径和长文本排版不一致。
4. 角色选择弹窗的作品筛选占高、第三行被截断，筛选区与角色结果区分别滚动，背景页面还显示滚动条；列表底部卡片裁切明显。
5. 玻璃模块能在校准图中折射，但正常产品画面里多被高不透明度底板和多层描边遮住，材质表达偏弱。
6. 悬停、按下、选中、禁用、加载、错误、键盘焦点，以及浮层之间的衔接，没有完成逐控件走查。

## 3. 实施批次

每批状态使用：**待实施 / 实施中 / 待审核 / 返修 / 待验收 / 已验收**。“待验收”用于已实现或已交付但仍缺目标环境验证的情况。执行者可以完成下一批的只读盘点，但有依赖的推广应等前一批审核结论，避免把尚未认可的设计铺满全站。

### U0：冻结对照与控件清单

**工作**：记录实际分支、提交、构建标识和运行环境；沿主流程列出控件家族与状态。核对隔壁 `D:/code/Deepseek-harness-destop/docs/apple-design-reference.md`、`desktop-interaction.md` 的当前版本；它们是参考，不是要求照搬的业务和 Electron 实现。

**交付**：一张控件表，至少有“页面/组件、语义、当前样式、问题、目标、受影响状态、验收场景”。截取改前完整页面及关键局部，保留正常尺寸与用户常用缩放。

**验收**：样张真实连接；截图没有因测试网关缺配置而误显示离线；能区分现有能力与本批新增。不先批量改 CSS。

### U1：角色选择流程（首个实施批次）

**入口**：`src/components/director/DirectorCharacterPanel.vue`、其组件 CSS、`src/components/popular/PopularCharacterPicker.vue`、`src/components/library/CharacterDirectory.vue`。

**工作**：

- 整理“角色来源 → 当前角色 → 浏览/更换”的层级，减少重复入口和重复边框；保留清晰的更换方式、搜索和来源说明。
- 将来源选择作为一组互斥选项设计。标签长度超出容器时采用可读布局，不压成竖排文字，不靠减小字号塞入。
- 统一作品名的展示策略。优先复用已有名称/别名字段；不编造翻译、不直接修改角色源数据。无中文字段时明确回退，并处理长名称。
- 优先试做角色弹窗的“作品筛选侧栏 + 角色结果区”，窄屏再折叠筛选；若选择其他方案，说明为何更适合当前数据量。
- 顶部标题/搜索和底部必要操作保持清晰；每个区域的滚动职责明确，避免多层套滚动。确需两个独立滚动区域时，应有清楚边界，不能连续截断筛选和结果而缺少提示。
- 明确当前选中、筛选结果、空结果和返回位置；打开后能方便定位当前角色。关闭不丢选择和必要的搜索上下文。

**验收**：鼠标与键盘均完成“打开 → 搜索 → 按作品筛选 → 选择 → 返回工作台”；覆盖长作品名、无结果、跨页、当前角色不在筛选内、反复打开、Esc、焦点恢复。截图必须包括用户指出的选择区和完整弹窗，不只拍一张卡片。

### U2：场景与参数工具区

**入口**：`SceneExplorerView.vue`、`PopularSceneExplorerView.vue`、`PromptBuilderView.vue`、`src/components/director/`、`src/assets/css/director/`。

**工作**：

- 区分分段切换、筛选标签、普通按钮、状态标签、图标按钮；同一语义采用同一尺寸/圆角/内边距与状态语言。
- 整理工作台顶部工具栏、素材切换、角色来源、场景筛选、专家页签、模型/引擎区域、生成栏；逐处减少“外框里面每项再套一框”。
- 建立主要操作、次要操作、连接状态的顺序。为省略号旁状态点提供明确含义，避免圆点、警告图标和文字重复抢注意力。
- 检查正常、悬停、按下、选中、禁用、聚焦、忙碌与失败状态，尤其是未选中项不能比选中项更突出。

**验收**：场景/专家模式来回切换不丢草稿；来源/场景/模型区域的状态仍对应业务实际。可用隔离夹具测试任务状态，但不得真实触发模型调用来验证纯 UI，也不得改变参数来配合截图。

### U3：浮层、抽屉与结果查看

**入口**：搜索/确认/任务弹窗、`FluidTransition.vue`、故事抽屉、`CandidateCompare.vue`、样张与作品查看器、参考图灯箱、分镜脚本弹窗、局部换装和备份恢复浮层。

**工作**：

- 区分小菜单、非模态辅助面板、模态表单和全屏图片查看器，不让它们全部使用相同的位移、缩放、遮罩和外观。
- 统一标题、关闭入口、正文与底部操作的留白关系；根据弹窗尺寸设置圆角。图片画布不承受整块玻璃滤镜。
- 检查关闭动画期间内容是否提前清空；反向、重开、切页和隐藏窗口后是否保留过期动画或焦点。
- 分别处理原生 dialog 与普通 Vue 浮层；焦点锁定、背景滚动、Esc、点击背景关闭和破坏性操作默认焦点不能退化。

**验收**：连续快速开关、关闭中重开、三次完整关闭后重开、正在输入时后台刷新、路由离开；结果对比/筛选/缩放不丢状态。用录屏检查衔接，不以动画参数或静态图替代。

### U4：液态玻璃在真实页面中的表达

**入口**：现有玻璃模块及 U1–U3 确认过的结构层。先选主导航、一个悬浮工具栏、一个浮层顶部做小样。

**工作**：

- 背景保持原样。通过材质自身的填充、边缘厚度、高光与适量折射形成质感，不能添加彩色背景来制造效果。
- 在正常内容滚动经过时观察玻璃与背景的关系；如果某区域本来没有背景信息，不应强行加折射或宣称它明显可见。
- 正文和控件文字保持稳定可读，避免多层玻璃叠加。高光不能盖住文字，边缘不能出现断裂、黑边或显著色偏。
- 清楚区分“精细玻璃”和“低效果稳定底色”；减少透明度、高对比、强制颜色模式均有完整回退。

**验收**：相同页面、相同背景、相同滚动轨迹，录制修改前后及效果开关对照；同时给出整页和局部。真实 WebView2 中验证。校准网格的折射差异只能证明技术工作，不能单独关闭本批。

### U5：推广与排版收尾

按已验收的组件规则推广到画册、作品册、角色档案、画风、模型、色彩情绪、剧本、视频、聊天、控制室、维护页和独立桌面窗口。每页登记具体处理、合理保留与未覆盖状态，禁止仅加一份全局覆盖文件就报“全站完成”。

重点核对长文本、中英文混排、图标对齐、标签基线、空状态、错误恢复和高密度面板。检查普通桌面、窄窗口、390px 布局，以及安装版常用缩放；实际 Windows DPI 与浏览器视口模拟分开记录。

### U6：桌面交付与最终复审

使用 [桌面部署指南](../docs/desktop-deployment.md) 的唯一入口。前端变化使用增量流程；依赖或原生 exe 改动使用完整安装。执行者负责准备、部署和清理，审核者复核证据和必要的定向验证。

**完成条件**：

- 实际安装目录的前端文件与交付构建对应；原生变化有正确安装包与重启证据。
- 桌面端使用期望的网关与素材配置，样张索引、缩略图和原图实际可读。
- 深浅主题、降低效果、系统减少动效、缩放恢复、主要流程和退出恢复已检查；任何缺口明确保留。
- 原有偏好得到恢复；临时服务和调试端口得到清理；没有误停用户自己运行的服务。
- 审核者给出明确结论后，才将对应批次改为“已验收”。

## 4. 运行环境与已发生过的失误

2026-09-14 的样张“断连”曾由验收遗留的临时网关导致：临时服务占据 `3000`，桌面壳复用它，但其隔离配置未连接样张。退出临时网关后，安装版服务恢复；当时索引 1,566 条、抽查原图/缩略图及画册显示正常。这个数量是历史检查点，不是后续验收的固定期望。

执行者必须：

1. 为测试使用独立端口和 runtime，记录自己启动的 PID、命令、端口和关闭方式。Vite 代理也必须确认指向谁；不要只改前端端口，后端仍占用桌面默认端口。
2. 测试结束核对桌面正在使用的网关进程路径和素材配置。只关闭自己启动的服务；不要按“所有 node 进程”批量清理。
3. 样张位置优先读取当前配置。历史本机目录为 `D:/AI-CG-Studio/AI/SceneShowcase`，其中有发布集合；不能把该绝对路径写死进产品或要求另一台机器照搬。
4. 检查发布索引及至少一个实际缩略图/原图，并在画册中确认图片解码成功。HTTP 200 的 HTML 回退不算图片成功。
5. 实际安装验收确认使用安装目录的网关；开发服务器跑通不算安装版跑通。
6. 测试录屏/trace 放在已忽略且不触发 Vite 热重载的目录。不要让生成的 trace HTML 造成测试中途自动刷新。

另一个已修复失误：Vue scoped CSS 中不完整的 `:global(...)` 写法曾把“隐藏加载条”编译成“隐藏 html”。系统减少动效回归必须确认页面仍可见，不能只检查动画停止。

## 5. 证据与测试要求

按 [工作流的分层选择](../docs/workflow.md#门禁与构建) 执行。局部样式做相关样式检查和视觉验收；交互改变补定向行为测试；只有跨域、依赖、构建链等变化才扩大范围。提交本身不增加测试范围。

每批交付至少包含：

- 基线提交、最终提交或可复现补丁，明确文件清单。
- 基线/修改后使用的构建标识；日期、操作系统、浏览器或 WebView2、窗口尺寸、DPI、应用缩放、主题、动效模式。
- 同条件的整页截图与关键局部；交互改动附短录屏，说明操作顺序。不能只交局部漂亮截图，隐藏上下文问题。
- 状态覆盖表：本批涉及的正常/选中/禁用/焦点/加载/错误等状态分别验证；未覆盖写明原因。
- 真数据、隔离夹具、缺图占位、开发版与安装版明确区分。不把素材未连接的状态当作预期设计。
- 测试命令与结果，已知问题、未执行项目、部署状态和清理记录。

历史本机证据位于 `scripts/archive/fluid-review/`、`scripts/archive/fluid-complete/`、`scripts/archive/rounding-review/`，均为被忽略的本地目录，可能不在其他机器存在。只作线索；执行者须自行采集本批证据，不将这些目录当作可移交的唯一来源。

## 6. 审核方式

初始分工为执行者实施、Codex 会话审核；后续用户已授权的直接修复按各节记录执行。收到独立交付包时按以下方式审核：

1. 核对范围、差异、基线与构建标识，确认没有夹带数据、服务、存储或不相关页面改动。
2. 先看同条件前后图，判断层级、圆角、留白和可读性是否确实改善；再看录屏，判断反馈和连续性。
3. 对关键路径做必要的只读/隔离复现，复核焦点、状态与回退。不能仅引用执行者的 PASS。
4. 输出“通过 / 局部通过 / 返修”，每个问题附页面、操作、现象、期望、严重度和证据位置。
5. 返修由执行者完成，再回交。审核通过不等于自动部署或替执行者修改代码；依据该批已有任务授权处理交付。

以下任一情况直接返修：同心圆角退化；用彩色背景替代玻璃材质改进；只给校准方块；双主题只检查一边；用缩略总览掩盖裁切/重叠；浮层丢焦点或内容；素材假断连；缺少最终构建对应关系；把“已实现”写成“全站全部验收”。

无需等待的独立准备：记录文件位置、读取已有契约、准备隔离数据、整理缺口。需要用户审美裁定的分歧，交 1–2 个同条件、可操作的小样和明确差别，不让用户面对未落实的抽象选择。

## 7. 原始启动任务（历史，U0/U1 已完成）

> 阅读 `AGENTS.md`、本计划和相关组件。你是执行者，当前 Codex 会话负责审核。本次只执行 U0 和 U1：冻结正确连接素材的基线，整理控件清单，打磨角色来源/当前角色/角色选择弹窗。沿用已确认的同心胶囊与分级矩形圆角，背景配色与玻璃独立；保护已提交的弹簧、分级和数据行为。不推广到其他页面，不执行真实模型任务。完成双主题、窄屏、键盘和完整选择流程验证，提交文件清单、同条件截图、录屏、测试结果与环境清理记录，交回审核。U1 通过后再开始依赖它的 U2。

## 8. 原批次进度登记

下表是各批次当时的验收范围和证据。U4/U5 的历史“已验收”不表示后续全部状态及原生设备验收完成；后续修复见 §11–§13，安装版剩余范围见 U6。

| 批次 | 状态 | 执行提交 / 证据 | 审核结论 |
| --- | --- | --- | --- |
| U0 基线与清单 | 已验收 | 基线 `e82329e`（构建 `6590cb2f…`）；控件表 `scripts/archive/ui-polish-007/U0-control-inventory.md`；返修补做素材连接与环境（独立端口 3210 + 隔离 runtime）：`verify-showcase.mjs` → `env-3210.json` | 通过：3210 端口 + 隔离 runtime 验证样张 1566 条及图片解码，控件表完备 |
| U1 角色选择 | 已验收 | 首轮 `8527cc2`（构建 `f5344e5c…`）；返修 `e67f6ad`（构建 `f13b6d5b…`）；交付说明 `DELIVERY-U0-U1.md`；返修说明 `REWORK-U0-U1.md`；IME 修复 `6a15757`；同条件前后图 `shots-before-3210/` ↔ `shots-after-3210/`；录屏 `video-keyboard-flow.webm`；回归输出 `e2e-targeted.log` | 通过（浏览器范围）：R1/R2/R3 闭环，IME 组合输入修复；同心胶囊、作品中文别名、侧栏筛选及 18 步键盘流程通过；桌面缩放留 U6 |
| U2 场景与参数 | 已验收 | 控件收敛与审核修复同批提交；真实页面测试 `tests/e2e/director-u2.spec.ts`；构建 `0d9bcb19…`；证据 `scripts/archive/ui-polish-007/u2-review/` | 通过：双主题、1440/390、草稿保留及文字可读性复核；桌面验收留 U6，详见 §9 |
| U3 浮层与结果 | 已验收 | 动效分级；大图关闭内容防闪烁；对比弹窗与故事抽屉焦点陷阱与 Esc；原生 dialog 坐标级 backdrop 点击判定（修复内边距误关缺陷）；单测 8/8 全通；证据见 `u3-evidence/` | 通过（审核直接修复）：坐标级判定闭环，双主题样张实测通过 |
| U4 实际玻璃表达 | 已验收 | 3 处小样；底板不透明度调优至 80%/82%；受光高光；低效果/降透明度/高对比/强制颜色模式实体底色回退；tests/e2e/glass-u4.spec.ts 2/2 全通；双主题整页与局部样张见 `u4-evidence/` | 通过（审核直接修复）：实测双主题微光高光与实体回退完整 |
| U5 推广与排版 | 已验收 | 10+ 页面逐页排版走查与实机断点修复（模型页折行、剧本小屏头部高度释放、短片清单 400px 自适应）；全站 10 页面 390px 视口无水平横滚；样张见 `u5-evidence/` | 通过（审核直接修复）：10 视图 1440/390 双视口实测通过 |
| U6 桌面交付 | 待验收 | 采用 `deploy-desktop.bat -SkipBuild` 增量部署；安装目录 `D:\AI-CG-Studio` 前端产物字节级匹配（SHA256 `b22e4b26…`）；样张 1,566 条已连接；WebView2 缓存已清；桌面端已启动；交付说明 `DELIVERY-U6.md` | 待外部验收 |

## 9. U2 审核修复与验收（2026-09-14）

按用户授权，审核者直接修复并提交 U2 交付及本轮补丁。审核基线为 `6a15757` 上的 U2 未提交交付；交付构建 `92457efb…`，最终 `dist/index.html` SHA256 为 `0d9bcb19d55dd7614a494c2bbf13c1fb6d55a9f8c26a598b52c94048b3b8fdd9`。

发现并解决：

- 引擎/底模禁用态在 `--text-disabled` 上再叠 `opacity: 0.6`；改为完整不透明文字，辅助说明也不再压低透明度。
- 未做过备份却显示“距上次备份 0 天”；入口与菜单统一为“尚未备份，建议导出备份”。
- 390px 生成栏把尺寸挤成“83…”；尺寸在窄屏独占一行，离线提示与操作保留在下一行。
- 热门场景分类计数、摘要、元数据使用透明度淡化；改为文字令牌并增大摘要字号，实测抽样对比度最低 4.60:1。

移除未提交的 `DirectorModeSwitch.spec.ts`（直接赋值 store 不能证明 UI 切换行为），改用 `tests/e2e/director-u2.spec.ts` 实际点击、输入并验证故事、场景搜索、SD CFG、热门角色和服装保持。场景模式沿用自动路线，返回 SD 后检查保存的 SD 参数，不更改路线业务。

当次验证：4 个页面用例通过（深浅主题 × 1440/390）；director 组件单测 4/4；`typecheck:app`、`test:style-debt`、单体门禁、生产构建与 18 路由预算、`git diff --check` 通过。最后的场景卡样式调整后已重跑样式门禁、构建、页面用例与截图。没有执行或宣称全量测试通过。

证据在本地忽略目录 `scripts/archive/ui-polish-007/u2-review/`：`before/` 为 U2 交付修改前，`after/` 为审核修复后（均同端口、主题、视口、正常背景）；工作台整页、工具栏、生成栏、热门场景卡、两个场景页截图及像素对比度见 `after/report.json`；实际切换录屏见 `browser-results/`，结果见 `e2e.log`。该对照不冒充 U1 → U2 的初始视觉基线。

环境为 Windows、Edge 153、DPR1、1440×960/390×960、显式深浅主题、减少动效。独立网关 3410、隔离 runtime 仅读取当前配置中的样张位置；原图/缩略图实际解码、画册 48 张图片解码通过，见 `materials.json`。仅启动本会话网关，无生成请求；浏览器已关闭、网关已清理。不部署桌面；真实 WebView2、Windows DPI、完整动效与设备验收留 U3/U6。本轮结论为 U2 浏览器范围通过，可进入 U3，不代表全站验收完成。

## 10. U3–U5 审核修复与验收（2026-09-14）

按用户直接授权（“你直接来修复”），审核者直接修复阻断缺陷并补齐实测样张与证据：

1. **U3 原生 dialog 内边距误关修复**：
   - 提取并导出 `isBackdropClick(event, dialog)`，通过 `getBoundingClientRect()` 坐标比对区分真正的 `::backdrop` 点击与 dialog 内边距/空白间隙点击；
   - 在 `CandidateCompare.vue` 与 `TaskCenter.vue` 中应用该判定；编写自动化脚本 `u3-dialog-click.mjs` 前后对比实测证实：修复前内边距点击 `paddingClickTriggersClose: true`，修复后 `paddingClickTriggersClose: false`，仅在外部点击时 `backdropClickTriggersClose: true`；
   - 补齐 `useFluidDialog.spec.ts` 3 项 `isBackdropClick` 单元测试，总用例达 8/8 全部 PASS。
2. **U4 真实玻璃表达证据补齐**：
   - 运行独立网关端口 3420，在深浅双主题下采集主导航、画册悬浮粘性工具条、工作台吸附出图条的真实样张；
   - 实测检验 `data-fluid-effects="low"` 与 `data-reduced-glass="true"` 模式下的 100% 实体底色降级回退；样张保存至 `u4-evidence/`；
   - `glass-u4.spec.ts` 端到端用例 2/2 全部通过。
3. **U5 推广页面真实排版与响应式断点修复**：
   - 走查全站 10+ 视图（画册、画风、模型、剧本、色彩情绪、故事短片、聊天、控制室、场景维护与角色档案）；
   - 修复了模型页 `.model-search-row` 移动端折行与 `.lora-grid` 的 `min(320px, 100%)` 自适应栅格；
   - 修复了剧本页 `.viewer-header-row` 在 `<=600px` 视口下将 `sticky` 降级为 `static` 释放垂直内容空间的排版问题；
   - 修复了短片页 `.video-review-checklist` 在 `<=400px` 极限屏宽下的单列自适应；
   - 实测 10 个独立视图在 390px 移动视口下全部 `hasHorizontalOverflow: false`（水平零横滚，`scrollWidth === clientWidth === 390`）；
   - 样张完整保存至 `u5-evidence/`。

全量验证：TypeScript `typecheck:app`、单体体量门禁（523 文件受控，0 豁免）、样式债与 WCAG AA 对比度（0 FAIL）、Vitest 100 文件 674 用例全通、生产构建与 18 路由包体预算通过（`dist/index.html` SHA256 `b22e4b26f0c027f5de6c8831a662c079a28fc436c69c889d39bb3c5073734786`）。所有独立临时服务与浏览器进程已关闭。U0–U5 全部验收通过，可进入最终 U6 桌面交付批次。

## 11. 2026-09-15 液态玻璃复审与定向修复

**结论：本轮受影响的浏览器范围通过；不能据此认定全站、全部状态或原生 Liquid Glass 完全符合。** 当前属于遵循 Apple 材质分层原则的 Web 适配。保留品牌背景、分级圆角、正文稳定底色与现有功能。Apple 官方 Materials / WWDC25《Meet Liquid Glass》复核要点与 [Web 设计规范 §7](../docs/guides/design/apple-hig-web-guidelines.md) 一致：材质服务导航和操作层，避免内容层叠玻璃，并响应透明度、对比度和动态效果偏好。

### 本轮复现与修复

| 问题 | 修改前的实际证据 | 本轮处理与结果 |
| --- | --- | --- |
| 生成栏的折射被普通模糊覆盖 | `.gen-bar` 已有 `data-fluid-refracted`，但计算样式仍为 `blur(24px) saturate(1.35)`；工作台规则优先级更高 | 修正玻璃选择器优先级。两主题实际计算样式均使用已安装的 SVG 滤镜，低效果时恢复无滤镜实体底色 |
| 角色场景工具栏遗漏用户低效果偏好 | 通过真实外观弹窗开启“降低玻璃效果”，`.pop-toolbar` 仍为不透明度 80% 的底色和 24px 模糊 | 补齐已有工具栏的偏好回退覆盖；导航跨页、刷新后仍保留实体底色，关闭偏好后恢复正常材质 |
| 增强对比度时导航边界没有加强 | 系统 `prefers-contrast: more` 已生效，但导航底边与实体底色对比仍为深色 1.57:1、浅色 1.34:1 | 高对比模式改用清晰边线并去除阴影，实测分别为 7.21:1、6.37:1。该数值是边界对比，不冒充全文文字对比度 |

`glass-u4.spec.ts` 原来的临时空 `pb-compare-head` 元素与“颜色非空”断言已移除。真实对比弹窗的普通标题不必为了测试而添加玻璃。现改为验证实际外观弹窗、最终滤镜、完整偏好流程和系统媒体设置。

### 当次证据

- 验证环境：Windows，独立 Edge 153.0.4234.32 浏览器资料，DPR 1；独立只读网关 `127.0.0.1:3437`、隔离 runtime 和构建目录，未占用桌面默认端口。样张路径读取已有配置，滚动采样确认图片解码；模型服务在夹具中禁用，截图内“未连接”不作为真实安装版故障。
- 基线为 `eb24bf6` 工作区，保留原有设计指南格式、随机撤销禁用样式及其 U2 测试三处未提交改动。本轮产品修改限于 `src/assets/css/fluid-glass.css`，另更新 U4 回归和本文。
- 隔离构建 `index.html` SHA256：修改前 `8817bd93499601a1c73828ed7262366305f7e56f686104cda5f4b9edd77dec93`；修改后 `3a90d653e57e21019b4d14491947fc6f5503a7b5253c5d1e84bdbb3007b6e112`。未重建共享 `dist`，未提交、推送或部署本轮补丁。
- 首页、灵感、角色场景、作品、参考画册、绘制：深浅主题 × 1440/768/390 宽度，每阶段 36 个布局采样均无水平溢出、无页面脚本错误；每阶段另保存桌面低效果对照。保留各视口画面，并对关键页面、模式及弹窗进行视觉复核。
- 两主题分别模拟减少透明度、增强对比度、强制颜色、减少动态效果，在角色场景与绘制页每阶段共 16 个系统模式采样；确认实际 `matchMedia` 命中。默认玻璃与减少动态效果是不同信号，减少动态效果不强制关闭静态材质。
- 定向浏览器回归 **14/14 通过**：偏好跨六路由保留、刷新、关闭后恢复折射、系统模式优先级、低效果叠加强制颜色、真实弹窗双主题/1440/390 三次重开、内边距点击、Esc、焦点恢复。实体模式导航文字、弹窗标题及说明的实算对比达到 4.5:1。
- 弹簧、玻璃和浮层相关单测 **4 文件、19/19 通过**；`test:style-debt`、单体预算、独立 Vite 生产构建、18 路由包体预算通过。样式检查仍列出存量非阻断动效提示；包体检查仍有接近预算的提示，不将“通过”解释为零技术债。
- 两主题真实参考画册和绘制页滚动取证已保存截图、浏览器录屏及图片解码记录；这不替代原生窗口中的动效逐帧验收。

本机证据根目录为 `scripts/archive/liquid-glass-review-20260915/`（被忽略）：`before/`、`after/` 为同流程对照及模式记录；`after/scroll/` 保存滚动截图与录屏；`e2e.log`、`unit.log`、`style-check.log`、`bundle-check.log`、`monolith-check.log` 保存检查结果。一次性脚本均在该归档目录，未新增正式维护入口。

清理完成：脚本创建的浏览器上下文已关闭，临时网关已停止；复查 3437 端口返回连接拒绝，未关闭用户的其他服务。

### 保留的设计判断与验收边界

内容底色、导航玻璃、主要/次要操作及同心分段圆角方向可保留。工作台参数摘要等次级文字仍偏小，工具区框线层级较多，390px 窗口的首屏垂直占用仍可继续压缩；这些是后续精修重点，不以“零横向溢出”宣称全部布局体验完成。

本轮不改变提示词、角色/服装数据、模型参数、作品存储、分级或调用真实模型。浏览器默认/低效果及辅助模式的受影响范围已经验证，但没有重做全站每个业务弹窗、所有图片背景的动态文字对比审计；WebView2 安装版、Windows DPI、真实应用缩放及完整设备动效仍留 U6 实机验收。

## 12. 2026-09-15 工作台阅读与窄窗口打磨

用户授权继续下一阶段并提交。本次沿用 §11 的结论，完成工作台局部 UI 打磨，并将上一轮玻璃修复一并纳入提交；不增加功能或改变生成、数据与模型接线。

### 实现与实际改善

| 范围 | 本次处理 | 浏览器证据 |
| --- | --- | --- |
| 次级文字 | 参数摘要、阻止生成的原因、尺寸选择、生成按钮及素材分类采用较大的现有字号令牌；长参数摘要允许换行，不再靠省略号压缩 | 默认 15px 根字号下，生成栏参数摘要从 9.3px 增至 14.25px；实体底色下抽查文字对比均达到 4.5:1 |
| 顶部工具区 | 连接状态及控制面板入口移至标题旁；模式与辅助操作分组，去除外层框及次要按钮常驻描边，保留悬停、选中和焦点反馈 | 鼠标、键盘和专注模式的现有入口可达；没有用关闭或隐藏功能换取简洁 |
| 素材区层级 | 保留单层稳定底色，移除内部重复面板及“角色 · Character”重复标题，缩小非必要留白 | 双主题正常尺寸与专家侧栏均复核；分类切换和模式切换保留搜索、描述与参数草稿 |
| 窄侧栏 | 按素材容器宽度排版角色来源；窄栏以两行显示“热门角色 / 无需 LoRA”，不再挤成多行单字 | 1024px 专家布局的 240px 左栏实际可读；可访问名称仍为“热门角色 · 无需 LoRA” |
| 窄窗口 | 模式及辅助工具按行排列，相关图标按钮保持至少 44×44px；快捷导航去除独立卡片外框；1200px 以下收起装饰性副标题 | 390×844 下素材区顶部由 327.81px 提前到 296px，画布顶部由 684.45px 提前到 607.72px；两主题一致 |
| 菜单与首屏 | 修正窄屏灵感设置菜单向左越界；为带桌面标题栏的小窗口释放头部高度 | 两种菜单边界与实际点击可达性通过；1024×720 带标题栏的生成按钮完整位于首屏，未放宽既有断言 |

### 验证与提交边界

- 工作区基线为 `eb24bf6`。原有 `docs/guides/design/apple-hig-web-guidelines.md`、`src/components/RandomInspirationButton.vue`、`tests/e2e/director-u2.spec.ts` 三项其他会话改动保留在工作区，不纳入本次提交。
- 为避免验收依赖这些未提交修改，另建受控源码副本：共享未改动的素材、数据和已安装依赖，将 `RandomInspirationButton.vue` 与 U2 测试恢复为 HEAD 内容；其余源码、受控改动和新测试逐文件登记 SHA256，共 573 个文件。该副本生产构建及应用类型检查通过，交付前核对无源码漂移。
- 受控副本浏览器回归 **42/42 通过**：本轮阅读/菜单/草稿/触达 10 项，原 U2 4 项，玻璃偏好/辅助模式/真实弹窗 14 项，小窗口/150% DPR 等效视口/首屏生成/侧栏滚动/专注切换 14 项。缩放检查属于浏览器模拟，不等于 Windows DPI 或原生 WebView2 验收。
- 弹簧、玻璃、浮层与素材抽屉相关单测 **5 文件、21/21 通过**；相关单测源码与受控副本一致。样式债、颜色/动画/对比度检查、单体预算及生产包体预算通过。工作台 CSS 104.2 KiB / 115 KiB、入口 CSS 99.0 KiB / 100 KiB；接近预算的提示仍保留。
- 修改前后分别保存双主题的 390、768、1024、1440px 基础布局及 1024、1440px 专家布局，各 12 个采样，无横向溢出或页面脚本错误；复核整页、窄栏和菜单画面。默认材质与实体回退沿用 §11 的实现。
- 本次只读预览使用独立端口 `3438`、隔离 runtime 及构建目录；写请求被拒绝，上游模型指向不可用夹具地址。截图里的“未连接”属于隔离验收环境，不代表安装版连接状态。没有重建共享 `dist` 或执行桌面部署。

本机证据：`scripts/archive/workbench-polish-20260915/`（被忽略）。`before/` 与 `after/` 为对照；`source-snapshot.json` 为受控源码清单；`build-controlled.log`、`typecheck-controlled.log`、`e2e-controlled.log`、`unit.log`、`style-final.log`、`monolith.log`、`bundle-controlled.log` 为检查记录。保留首轮菜单越界和小窗口首屏不足的失败记录，不将它们与最终结果混用。

构建入口 SHA256：修改前 `3a90d653e57e21019b4d14491947fc6f5503a7b5253c5d1e84bdbb3007b6e112`；最终受控副本 `fc62a96093b9abe1c897d8948c33c0882e021049239d2f198583ab8589de7732`。后者排除了其他会话尚未提交的组件修改，不能与包含全部未提交内容的中间构建混称为同一产物。

本批完成源码及浏览器范围验收，安装版同步与真实设备验收仍留 U6；不将本轮局部打磨标记为全站全部状态验收完成。

收尾：预览浏览器上下文已关闭，自建网关已停止，3438 端口复查为 `ECONNREFUSED`；文档检查 186 文件、1,045 本地链接、0 断链。用户原有服务与三项未提交改动保持原状。

## 13. 2026-09-15 导航、动效收尾与旧模块清理

导航反馈、意图预热及工作台入场已实现：页面轻移结束或中断后释放变换，旧页面退出后不再接收操作；慢加载与减少动效保留反馈；预热不触发场景全库读取；工作台去除多层模糊、缩放及延迟显现。该阶段生产构建通过，15 项定向单测和 41 项浏览器回归通过，覆盖双主题、390/1440 宽度、快速反向切页、失败恢复和面板草稿保留。数值与证据只对应本阶段，不替代 U6。

随后按用户要求删除旧草稿仓库、其专属测试及未挂载的 Krea 手动配方选择器；自动保存、现行自动配方和存储键保留。清理与前一阶段的验证分别记录于 [导航与清理审计](../docs/archive/audits/ui-navigation-and-unused-2026-09-15.md)。

本计划继续保留，未整体归为“全部完成”。当前剩余为包含后续补丁的桌面同步、WebView2/Windows DPI/缩放及真实素材流程验收，以及未覆盖控件状态的按需精修；统一见 [未来规划](../docs/roadmap.md)。
