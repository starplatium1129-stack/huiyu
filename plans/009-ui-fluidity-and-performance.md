# 009 · 交互流畅度与运行性能分批实施计划

> 创建日期：2026-09-17（Asia/Singapore）。状态：**F0–F2 已完成办公机定向验收，F3 部分验收，F4 与 F6 已完成办公机验收，F5 办公机生命周期项完成，F7 与真实资源趋势待执行**。
>
> 编制基线：`main@6afdc7d0f7091547123d44e9a21c8d6d661d6166`。F0 已在办公机执行；Windows 安装版、真实 GPU/Live2D/语音负载仍未验收。已有历史 PASS 不作为本专项成绩。
>
> 最近核对：2026-09-19 于 `main@d738590` 复核。F4/F5 的后续局部实现与当前门禁见本页回填；没有因此关闭浏览器视觉、性能或 F7 真机验收。
>
> 用户目标：保留绘遇的视觉品牌与 TypeScript 主体，让应用点击及时、页面衔接连续、滚动稳定、返回保留现场、连续操作不被动画阻挡。参考 Apple 的交互原则，不复制其外观，也不以增加特效或改写成 C 作为完成标准。

## 1. 如何使用这份计划

总优先级仍由 [未来规划](../docs/roadmap.md) 维护；本文负责该性能专项的执行拆分、批次状态和验收证据。协作遵循 [AGENTS.md](../AGENTS.md)，设计遵循 [DESIGN.md](../DESIGN.md)，检查与部署分别遵循 [统一工作流](../docs/workflow.md) 和 [桌面部署](../docs/desktop-deployment.md)。

每次只领取一个批次或明确的子批次：读取当前实现 → 核对前置证据 → 最小改动 → 定向验证 → 提交受控文件 → 回填结果。**计划提交不等于授权立即执行全部批次、安装软件、调用真实模型或发布版本。**

开始任何批次前重新核对 `main`、工作区和相关文件；不能将本文基线当作未来源码。后续实现已满足某任务时，只补缺失证据，不重复重构。单批仍过大时按文中的子批次独立提交，不把一批变成全站重写。

### 执行状态

| 批次 | 交付结果 | 前置条件 | 当前状态 | 提交 / 证据 |
| --- | --- | --- | --- | --- |
| F0 | 可复现的流畅度基线、场景矩阵和测量入口 | 无 | 已验收（办公机；S06–S08 延期） | F0 原始报告（本机原始证据：`runtime/ui-fluidity-f0/ui-fluidity-f0.json`；[去敏摘要](../docs/evidence/ui-fluidity-f0-f3-2026-09-17.json)）、代表性 trace（本机原始证据：`runtime/ui-fluidity-f0/traces/representative-r0s0.zip`；[去敏摘要](../docs/evidence/ui-fluidity-f0-f3-2026-09-17.json)） |
| F1 | 即时反馈、最新导航意图和可中断切换 | F0 | 已验收（办公机；当前趋势非纯净 A/B） | 单测、导航回归与 F0 场景基准通过 |
| F2 | 热切换、图片就绪和返回现场恢复 | F1；使用 F0 的数据/资源分析 | 已验收（办公机；S03 留给 F3） | S02/S04、预热与图片回归通过 |
| F3 | 大列表、主线程和滚动掉帧定向优化 | F0；默认接续 F2 | 部分验收（F3.1 完成；S03 残差移交 F7） | [去敏摘要](../docs/evidence/ui-fluidity-f0-f3-2026-09-17.json)、F3 基线（本机原始证据：`runtime/ui-fluidity-f0/ui-fluidity-f0.json`；[去敏摘要](../docs/evidence/ui-fluidity-f0-f3-2026-09-17.json)）、分解探针（本机原始证据：`runtime/ui-fluidity-f0/ui-fluidity-f0-narrow-mechanism.json`；[去敏摘要](../docs/evidence/ui-fluidity-f0-f3-2026-09-17.json)） |
| F4 | 一致且不阻碍操作的页面/面板动效 | F1、F3 的关键阻塞已处理 | 已验收（办公机；来源几何与快速反转，真机观感留 F7） | `9d8d1fe`、[证据](../docs/evidence/009-f4-f6-office-2026-09-19.json) |
| F5 | 长时间使用、后台资源和 Live2D 共存 | F0；默认接续 F4 | 部分验收（办公机生命周期/低效果完成；真实资源趋势留 F7） | `05bfe9b`、`6eac3cf`、`9d8d1fe`；完整 GPU/纹理/功耗待验 |
| F6 | 稳定的行为回归、性能预算和交付记录 | F1–F5 已实现或有明确延期记录 | 已验收（办公机；性能噪声与跳过项如实记录） | [证据](../docs/evidence/009-f4-f6-office-2026-09-19.json) |
| F7 | 目标 Windows 安装版与真实负载验收 | F6；目标设备和部署条件就绪 | 待执行 | 未运行 |

默认顺序：**F0 → F1 → F2 → F3 → F4 → F5 → F6 → F7**。F0 证明某个卡顿热点应优先修复时，可以提前执行 F3 的该子项，但需记录依据，且仍先补基线。不得并行修改同一导航、缓存或共享动效模块。

阶段性交付：F0–F2 验证“点下就有回应，回来不重来”；F3–F4 验证“滚动和运动连续”；F5–F7 验证“长时间使用与真实桌面负载下仍可控”。每一阶段均可独立停下，不要求一次做完。

## 2. 范围与不可牺牲的边界

### 2.1 本专项要改善的体验

- 点击立即确认；请求处理中、任务成功和失败是不同状态，不用乐观动画伪造完成。
- 普通切页时应用框架稳定，旧内容与新内容交接清楚；不先清空页面再无期限等待。
- 新内容可用后能够立即操作，不等待装饰性动画播完；连续导航、取消和返回响应最新意图。
- 场景库、作品册和样张的滚动/筛选稳定；回到列表能恢复合理的位置、筛选和选择。
- 开启桌宠、语音或运行生成任务时，界面仍有可预测的反馈；资源不足时有明确、可恢复的降级策略。

### 2.2 本专项不做什么

不重做 JavaScript → TypeScript 迁移；不全量改成 C/Rust；不更换 Vue/Tauri；不重新设计全站视觉；不新建一套通用动画引擎；不因为“苹果风格”增加全屏模糊、层层缩放、长入场或强制滚动接管。

不修改模型、生成参数、定稿场景、提示词、内容评级或作品存储格式。音频 C/WASM 内核、模型推理加速和数据库迁移只有在独立测量证明必要后另立范围，不夹带进界面专项。

### 2.3 必须保留

保留深浅主题、键盘导航、焦点恢复、减少动态效果与现有低效果偏好。高频动画遵守仓库 `transform` / `opacity` 红线；例外按 AGENTS 留理由和评审证据。保留手绘线条图标、品牌色与内容可读性。

保留 CSP、麦克风权限、任务中断确认、本机/远程内容分级和 fail-closed 行为。过渡快照、缓存和提前解码也不得绕过分级遮罩或加载未授权内容。禁止为无缝切页全局开放 `unsafe-eval`、移除权限策略或无条件取消文档重载。

导航“最新意图优先”只适用于导航与可取消的读取。保存、生成、删除等写操作继续遵循各自事务、幂等和取消契约，不能因为切页就静默丢弃已发出的写任务。

## 3. 已核对的基础：保留，不重复造轮子

以下是编制基线的代码事实，不代表已经测得性能问题。

| 位置 | 已存在的实现 | 本专项的正确切入点 |
| --- | --- | --- |
| [AppLayout.vue](../src/components/AppLayout.vue) | 导航在内容路由外；对 Gallery、Showcase、PromptBuilder、VideoStudio 使用 KeepAlive；组件键为 `route.path` | 核对缓存命中、激活副作用和内存，而不是一律给所有页面加缓存 |
| [useRouteTransition.ts](../src/composables/useRouteTransition.ts) | WAAPI 入场位移 6px、220ms；离场直接完成；有取消收尾和减少动态效果处理 | 先测点击至入场之前的等待，再完善连续交接；不能把 220ms 当作整页加载时间 |
| [useNavigationFeedback.ts](../src/composables/useNavigationFeedback.ts) | 有导航目标、180ms 延迟 loading 指示及旧导航保护 | 保留防闪烁策略，检查即时按压/目标反馈是否独立；不盲目把所有延迟改为 0 |
| [路由预热](../src/router/prefetch.ts)、[交互入口](../src/components/AppInteractionLayer.vue) | 已有按用户意图预热、共享模块加载及失败重试路径 | 验证代码、业务数据、首屏图片三种就绪状态；模块预热不等于页面已可用 |
| [router/index.ts](../src/router/index.ts)、[documentPolicy.ts](../src/router/documentPolicy.ts) | 有 savedPosition/锚点滚动；standard、voice、live2d-voice 策略不同，跨策略走文档重载 | 分开测普通 SPA 与跨策略导航，保留安全边界，不能将两者混为同一动画问题 |
| [现有性能配置](../playwright.performance.config.ts)、[导演台基准](../tests/e2e/office-performance.bench.ts) | 单 worker；3 组新 context；每组两次 `page.goto('/prompt-builder')`，测可见入口与首次操作 | 当前 warm 是同 context 的再次文档导航，不等于 SPA 切换或 KeepAlive 激活；补独立场景，不改名冒充已有覆盖 |
| [Live2D 纹理](../services/live2d-textures.ts)、[原生构建](../desktop-tauri/native-live2d/build.rs) | 已有 Sharp 处理、纹理缓存与串行队列；已有 Rust/C++ 原生集成 | 测共用 CPU/GPU 和生命周期；不要把现有原生依赖重写一遍 |

[007 UI 计划](007-ui-detail-polish-and-review.md)、[008 TypeScript 计划](008-typescript-migration-repair.md) 和 [导航收尾记录](../docs/archive/audits/ui-navigation-and-unused-2026-09-15.md) 仅提供已做范围与回归线索。本文不重新开启它们已经完成的开发批次，也不关闭其中尚未完成的设备验收。

## 4. 测量口径与拟定目标

### 4.1 先区分四种“快”

| 指标 | 起点与终点 | 不允许的替代判断 |
| --- | --- | --- |
| 输入反馈延迟 | 输入事件时间戳 → 按压、待导航或忙碌状态首次可见 | 函数执行完、Vue nextTick 完成不等于已经显示 |
| 页面主要内容就绪 | 本次导航意图 → 主要内容和首个必要操作可用 | route afterEach、占位出现、动画结束不等于内容就绪 |
| 运动稳定性 | 指定过渡/连续滚动时间窗内的帧节奏与渲染开销 | 平均 FPS、rAF 次数、无长任务不能单独证明真实呈现平滑 |
| 稳态资源成本 | 预热后同一使用序列的 CPU、堆、进程内存和 GPU 记录 | JS heap 不是整个 WebView/原生 overlay/GPU 的内存 |

新增测量可使用导航唯一 ID 和 `performance.mark/measure`，建议事件为 `intent`、`feedback-committed`、`shell-ready`、`primary-ready`、`settled`、`cancelled`。它们是**拟新增的契约**，不是现有接口。取消样本单列，不混入成功导航耗时。

DOM 提交、rAF 或双 rAF 只能作为近似信号；首次可见反馈与掉帧判断应结合浏览器 trace 的呈现信息和真机录像。支持 Event Timing/Long Tasks 的环境可补采，缺失时标为 unavailable，不填 0。脚本化单次交互延迟不冒称完整页面生命周期的 INP。

### 4.2 F0 需要冻结的目标与预算

以下是本项目的**初始工程目标，不是 Apple 标准或当前实测成绩**。F0 固定机器、刷新率、数据集和波动范围后登记版本；若目标不可行，保留差距及理由，不悄悄放宽阈值。

| 项目 | 初始目标 / 验收规则 |
| --- | --- |
| 常见本地操作反馈 | 目标 p95 ≤ 100ms；实现上尽可能下一可呈现帧响应，不人为等计时器 |
| 普通热切换主要内容就绪 | 在指定模块、数据与必要缩略图已缓存的夹具上，目标 p95 ≤ 300ms；不等待装饰性动画收尾 |
| 冷进入与跨策略重载 | F0 分别记录分阶段耗时并设路由预算，不套用 300ms 热切目标；之后必须无已确认退化 |
| 60Hz / 120Hz 帧预算 | 显示间隔分别约 16.7ms / 8.3ms；这不是全部可用于应用 JS 的时间。固定前台测试窗内以帧时间分布和渲染 trace 验收 |
| 掉帧候选阈值 | F0 可用静止前台的有效间隔 T 校准：rAF 间隔 > 1.5T 的比例目标 ≤ 1%；必须与真实呈现证据配对，单独达标不关闭掉帧问题 |
| 主线程长任务 | 指定纯过渡/稳定滚动窗口内，目标不产生可归因于应用处理的 >50ms 阻塞；50ms 以下仍可能丢帧，不能只查这一项 |
| 返回现场 | 同一数据版本的夹具中恢复筛选/选中项；布局稳定后同一滚动锚点误差目标 ≤ 2 CSS px；数据变化时按稳定 ID 回落，不强行旧像素位置 |
| 资源生命周期 | 预热后完成 20 轮往返及三组稳态快照；活动监听器、观察器、ticker、可达离屏 DOM 不得因每轮切页持续累积；进程/GPU 趋势单独报告 |
| 安全与可访问性 | 越权、漏遮罩、旧页面误响应、焦点丢失、写任务丢失等正确性回归为 0；性能变快不能抵消 |
| 构建预算 | 沿用当前构建门禁，不通过提高预算、忽略失败或缩减数据夹具制造通过 |

p50/p95 使用全部原始样本计算：每条成功交互路径至少 20 次，记录三轮独立运行及预热方法；不足 20 次的冷启/设备样本保留原值并标记样本不足，不做稳定 p95 声明。新鲜 browser context 不是磁盘/OS 缓存完全冷启动，必须写清“冷”的范围。

比较必须固定源提交/构建标识、数据集、素材模式、浏览器/WebView 版本、视窗、DPI、刷新率、电源状态和并发负载。先 A/B 同条件复测；超出基线波动且超过 10% 的交互延迟/资源成本退化作为调查线索，不直接把机器噪声写成代码结论。反复确认的退化不得直接通过。

### 4.3 必测场景

| ID | 操作序列 | 重点观察 |
| --- | --- | --- |
| S01 | 首页 → 场景库 → 导演台 | 首次/再次进入、按压反馈、内容交接、首个操作 |
| S02 | 导演台 → 作品册 → 返回导演台 | KeepAlive、编辑现场、无重复入场、无后台任务误取消 |
| S03 | 场景库与热门场景库滚动、输入筛选、清空筛选 | DOM 数量、更新范围、输入与滚动并行、锚点稳定 |
| S04 | 作品册/样张 → 打开预览 → 立即关闭 → 返回 | 图片就绪、来源关系、焦点与滚动恢复、Blob 生命周期 |
| S05 | 在动画和加载未完成时连续导航 A → B → C → 返回 | 最新导航目标、失败/取消、无陈旧覆盖和“幽灵点击” |
| S06 | 普通页面 ↔ chat；companion-chat ↔ 普通策略页面 | 文档策略重载、任务确认、清楚反馈、重启状态恢复 |
| S07 | 生成任务或 Live2D/语音开启时重复 S01–S05 | UI 延迟、主线程、CPU/GPU 争用与媒体行为；真实负载单列 |
| S08 | 20 轮往返、隐藏/恢复、最小化、休眠恢复 | 资源稳定、循环唯一、恢复无补播风暴 |

正常/失败/慢响应至少各覆盖一次；主流程覆盖深浅主题、默认/减少动态效果、鼠标与键盘、标准与窄窗口。真实高刷、4K/150% DPI、多屏与 Windows 安装版留在 F7，不能用 headless 或模拟缩放冒充设备证明。

## 5. 分批实施清单

### F0 · 先建立证据，不改变体验

**交付：** 可重复跑的场景、原始数据和一份按影响排序的瓶颈清单；没有找到瓶颈的项明确标为待测。

**文件入口：** `tests/e2e/office-performance.bench.ts`、`playwright.performance.config.ts`、`playwright.config.ts`、上述导航模块和 `package.json`。拟新增 `tests/e2e/ui-fluidity.bench.ts` 及可复用测量 helper；新增入口必须登记实际配置，不能只创建一个不会被 testMatch 发现的文件。

- [x] F0.1 核对现有性能命令实际选中哪些测试、使用哪个构建/网关与数据目录；保留导演台原基准以便对照。
- [x] F0.2 新增真正的 SPA 切页、KeepAlive 返回、滚动、连续导航基准，与冷/热文档导航分开输出；建立必要的导航/内容就绪标记，默认不上传遥测。
- [x] F0.3 建立固定隔离夹具与缓存条件，锁定路由、数据量和样图尺寸；不访问生产作品库，不执行真实生成或使用真实麦克风。
- [x] F0.4 至少测 S01–S05 的办公机基线，采集原始时序与代表性 trace；S06–S08 先登记环境要求与未执行范围。
- [x] F0.5 输出问题清单：场景、症状、证据、可能原因、已排除项、建议批次。区分网络、模块求值、挂载、图片解码/纹理上传、布局/绘制和计算阻塞。
- [x] F0.6 冻结测量口径与预算版本；比较启用/关闭测量的开销，避免诊断本身造成掉帧。

**验收：** 相同夹具能够重复得到可解释结果；所有样本绑定源/构建/设备；没有把 proxy 当真机帧率；输出至少一个可验证的下一批优先项，或明确“当前环境未复现”。

**回退：** 仅撤销新增诊断/helper 或禁用诊断开关；保留原性能入口，不修改产品行为。

建议提交：`test(perf): establish fluidity scenarios and baseline`。先测量，再决定 F1–F5 的具体文件增量。

**F0 回填（2026-09-17，办公机）：**

- 入口验证：`npm run test:e2e:performance` 选中 `office-performance.bench.ts` 与 `ui-fluidity.bench.ts`；两项均通过（单 worker）。原导演台冷/热文档导航基准保留并同轮执行。
- 固定条件：Chromium 153.0.4234.32、Windows x64、1440×960、DPR 1、`no-preference`、3 轮 × 20 样本；F0 夹具为 48 个场景、36 个样张、832×1216 SVG，空 localStorage/IndexedDB，状态接口离线回执，不触发生成/麦克风/生产作品库。
- 主要结果：S01 首页→场景库导航意图→主要内容 p95 209ms；S01 场景库→导演台 67.8ms；S02 导演台→作品册 68.9ms；S02 作品册→导演台 40.7ms。S05 共 60 次连续 A→B→C，累计采到 119 个取消事件，最终 URL 与目标一致。
- 候选问题：S03 60 次滚动窗口中 54 次超过 1% 的 1.5×60Hz 间隔候选，比例 p50 1.72%、p95 4.69%、最大 5.08%；S04 有 17/60 次关闭预览后滚动误差超过 2 CSS px（最大 68px）。它们只登记为 F3/F2-F4 调查线索，不把代理帧样本或固定夹具直接宣称为真机缺陷。
- 诊断开销：启用/关闭测量的 5 组热切对照中位差约 -2.9ms，未见启用标记造成可归因退化。冷/热文档进入结果仍见 `runtime/office-code-2026-09-11/browser-after.json`。
- 未执行：S06 文档策略重载与任务确认、S07 真实生成/Live2D/语音争用、S08 后台/最小化/休眠恢复与 20 轮资源快照；留给目标 Windows/真实负载验收。

### F1 · 即时响应与导航可打断

**交付：** 点击立刻有确认，导航期间能改主意，旧导航不会覆盖新目标。

**文件入口：** `useNavigationFeedback.ts`、`useRouteTransition.ts`、`AppInteractionLayer.vue`、`AppLayout.vue`、`src/router/index.ts` 及现有对应测试。

- [x] F1.1 检查按压/聚焦/待导航反馈是否在重计算前可见；保留 loading 的防闪烁延迟，不将按钮反馈也一起延迟。
- [x] F1.2 用最小的导航 ID/状态契约关联意图、就绪、失败和取消；优先扩展现有机制，不并存两套导航控制器。
- [x] F1.3 明确“当前内容保留至新内容或诚实错误状态就绪”的交接；慢响应时框架稳定、重试可用，不强制等待全部图片。
- [x] F1.4 验证 A→B→C、相同路由 query 更新、后退、加载失败、取消及用户改变 motion 偏好；确保动画清理完成回调只结算一次。
- [x] F1.5 离场内容不得继续响应输入；新内容可用后不因动画被整页锁定。仅显示反馈，业务提交仍使用原 click/键盘语义，避免 pointerdown/click 双触发。
- [x] F1.6 对比反馈 p95 与热切主要内容就绪；确认未增加固定等待、导航历史污染、误取消生成或焦点回退。

**验收：** S01/S05 的成功、失败和取消测试通过；每次最终可见页面、URL、导航目标一致；已取消页面不复活；初始反馈目标有测量结果，未达项如实保留。

**回退：** 撤销本批导航状态/过渡改动，保留基线与新增回归用例；不删除原有安全重载逻辑。

建议拆为 F1.1–F1.2 一次、F1.3–F1.6 一次提交；提交前分别定向验证。

**F1 回填（2026-09-17）：**

- 激活前由现有 `AppInteractionLayer` 记录 intent；导航栏即时显示 `data-intent`，loading 指示仍保留 180ms 防闪烁，不把视觉确认绑定到延迟计时器。
- `useNavigationFeedback` 扩展现有反馈状态，使用单一导航 ID 记录 `intent → pending → loading → completed/cancelled/failed`；旧导航不会清理新意图，未启动导航的 intent 1 秒自动过期。
- 内容交接：懒加载失败保留原页面并显示可重试的 `RouteRecoveryBanner`；新增失败重试、query 更新、浏览器后退、动态切换 reduced-motion 用例。
- 输入与动画：慢加载旧页面保持可见，成功后新页面解除 `inert`；缓存返回与快速反转验证通过，未增加 pointerdown/click 双提交。
- 当前工作树基准：S01 首页→场景库 intent→primary p95 291.3ms；S01 场景库→导演台 117.7ms；S02 导演台→作品册 77.5ms；S02 作品册→导演台 43.6ms。S05 60 次连续导航累计 117 个取消，测量开销中位差约 -2.9ms。
- 验证：导航反馈单测 7/7；`navigation-fluidity.spec.ts` 桌面项目 16/16（含新增 3 项）；构建与 bundle budget 通过。当前工作树含其他会话的内容改动，因此本轮数值只作趋势复核，不作纯净 A/B 归因。

### F2 · 热切换、图片与返回现场

**交付：** 再次访问不重复做无价值工作；返回时保留现场；缓存不破坏数据新鲜度。

**文件入口：** `AppLayout.vue`、`src/router/prefetch.ts`、`src/router/index.ts`、`GalleryView.vue`、`ShowcaseView.vue`、`PromptBuilderView.vue`、`VideoStudioView.vue` 及其实际数据/图片 composable；F0 记录实际依赖后再选改动。

- [x] F2.1 列出组件缓存、业务数据缓存、HTTP 缓存、解码图像各自职责；实测现有 KeepAlive 是否命中。不要以保留全部页/全部解码大图换“秒开”。
- [x] F2.2 为有限页面建立失活/激活契约：保留必要状态；停止只服务该界面的轮询/观察；共享生成任务不因页面离开而丢失。
- [x] F2.3 将滚动锚点、筛选与选择绑定稳定 ID、有效 query 和数据版本；同时覆盖 window 与实际内部滚动容器，避免路由滚动恢复互相覆盖。
- [x] F2.4 模块、必要数据与可见缩略图按用户意图有限预热；复用去重与失败重试，尊重现有节省流量策略。预热不得写数据、启动生成或读全库资源。
- [x] F2.5 图片占位预留正确尺寸；按显示尺寸选择可用派生图，首屏优先、其余按需；核对解码并发和纹理上传尖峰。不得损坏原图、遮罩或导出画质。
- [x] F2.6 为所有新增缓存指定容量、失效、释放和所有者；保存/删除/切素材根后不能继续显示错误旧内容。Blob URL 在仍被预览/下载引用时不得过早 revoke。

**验收：** S02/S04 返回现场正确；同一有效版本不重复全量加载；修改数据后正确刷新；缓存上限可测；热切收益与资源代价同时记录。缓存淘汰后仍可安全重建，不丢未保存编辑。

**回退：** 单独关闭/撤销本批预热或缓存策略；保留源数据与用户作品，无数据库迁移。状态契约与图像加载可分两个提交。

**F2 回填（2026-09-17，办公机）：**

- 缓存职责已登记：路由模块由 `prefetch.ts` 与浏览器 HTTP 缓存负责；场景业务数据由 `sceneStore` 按 DATA_VERSION、分片和在途去重负责；Gallery 缩略图由 KV dataURL、可见 HD 由 40 条 LRU、查看器大图由临时 Blob URL 负责；Showcase 只保留当前渲染页的浏览器图片与就绪标记。没有新增全量页面或全量解码缓存。
- KeepAlive 返回验证：工作台实例往返后仍复用；作品册失活时停止键盘监听、IntersectionObserver、待处理图片队列并关闭 Teleport 查看器；重新激活后按需重挂观察器。
- 返回现场验证：路由切换前在 beforeEach 捕获有限路径的 window 滚动，异步内容增高后以最多 1.5 秒的下一帧重试恢复；浏览器 savedPosition 优先；模态滚动锁在同步与两帧收尾中恢复滚动和 opener 焦点。
- 图片占位：Showcase manifest 支持经过解析的 `width/height`，卡片按实际尺寸预留空间，缺失尺寸使用显式 3:4 fallback；Gallery 原有尺寸占位、KV 缩略图与 40 条 HD LRU 保持不变。
- 证据：F0 固定夹具 3×20 重跑后 S04 预览关闭滚动误差 0/60；`navigation-fluidity.spec.ts` 19/19、`desktop-ux.spec.ts` 8/8、FluidDialog 相关单测 12/12；构建与 bundle budget 通过。
- 资源预热：保留 pointerover/focus 的模块预热；真实激活意图才追加场景核心 JSON 或画册 manifest + 前 4 张非 R18 缩略图，AbortController 取消旧目标，跳过 Save-Data/2G，不写业务状态、不读作品库、不启动生成。
- 证据：20/20 导航流畅度回归、8/8 桌面 UX 回归、预热单测与 FluidDialog 单测合计 15/15；F0 固定夹具 3×20 重跑后 S04 滚动误差 0/60。S03 仍有 59/60 窗口超过 1% 候选，转交 F3。

**F2.3 补充（2026-09-17，由 F3 取证发现）：**

- 缺口：F2.3 只覆盖了「路由返回」的滚动锚点，没覆盖**页面内筛选**导致的位置丢失。场景库滚到中段后输入搜索再清空，列表塌缩会把文档变矮，浏览器随即钳掉滚动位置 —— 真实数据实测 700 → 473（F0 夹具 2600 → 940），用户回不到原处。
- 处理：把 F2.3 已有的「有界锚点恢复」从 `router/index.ts` 抽成 [scrollAnchor.ts](../src/utils/scrollAnchor.ts) 共用（不并存两套），场景库在筛选变化时记住塌缩前的位置、等列表长回来再恢复；用户在筛选状态里自己滚动过（滚轮 / 触摸 / 滚动键，排除可编辑元素，否则打字会被当成滚动）就放弃恢复，不把人拽回旧位置。
- 踩到的坑：待恢复帧一开始做成模块级单槽，结果筛选写 URL 触发导航时，路由的取消除掉了筛选那一路（`full` 变体 scrollY 仍停在 940）。改成每次调用返回自己的取消句柄，两路互不干扰；单测里留了一条 `keeps two concurrent restores independent` 专门守这个回归。
- 已知限制：锚点在"筛选变化那一刻"采样，若用户仍在滚轮动画中途就输入，采到的是过渡位置（探针里各 `full` 变体落点不一致即由此而来，2600 / 2749 / 512 / 589）。要更准可在 `scrollend` 后再采样，但会推迟保护窗口，本轮未做。
- 验证：`navigation-fluidity.spec.ts` 22/22（新增「清空筛选回到原位置」与「用户自己滚过就不拽回去」两条；前者在旧实现上实测失败，698 期望 vs 473 实得）；`scrollAnchor.spec.ts` 5/5；`desktop-ux.spec.ts` 9/9；构建与包体预算通过。
- 未改善：本项只解决「位置不丢」，**不改变** S03 的掉帧候选（`full` 仍 6.8% / p95 25ms），列表重建那条仍在 F3。

**F0–F2 提交前复核（2026-09-17，办公机）：**

- 落库顺序：F0（测量入口与就绪标记）→ F2（热切资源、返回现场、图片占位）→ F1（即时反馈与意图预热）。F1 的 `AppInteractionLayer` 调用 F2 的 `prefetchRouteResources` 出口，按“每个提交都能独立通过类型检查”的顺序排列，不改变批次内容。
- 复核命令与结论：`npm run typecheck` 0 错误；`npm run lint:js` 0 error（6688 条既有 warning）；`npm run build` 路由包体预算通过；`npm run test:style-debt` 通过；`node scripts/maintenance/pin-scene-prompts.js --check` 报告 100 个定稿场景与基线逐字节一致；`npm run wf -- docs:check` 0 断链。
- 回归证据：`navigation-fluidity.spec.ts` 桌面项目 20/20、`desktop-ux.spec.ts` 9/9、样张源码契约 10/10；`npm run wf -- gate:full --all` 的 check / vitest / contract（33 过 0 挂）/ build 全部通过，unit 套件 1032 过 / 2 挂（见下，非本批）。
- 修复 1（本批引入的体验回归）：F2 的 `.sample-visual { aspect-ratio: var(--sample-ratio, 3 / 4) }` 在缺少尺寸数据时会强行锁 3:4。实测生产 `manifest.json`（2026-09-02_v27-miaomiao，1566 条）**不含任何 `width/height`**，该集合 2285 张缩略图中 84% 为 0.684（438×640）、9% 为 1.46 横构图、仅 5% 是真 3:4，因此绝大多数卡片会出现衬底色留白边；`test-showcase.js` 的“保持每张图原始比例”契约同时失败，且 F0 夹具（同样不含尺寸）的 S03/S04 基线几何会失去可比性。现改为只有 manifest 明确给出尺寸时才加 `sample-visual-measured` 锁比例框，缺失时退回原图自然高度；并补一条 `desktop-ux.spec.ts` 断言覆盖“未声明尺寸不得凭空锁比例”。
- 修复 2（本批引入的门禁硬错误）：`src/router/index.ts` 的 `prefetchRouteResources` 只导入未使用（`lint:js` error，重导出由独立 `export ... from` 承担）；`tests/e2e/helpers/ui-fluidity-fixture.ts` 直接使用 `window.__AICS_UI_FLUIDITY__` 但未引入应用侧全局声明（`tsconfig.json` 覆盖 `tests/e2e/**`，`npm run typecheck` 报 TS2339，现按同目录 helper 的既有写法做局部收窄）。
- 未通过（HEAD 既有，非本批，2026-09-17 已修复于 `86ec1ce`）：`scripts/tests/test-api-client.js:374` 是 `addCount` 断言过时（期望 1 实得 2）—— `callerSignal` 上确实需要两个 abort 监听：`trackSharedConsumer` 的 `release` 释放共享传输名额、`awaitShared` 的 `abortListener` 让本次等待立即以 aborted 结束，合并会导致仍被他人持有的传输继续拖着本次调用等待；已改为断言"注册与移除一一对应、且确有注册"的不变量，探针实证两个监听器均在结束后移除、零泄漏。`scripts/tests/test-prompt-compiler.js:77` 期望画师目录 52 位、实得 53 位，来自 `5e0c796` 接入画师 hidulume 时未同步断言与注释，已补齐并追加历史说明。修后完整 unit 套件 1034 过 / 0 挂。
- 未执行：F0 的 S06–S08、真实设备负载均未在本轮重跑。

**F0 掉帧判据校准与 S03 重测（2026-09-17，F3 起步）：**

- 问题：`summarizeFrameProbe` 用硬编码的 1.5×16.7ms=25.05ms 判掉帧，与 §4.2「掉帧候选阈值可用静止前台的有效间隔 T 校准」不符。实测本机 T=8.3ms（前台 120Hz），且 F0 原始报告自带的帧间隔 p50 就是 8.4ms —— 采集当时已是 120Hz 前台，该判据自始错配：120Hz 上 16.7ms 已是一次丢帧，却通不过 25.05ms 阈值，S03 因此被低估。
- 处理：新增 `measureIdleFrameInterval`，在每轮 S03 采样前实测静止间隔 T，判据改为「rAF 间隔 > 1.5T」；报告同时保留 60Hz 参照值，并新增 `runner.display`（实测 T 与反推刷新率）供后续核对。旧判据报告存档为 `runtime/ui-fluidity-f0/ui-fluidity-f0-legacy-60hz-criterion.*`，未删除、未改写。
- S03 重测（`170cba1`，3×20，120Hz 前台，固定夹具）：60/60 窗口超过 1% 目标；校准候选比例 p50 7.46%、p95 10.17%、max 10.61%（同一批样本的 60Hz 参照为 39/60、p50 1.54%）。帧间隔 p95 中位 16.7ms（120Hz 下一次丢帧）、p95 25.0ms（三次）；`longtask` 计数为 0 —— 该热点不是单次 >50ms 的主线程阻塞，而是每帧更新/渲染开销超出 8.3ms 预算，指向 F3.2 的更新范围与 F3.3 的列表成本。
- S04 未复现 F2 的 0/60：同一批 60 个样本中 13 次关闭预览后滚动误差 >2 CSS px（众数 67px，最大 211px）。F0 旧报告在「原图自然高度」几何下为 17/60（最大 68px），而 F2 记录的 0/60 是在强锁 3:4 的几何下取得 —— 差异取决于卡片高度是否由图片解码决定，不是本轮修复引入的新回归；S04 仍留给 F3/F4。
- 导航趋势（同批，工作树含未提交测量改动，只作趋势）：S01 首页→场景库 intent→primary p95 258.4ms、S01 场景库→导演台 69.2ms、S02 导演台→作品册 59.1ms、S02 作品册→导演台 42.4ms。
- 未执行：本批只改测量口径，未动任何产品渲染路径；F3.2 起才按证据改列表与更新范围。

### F3 · 滚动与主线程减负

**交付：** 针对已测热点减少不必要的渲染/计算，而不是无差别“优化所有循环”。

**文件入口：** `SceneExplorerView.vue`、`PopularSceneExplorerView.vue`、`GalleryView.vue`、`ShowcaseView.vue`、`PromptBuilderView.vue` 的实际列表与 computed/watch 依赖。先沿导入定位；搜索不到某关键字不代表没有虚拟化或缓存。

- [x] F3.1 用 Vue/浏览器性能记录定位重复挂载、宽范围响应式更新、同步过滤/排序、强制布局和图片相关尖峰；每个改动绑定一个证据。
- [ ] F3.2 优先稳定 props/key、复用计算和索引、减少深层响应式追踪；不可变大集合仅在符合真实更新契约时使用 shallow 方案，不能让界面漏更新。
- [ ] F3.3 大列表确实是瓶颈时，先复用已有窗口化/分页；补足 overscan、动态行高、键盘访问、焦点保留与滚动锚点。不要为了减少 DOM 破坏筛选完整性。
- [ ] F3.4 消除高频读写布局交错，按帧合并可合并更新；搜索考虑输入法 composition，不能为了节流吞字或延迟按键反馈。
- [ ] F3.5 只有确有 CPU 热点时才拆分任务或移到 Worker；将序列化/复制、取消、旧结果丢弃和打包成本计入端到端比较。
- [ ] F3.6 用相同数据量与尺寸复测 S03/S07 的前端夹具。一次只改一个热点；没有收益的改动不因代码“看起来更底层”而保留。

**验收：** 过滤/排序结果与旧实现一致；快速输入不会回跳旧结果；帧时间、长任务和关键操作有配对证据；不降低素材质量、不增大无限缓存；无新增焦点和无障碍退化。

**回退：** 按热点单独 revert；纯函数保留旧实现对照。Worker 失败有安全回退，不能让业务永久等待。

**F3.1 回填（2026-09-17，办公机 120Hz 前台）：**

- 新增测量能力（`tests/e2e/helpers/ui-fluidity-measure.ts`、`ui-fluidity.bench.ts`、`ui-fluidity-fixture.ts`）：CDP `Performance` 域的窗口级 Script / Layout / RecalcStyle 成本与计数；`thumbFormat: 'micro'` 的 1×1 PNG 对照图（请求数与 DOM 结构不变，仅解码成本≈0）；S03 分解探针（scroll-only / scroll-only-micro / filter-only / full / full-low-glass）。带 tracing 的样本单列、不混入聚合：实测 trace 会把同一窗口的 task 从 213ms 抬到 324ms、DOM 节点从 9747 抬到 22522。
- S03 基线（`6a307f6`，3×20，59 个非 traced 样本）：100% 窗口越 1% 目标，候选比例 p50 7.14%、p95 9.52%；窗口内主线程 task p50 234ms / p95 264ms，其中 script **2.38ms**、layout 12.6ms、recalcStyle 26.2ms（`renderShareOfTask` 16.5%）、layoutCount 19、recalcStyleCount 78、缩略图请求 19。原始报告 `runtime/ui-fluidity-f0/ui-fluidity-f0.json`。
- 分解探针（各变体单次，仅用于定位，不作统计结论；报告 `runtime/ui-fluidity-f0/ui-fluidity-f0-s03-decomposition.json`）：
  - `scroll-only` task 113–122ms / layout 1.9–2.2ms / 帧候选 3.8% / p95 帧 8.5–8.9ms（多次运行稳定）；
  - `filter-only` task 196–206ms / layout 11.4–11.9ms / 帧候选 3.3–6.7% / p95 帧 8.5–16.7ms；
  - `full`（滚动 + 窄查询，列表 24→1→24）task 235–267ms / styleRecalc 72–74 / 帧候选 6.8–6.9% / p95 帧 16.6–24.9ms。
- **决定性对照 `full(wide-query)`**：把查询词换成匹配全部固定场景（筛选、排序、防抖、写 URL 全部照跑，唯一差别是列表不增删卡片），帧候选稳定回到 **3.3–3.4%**、p95 帧 **8.5ms**，与纯滚动同水平；styleRecalc 也从 74 降到 50。→ **掉帧的直接原因是筛选引起的列表增删（卡片销毁/重建 + 文档高度塌缩回弹），不是滚动本身、不是筛选计算本身。**
- **已排除的机制（每条都有对照证据）**：图片解码/光栅（零解码对照 task 差 −1.8～+4.8ms，`full(micro)` 的 p95 仍在 16.6–24.9ms）；筛选/排序算法（script 全程 2.4ms）；列表入场动画（`.scene-grid.stagger-container > *` 已被显式关闭）；吸顶玻璃模糊（`full(low-glass)` 关闭后 computed 为 `none`，p95 仍 24.9ms）；缩略图 `filter:blur()` 渐变过渡（`full(no-thumb-blur)` 注入后 computed `filter:none`、`transition:none`，p95 仍 24.9ms）。
- 诚实性说明：单次运行下 `full` 系列的 p95 帧在 16.6 / 24.9ms 之间跳动，"掉帧程度"受运行时噪声影响；能稳定区分的是**是否**掉帧（增删列表 6.8–6.9% vs 不增删 3.3–3.4%），不能据此比较各排除项之间的小差异。分位值需要更多样本才能作为结论。
- 结论：F3.2 的原切入点（减少响应式追踪、复用计算）收益有限（script 仅 2.4ms），真实目标是**筛选结果增删时的卡片重建与文档高度突变**。候选方向按"最小可测、可回退"排序：①重建时缩减每张卡的派生计算与 DOM/样式重算面（style 33ms + layout 14ms + styleRecalc 72 次是主要可归因成本）；②避免列表塌缩造成的文档高度突变与滚动位置钳制。任何改动都要过双主题 + reduced-motion 视觉审查，并用同夹具复测 `over1_5xBudgetRatio`。
- **收益上限诊断（2026-09-17）**：注入 `.sc-body { display:none }` 把卡片正文区整块从布局里摘掉（视觉会坏，仅用于测上限），只省下 style 2.4ms + layout 4.3ms，帧候选反而 6.8% → 8.9%（同一轮噪声量级）。→ **"把卡片做轻"收益上限很低，不值得为此做结构性改造。**
- **归因修正（两因子对照）**：把"是否已滚动"和"列表是否增删"分开看，证据指向的其实不是增删本身：页面在顶部 + 筛选增删 = 3.2% / p95 8.5ms；已滚动 + 筛选不增删（wide-query）= 3.5% / 8.5ms；**已滚动 + 筛选增删 = 6.8% / p95 16.8ms**。三者主线程成本几乎相同（style 24.5–25.2ms、layout 11.3–11.8ms），但帧结果差一倍 → **代价在合成器/光栅（滚动位置跳变后的重绘），不在主线程**，与 `longtask` 为 0、script 2.4ms 一致。
- 因此 F3 的可动目标应落在"减少已滚动视口在内容变化后的重绘/光栅"，而不是主线程计算或卡片重量。候选：给列表卡片加 `content-visibility: auto` + 准确的 `contain-intrinsic-size`（离屏子树跳过样式/布局/绘制/光栅，屏内渲染结果不变），需用同夹具复测并做双主题视觉验收。
- 未执行：S07 前端夹具（需要真实生成/Live2D 负载）；所有变体仅在办公机 headless Chromium 上测得，帧候选仍是代理指标，必须与 F7 的真机录像配对才能关闭掉帧问题。同一变体在不同轮次间 p95 在 8.5–25ms 波动（本轮 `scroll-only(micro)` 甚至从 3.9% 跳到 5.7%），**单轮小差异不可作为结论**。

**F3.1 第二轮（2026-09-17）：候选做尽、残差归因交给 F7**

- **候选其实早已存在**：`content-visibility: auto` + `contain-intrinsic-size` 已经在 `scene-card.css:18`、`gallery-view.css:55`、`character-view.css:28`、`PopularSceneExplorerView.vue:482` 里。注入同值后帧候选 7.0% vs 7.0%、p95 帧 16.7 vs 16.8ms —— 完全无余量，不是可做的优化项（变体自证列读到 `content-visibility: auto` 才发现）。**教训：提出优化前先确认代码里是否已有。**
- 探针升级：同一变体按 `AICS_FLUIDITY_PROBE_REPEAT`（默认 3）重复并取中位数 —— 单轮已证明不可用（同一变体跨轮 3.0%→9.0%、p95 8.5→33.3ms）。
- 机制区分：`full(narrow-only)`（只筛不清空，全程只发生"文档塌缩把滚动位置钳掉"这一次跳变）5.0% / p95 16.6ms，对比 `full(svg)`（筛选 + 清空 + 位置恢复）9.0% / p95 25.0ms → 钳位那次跳变约占一半，清空与恢复再加一半。
- **本轮出现明显漂移**：`full(wide-query)` 从上轮的 3.0% / 8.5ms 变成 5.0% / 16.6ms，`full(no-thumb-blur)` 的 task 一度到 612ms（同期另一会话在同一工作区做标签分片并构建）。→ **并行会话的构建负载让小于 2× 的差异不可分辨**，S03 的精确归因需要安静机器 + 真机 trace，属 F7；不应继续在无头环境里堆变体。
- 结论：S03 **部分验收**。已知可复现停顿的范围收窄到「已滚动 + 文档高度变化」；主线程无阻塞（`longtask` 为 0、script 2.4ms），图片 / 玻璃 / 缩略图 blur / 卡片重量 / `content-visibility` / 单纯的列表增删均已用对照排除。剩余 5–9% 的掉帧候选是代理指标层面的残差，不做无证据的改动。
- 唯一已知的剩余杠杆（保留网格最小高度以消除钳位跳变）会带来"结果很少时下方留白"的**视觉变化**，与「不减弱视觉效果和动效」的原则冲突，属产品决策，本轮不做。

**F3 回填（按 §7.2 模板）：**

```text
批次 / 子批次：F3（F3.1 定位完成；F3.2–F3.6 未执行）
状态：部分验收
基线提交 / 受测实现提交 / 构建标识：基线 6a307f6；包含 F0–F2 与径向修复的 170cba1 / 172eb47 / bd2639d；构建为 dist（未单独记录 SHA-256，见"未执行"）
改动与未改动范围：改动仅限诊断/证据基础设施（tests/e2e/helpers/*、ui-fluidity.bench.ts、playwright.performance.config.ts）与 F2.3 的滚动锚点补充（src/utils/scrollAnchor.ts + 场景库接线）；**未改动任何渲染路径、样式、动效时长或缓存策略**
场景 / 机器 / 缓存状态 / 数据夹具 / 负载：S01–S05；办公机 Windows x64 / Chromium（headless）/ 1440×960 / DPR 1 / 120Hz 前台 / reducedMotion 默认；固定夹具 48 场景 + 36 样张 + 832×1216 图；空 localStorage/IndexedDB；无生成/麦克风/生产库。**注意：同期另一会话在同一机器上构建，部分轮次出现漂移**
测试命令、退出状态与日志：`npm run test:e2e:performance`（PASS，单 worker）；`npm run check` 16/17（余 1 项为并行会话的词条套件清单）；`navigation-fluidity.spec.ts` 22/22；`desktop-ux.spec.ts` 9/9；`scrollAnchor.spec.ts` 5/5；`docs:check` 0 断链
前后指标：S03 校准判据（rAF 间隔 > 1.5×实测 T=8.3ms）下候选比例 p50 7.14% / p95 9.52%（60 窗口全部越 1% 目标）；窗口内 task p50 234ms，其中 script 2.38ms / layout 12.6ms / recalcStyle 26.2ms；longtask 0。位置恢复：清空筛选后落点由 473 回到 ≥698（真实数据）
双主题 / 键盘 / reduced-motion / 安全边界：本轮未改动 UI，双主题与 reduced-motion 由既有回归覆盖并通过；未触碰 CSP、分级、写任务契约
未执行项、失败项与原因：F3.2–F3.6（依据 F3.1 对照证据：卡片重量上限 ~7ms、content-visibility 已在代码中存在、图片/玻璃/blur/单纯增删均已排除，继续在无头环境堆变体无法分辨 <2× 差异）；S07 前端夹具需真实负载；构建 SHA-256 未单独登记
是否保留本改动 / 回退方式：保留诊断基础设施与滚动锚点；回退只需 revert 对应提交，无数据/格式迁移
远端提交与证据位置：0aa8a41 / 885fa6e / 1332e1a / 170cba1 / 6a307f6 / 86ec1ce / c509d48 / 172eb47 / bd2639d / 7832315 / d67d588；去敏摘要见 docs/evidence/ui-fluidity-f0-f3-2026-09-17.json，原始结果见 runtime/ui-fluidity-f0/ 下 ui-fluidity-f0.json、-f3-baseline.json、-s03-decomposition.json、-lean-probe.json、-cv-probe.json、-narrow-mechanism.json 与 -legacy-60hz-criterion.*
下一批允许开展的范围：F4（动效统一）；F3 若要继续必须先在安静机器上取得可分辨的证据
```

**移交 F7 的清单（S03 残差精确定位）：**

- 需在**无并行构建负载**的机器上重跑 `AICS_FLUIDITY_PROBE_REPEAT≥5` 的分解探针，先把 `full(wide-query)` 的漂移压到可复现区间，再谈残差归因。
- 需要真机 trace/录像与帧候选配对：`frameIntervalP95Ms` 中位 16.7ms（120Hz 丢一帧）在真机上是否可见，只有 F7 能判定；`longtask` 为 0 说明这不是主线程阻塞问题。
- 待办里仍悬着的一条：S04「关闭预览后滚动误差」，F0 旧口径 17/60、本轮 13/60（众数 67px、max 211px），与 F2 记录的 0/60 不一致 —— 需要在真机与正确几何下重新取证。
- F3 未做的 F3.2–F3.6 若在真机证据下被判定仍需执行，按"一次只改一个热点 + 同夹具复测"重开。



### F4 · 统一动效行为与空间连续性

**交付：** 同类操作表现一致，开合有来源，动画中途可结束或改向；绘遇风格不变。

**文件入口：** `useRouteTransition.ts`、`AppLayout.vue`、实际预览/抽屉/弹窗组件、`src/utils/motionPreference.ts`。共享时长/缓动从当前 DESIGN 与真实 token 定义核对，不凭旧示例新增第二套值。

- [x] F4.1 将现有动作整理为按压反馈、同级导航、层级进入/返回、抽屉/弹窗、内容更新五类；列出现有规范与例外，再最小统一。
- [x] F4.2 同级导航保持外壳稳定，避免整页大幅滑动或文本缩放；缓存激活和 query 改动不机械重播首次入场。
- [x] F4.3 先选“作品预览”一个局部试点来源/去向关系；来源已移出视口或无可靠几何时走轻量回退，不强迫做共享元素特效。
- [x] F4.4 动画中收到关闭/新导航时，从当前可见状态自然收尾或改向，避免先跳回初始位置；事件清理、`inert`、焦点和滚动锁均有生命周期测试。
- [x] F4.5 重叠内容只有当前层可操作，装饰克隆必须从可访问树和输入中排除；过渡结束清除 transform/fill/will-change，核对 fixed/sticky 工具栏坐标。
- [x] F4.6 减少动态效果启用/运行中切换时即时收敛；无 WAAPI 或可选能力失败时仍能导航。仅在测量证明必要后评估新增 API/依赖，并保留回退。

**验收：** S01/S04/S05 在两主题、鼠标/键盘、低效果/减少动态效果下通过；不会为过渡出现空白、文字模糊、焦点跳失或返回方向错误。帧时间与输入延迟不得因“更好看”退化。

**回退：** 局部试点可关闭/撤销并回到现有轻量入场；不要让整个导航依赖某种浏览器专属过渡能力。通过试点后才扩到其他同类组件。

**F4.1 回填（2026-09-17，盘点）：**

| 类别 | 现有实现 | 时长 / 曲线 | 规范状态 |
| --- | --- | --- | --- |
| 按压反馈 | `--motion-press` 与各页 `:active` | 120ms + `--ease-out` | 令牌存在，但引用极少 |
| 同级导航 | [useRouteTransition.ts](../src/composables/useRouteTransition.ts)（WAAPI `translateY(6px)`） | **220ms 硬编码** + `cubic-bezier(.22, 1, .36, 1)` | **例外**：与 `--motion-route` 320ms 不一致，曲线也不在令牌集内 |
| 层级进入/返回 | `.layer-pop-*`（`design-system.css:1073–1083`） | 进入 `--motion-surface` 260ms、离场 `--motion-hover` 160ms，均 `--ease-out`；位移 14→8px、scale .96/.98 | 令牌化最好的一类，可作基准 |
| 抽屉/弹窗 | [FluidTransition.vue](../src/components/visual/FluidTransition.vue) + [useFluidSurface.ts](../src/composables/useFluidSurface.ts)（`:css="false"` JS 驱动，`data-fluid-leaving`） | 时长走 CSS 令牌，含 `--ease-drawer` | 机制统一；但浮层选择器清单硬编码在组件 props 默认值里 |
| 内容更新 | `.stagger-item` / `.stagger-container > *`（20/40/60ms 错峰）、`[data-reveal]`、`skeleton-shimmer`、`facetIn`、showcase `sample-*` | 混合：`--motion-control` 与硬编码 `.22s` | **例外**：`.scene-grid` / `.showcase-grid` / `.gallery-wall` 已显式 `animation:none`（高频筛选不重播），其余仍在错峰入场 |

令牌现状（`design-system.css:221–235`）：时长 `--motion-press` 120 / `hover` 160 / `control` 200 / `surface` 260 / `route` 320 / `route-cut` 420 / `atmosphere` 560；**旧别名 `--t-fast` 150 / `--t-base` 240 / `--t-slow` 300 与新令牌双轨并存**。曲线有 `--ease-out`、`--ease-in-out`、`--ease-drawer`、`--spring-bounce`、`--spring-soft`。

**使用率（全仓 `transition`/`animation` 声明扫描）：只有 4 条引用 `var(--motion-*)`/`var(--t-*)`，58 条硬编码时间值** —— `chat.css` 10、`design-system.css` 9、`companion.css` 8、`director/stage.css` 5 等。样例跨度 0.22s–9s，其中相当一部分是氛围/循环类（ambience 7s/9s、`voice-pulse` 1.1s infinite、`cursor-blink` .8s infinite），属 F5 范围，**不按交互时长统一**。

待最小统一的例外（每项都要过双主题 + reduced-motion 视觉验收）：

1. `useRouteTransition.ts` 的 220ms 与自有曲线 —— 同级导航里唯一不走令牌的一处。
2. `--t-*` 旧别名与 `--motion-*` 双轨。
3. `.22s` 在 `facetIn`、`message-in` 等同族动效里与 `--motion-control` 200ms 混用。
4. 浮层选择器清单硬编码在 `FluidTransition.vue` 的 props 默认值中。

注意：统一时长**会改变动效观感**（例如同级导航 220ms → 320ms）。按「不减弱视觉效果和动效」的原则，这类改动必须先给出可对比的前后观感、经确认后再落，不能只凭"更统一"就改。

**F4 后续实现回填（2026-09-18 至 2026-09-19）：**

- `97ade97` 完成 F4.6 的路由局部批次：运行中切换减少动态效果、缺少或调用失败的 WAAPI、取消异常和重复回调均能一次性收尾；22 个定向用例已登记。它不等于两主题、键盘和真实浏览器视觉验收。
- `a7eb368` 将旧 `--t-*` 别名映射到语义 motion token，不再保留第二套时长数值；没有把现有 220ms 路由入场强行改成 320ms。
- `05bfe9b` 将浮层、全屏查看器和 popover 的选择器集中到 `useFluidSurface.ts`，修复根节点本身命中选择器时仍误取外层的问题，并补现代/旧版 motion 偏好监听与销毁测试；该提交当时未关闭 F4.2–F4.5，后续由 `9d8d1fe` 接续。
- `9d8d1fe` 完成办公机收尾：缓存页面复用同一 DOM 时不重播首次入场；作品预览从仍在视口内的来源控件计算二维 transform origin，来源离屏时回退中心；spring 改向先结算上一 Vue 回调，leave-cancelled 立即恢复 `inert`；Showcase 只保留 `useFluidDialog` 一个滚动锁所有者。导航回归 22/22、快速搜索反转双主题 2/2、样张 20 轮开关滚动误差均通过。原生 WebView2 的观感、DPI 与高刷仍归 F7。



### F5 · 长时间使用与桌宠/生成共存

**交付：** 不可见部分少做无用工作，恢复时无重复循环；在资源压力下保住交互。

**文件入口：** `RouteAtmosphere.vue`、实际粒子/Live2D composable、`services/live2d-textures.ts`、`desktop-tauri/native-live2d` 及已有可见性/偏好管理。必须先读实际生命周期，不建立第二套全局调度器。

- [x] F5.1 盘点 rAF、timer、观察器、ticker、媒体与 IPC 的所有者；区分页面失活、文档隐藏、窗口最小化和用户仍需后台执行的任务。
- [x] F5.2 在适当生命周期暂停无用视觉工作，恢复时只创建一个循环，丢弃过时视觉更新，不补播积压动画；媒体/生成保持既有契约。
- [ ] F5.3 测试保留四类 KeepAlive 页面后的稳态内存、解码图像、纹理与 GPU 成本；不因离场释放正在使用的共享资源。
- [x] F5.4 复用现有效果档位；资源压力时优先降低装饰和不可见负担，保留文字清晰度、操作反馈与结果质量。设置有迟滞/恢复边界，不能来回跳档。
- [x] F5.5 办公机只做模拟负载、生命周期和取消契约验证；真实 GPU 争用、Live2D 渲染、音画同步和功耗交给 F7。
- [x] F5.6 不默认卸载用户模型、修改采样参数或限制业务并发。确需改变调度时，单列测量依据、产品影响和授权边界后另做小批次。

**验收：** S08 夹具中循环/监听器不重复、恢复行为正确；20 轮资源趋势有记录；默认/低效果均可恢复。实际 GPU/音画指标未测时保持待设备验收，不用纯 Rust 或浏览器单测关闭。

**回退：** 分别撤销视觉暂停/资源策略，回到现有生命周期；不改用户设置格式、不删除模型、缓存以外的素材或作品。

**F5 局部实现回填（2026-09-19）：** `05bfe9b` 让 `AnimatedSelection` 在 KeepAlive 失活或文档隐藏时取消帧循环并释放 spring，恢复后只重新调度一次；`6eac3cf` 让 `RouteAtmosphere` 复用既有低效果／低玻璃档位隐藏纯装饰背景。对应单测已进入完整门禁；当时未完成的所有者盘点与办公机生命周期由下方 `9d8d1fe` 接续，真实 GPU/纹理/功耗仍留 F7。

**F5 办公机收尾（`9d8d1fe`）：** 盘点确认路由／浮层 motion、粒子 scheduler、场景卡光斑、Gallery/Showcase 观察器、绘图页健康轮询、Live2D ticker 与语音口型各自所有者。新增 `useParticlePerformanceLifecycle`：KeepAlive 失活停帧，低效果档降为 20fps 与 65% 粒子密度，恢复不补播；场景卡取消离场待执行帧。绘图页失活只停止并中断模型健康轮询，不取消真实生成 job；重新激活只恢复一个轮询。Anima 会话 16 项测试含“后台暂停不改变运行任务”。F5.3 的四类页面稳态 GPU/纹理/解码内存与功耗仍需目标 Windows 环境，转 F7，不以 20 轮弹窗测试代替。

**2026-09-19 当前基线：** 在 `main@d738590`、`AICS_REFERENCE_AUDIT_MODE=structure` 下执行 `npm run wf -- gate:full`，17 项质量检查、Vitest、1,043 项 Node 单测、34 组契约及构建通过，总耗时 6 分 33 秒。未配置素材根的默认实物模式会因 2,534/2,534 个已登记参考文件不可见而失败；这属于外部素材缺口，不用 structure 结果关闭 V01，也未在本轮重跑浏览器或性能场景。

### F6 · 固化回归、预算与批次交付

**交付：** 后续开发不会轻易把已经改善的手感改坏；行为测试与性能测试各司其职。

**文件入口：** 新增/扩展的流畅度测试、现有 Playwright 配置、`package.json`、`scripts/workflow.ts` 与 `docs/workflow.md`。拟新增 `tests/e2e/ui-fluidity.spec.ts`；名称为计划值，不表示文件已存在。

- [x] F6.1 固化快速导航、失败/取消、缓存恢复、减少动态效果、离场不可交互等行为回归；复用现有测试，不以海量截图替代行为断言。
- [x] F6.2 性能测量继续单 worker、固定夹具和独立运行；改动 testMatch 后验证实际发现测试列表，避免“命令成功但没跑到新测试”。
- [x] F6.3 共享模拟上游的用例沿现有串行/隔离策略，不让一个测试 reset 另一个测试的服务。禁用隧道、使用隔离 runtime，不复用未知生产服务。
- [x] F6.4 将 F0 冻结并经复测确认的预算落入测试/报告；CI 噪声较大的指标保留原始分布，行为正确性仍是硬门槛。不得使用 continue-on-error 掩盖失败。
- [x] F6.5 本轮只补真正新增入口的工作流/文档登记；不改 lockfile、不升级依赖、不提升现有包体预算来“通过”。
- [x] F6.6 生成可对账的前后比较与交付摘要，关联最终受测实现提交和构建标识；若仅新增文档回执，再核对其父实现内容一致，避免循环自填最终 SHA。

**F6 回填（2026-09-19）：** 完整结果见[去敏证据](../docs/evidence/009-f4-f6-office-2026-09-19.json)。性能入口第一次按默认附加分解探针运行，在主场景完成后命中 15 分钟上限且未写报告；最终以同一 3×20 主场景、单 worker、隔离端口重跑，显式跳过已另有历史证据的 overhead/decomposition，10.3 分钟通过。修正 S04 测量先让 Playwright 完成目标自动滚动再记基线，最终 0/60 漂移；旧 11–21/60 结果混入自动化滚动，不直接同比。S05 60/60 最终落在 `/prompt-builder`。本轮宿主静止间隔 18.5ms（约 54Hz）且 S01/S03 明显受负载影响；不与 120Hz 历史轮次比较，不关闭 S03/F7。

完整门禁的质量检查、Vitest、1,045 项 Node 单测均通过；契约套件中 `test-maintenance-blueprint-transaction.js` 因当前宿主负载命中每文件 180 秒外层超时，未出现断言失败，随后同文件直接执行 9/9 通过（245 秒）。因此本批不宣称 `gate:full` 总体 PASS；生产构建、18 路由包体预算及预压通过，未提高预算或修改依赖。

**验收：** 定向回归、构建预算、文档链接等按实际改动范围通过；失败和缺环境明确记录。headless CI 通过只证明其覆盖范围，不得写“全机型 120 FPS”。

**回退：** 撤销不稳定的新性能阻断门槛时保留采样报告与待修项，不能撤掉正确性/安全回归。预算调整需单独审查理由。

### F7 · 目标桌面与真实负载验收

**交付：** 对实际安装版作出有设备证据的结论，明确已达到和仍未达到的范围。

**前置：** F6 的受控实现/构建标识、目标主力机、真实资源、部署授权及必要 UAC 操作就绪。桌面同步只走 [部署指南](../docs/desktop-deployment.md) 的 `deploy-desktop.bat` 入口；纯同步、完整安装、启动与验收分别记录，不混为一个 PASS。

- [ ] F7.1 核对源码提交、安装目录构建、网关与素材配置；不在旧 exe/旧前端包上评价新批次。
- [ ] F7.2 在真实 Windows WebView2 上执行 S01–S08；主目标 4K/150% DPI；可用时分别测实际 60Hz/120Hz，不可用则明确标缺设备。
- [ ] F7.3 覆盖深浅主题、缩放、窗口移动/多屏、最小化/恢复、休眠、键盘焦点、低效果与系统减少动态效果。
- [ ] F7.4 分空载、Live2D/语音、真实生成并发采集 CPU/GPU/内存与交互；每组记录引擎/硬件/模型配置，不靠减少任务或修改出图质量隐藏争用。
- [ ] F7.5 记录真实片段和配对 trace；录像帧率不足时只证明可见操作，不据此宣称高刷呈现。保留所有失败样本与外部瓶颈。
- [ ] F7.6 将通过范围与剩余限制回填本计划及 roadmap，关联既有 V04/V05/V07/U6；只关闭覆盖且确实通过的子项。相关内容、模型和其他功能验收不因本专项通过而自动完成。

**验收：** 有与受测构建一致的安装、操作、性能和资源证据；用户主要路径的响应与连续性得到确认。设备不足或仍掉帧时允许“部分验收”，不写“已达到苹果级丝滑”。

**回退：** 按部署指南使用已验证的上一构建并验证用户数据保留；不 `reset --hard`、不强推 main、不回滚作品/定稿数据。

## 6. 每批验证与提交规则

本节是后续执行指南，**不是本次已经运行的命令记录**。入口以执行时的 `package.json`、`scripts/workflow.ts` 和工作流手册为准；生成的 `.js` 不手工维护。

| 改动类型 | 必须核对的验证 |
| --- | --- |
| 仅计划/索引文档 | Markdown 结构、相对链接与索引；具备完整工作区时 `npm run wf -- docs:check`；不因文档提交运行真实模型或全量 UI |
| composable/纯状态逻辑 | 对应单测、前端类型检查，导航/取消/激活场景回归；按范围选择 quick gate |
| 页面/CSS/动效 | 受影响页面行为测试、双主题与 reduced-motion 视觉审查、相关样式门禁；引用同一次生产构建做对比 |
| 列表/缓存/Worker | 正确性、失效/取消/释放测试，同数据基准，构建预算；验证预热不产生业务写入 |
| 公共路由/构建/测试配置 | 实际测试发现列表、相关关键回归与构建；按工作流判断是否需要完整门禁 |
| 原生/桌面资源 | 对应构建和契约检查，实际设备/安装验收单列；无设备不能代替通过 |

可复用入口：`npm run typecheck:app`、`npm run test:frontend`、`npm run build`、`npm run test:e2e:performance`、`npm run wf -- gate:quick`、`npm run wf -- gate:full`。不机械逐批全跑，也不把不同工作流入口的“是否自动 build”视为相同；性能入口应指向明确的生产产物。

性能与普通回归分开执行。测试环境使用已核对的隔离端口/runtime 配置和假设备；现有 Playwright 在非隔离开发环境可能复用服务，执行前确认不连生产实例。使用 fixture 的通过不等于真实素材、麦克风、GPU 或模型可用。

提交要求：一个独立改动对应一个可回退提交；写入前检查 `git status` 和 `git diff`，只暂存受控文件，保护其他会话。直接提交 main 时也禁止 force push。远端推进或目标文件变更时重新读取/合并，不覆盖他人修改。测试通过后提交并推送，再核对远端内容。

每批结束必须报告：改动文件、实际测得的收益/代价、测试命令及结果、未执行项、提交标识、下一批前置条件。不能用“编译成功”“没有报错”替代操作流畅度验收。

## 7. 证据、复盘与完成定义

### 7.1 证据存放建议

F0 可建立 `runtime/ui-fluidity/<run-id>/` 保存原始 JSON、trace、视频和截图；该路径是**拟使用的本地输出目录**，写入前确认忽略规则。只在 `docs/evidence/` 提交小型去敏摘要与证据索引，不能把聊天、提示词、凭据、个人作品或绝对素材路径带入 Git。

每份摘要至少包含：批次、场景 ID、基线/受测提交、构建 SHA-256、机器/OS、浏览器或 WebView 版本、显示刷新率/DPI、夹具哈希/素材模式、负载、采样数量、原始结果位置、统计方法、预算版本、通过/失败/未运行和回退决定。字段未知时填写 unknown，不猜数值。

资源证据分 JS heap、WebView 进程、Node/原生进程和 GPU；构建证据分源码、前端产物和安装包。不得将一种标识或指标当作另一种。

### 7.2 每批回填模板

```text
批次 / 子批次：
状态：待执行 | 实施中 | 已实现待验收 | 部分验收 | 已验收 | 阻塞
基线提交 / 受测实现提交 / 构建标识：
改动与未改动范围：
场景 / 机器 / 缓存状态 / 数据夹具 / 负载：
测试命令、退出状态与日志：
前后指标（全部原始样本、p50/p95、掉帧与资源代价）：
双主题 / 键盘 / reduced-motion / 安全边界：
未执行项、失败项与原因：
是否保留本改动 / 回退方式：
远端提交与证据位置：
下一批允许开展的范围：
```

“已实现待验收”不改成“已验收”直到有对应环境证据；批次部分延期时列出具体任务 ID、缺失条件和可继续的独立事项，不能让一个设备缺口掩盖其他工作，也不能绕过前置正确性问题。

### 7.3 最终完成的判据

用户在主要流程中能够连续操作：及时收到反馈，加载期间不迷失，返回不丢现场，动画不挡下一步，滚动不出现已知可复现停顿，默认与低效果模式均可靠。与此同时，安全/数据/可访问性没有退化，目标桌面负载和稳态资源有测量依据，后续变更有回归保护。

达到上述覆盖范围后才声明“本专项验收完成”；设备缺口、冷进入或重负载下仍存在的限制必须同时公布。**不承诺所有硬件恒定高刷，不把代码语言、毛玻璃数量或动画时长当作流畅度。**

## 8. 首批执行指令

后续领取 F0 时可直接使用：

> 执行 plans/009-ui-fluidity-and-performance.md 的 F0。先核对当前 main 与相关实现，复用现有单 worker 性能入口，补齐真正 SPA 切换、KeepAlive 返回、滚动和连续导航基线。只做测量、必要就绪标记、隔离测试和证据，不修改动画方案、不迁移语言、不调用真实模型、不部署安装版。登记测量开销、目标环境和未执行范围，完成定向验证后提交受控文件，并回填 F0 状态。没有复现的性能问题不凭源码猜测为已确认缺陷。

## 9. 设计与工程依据

以下官方资料于 2026-09-17 核对，仅支撑原则与 API 概念；具体参数、预算和批次安排是本项目提案，不代表官方保证。实现时继续核对项目锁定版本，不为追随文档的新版本接口升级依赖。

- [Apple · Designing Fluid Interfaces](https://developer.apple.com/videos/play/wwdc2018/803/)：响应、可打断、空间一致性；流畅不只看帧率。
- [Vue · Performance](https://vuejs.org/guide/best-practices/performance)：测量、更新稳定性、大列表与响应式成本。
- [Vue · KeepAlive](https://vuejs.org/guide/built-ins/keep-alive)：缓存实例、容量与激活/失活生命周期。
- [Vue Router · Transitions](https://router.vuejs.org/guide/advanced/transitions.html)：路由组件与过渡的组合边界。
- [web.dev · Rendering performance](https://web.dev/articles/rendering-performance)：渲染流水线与帧预算。
- [web.dev · High-performance CSS animations](https://web.dev/articles/animations-guide)：合成友好属性与避免不必要的布局/绘制。
- [web.dev · Optimize long tasks](https://web.dev/articles/optimize-long-tasks)：主线程阻塞与任务拆分。
- [web.dev · INP](https://web.dev/articles/inp)：交互到呈现的指标含义，避免将自定义导航时长混称 INP。

内部约束另见 [Apple HIG Web 适配指南](../docs/guides/design/apple-hig-web-guidelines.md)、[工程契约](../docs/engineering-contracts.md)；品牌与执行优先级始终以当前仓库规范为准。
