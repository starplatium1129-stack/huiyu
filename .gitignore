# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temp
*.log
*.swp
*.bak
*~

node_modules/
test-results/
playwright-report/
# vitest --coverage 报告产物（npx vitest run --coverage 生成，不入库）
coverage/
# 环境变量文件（前瞻缺口：当前无 .env 入库，密钥一律本地/网关侧）
.env*
# Vite 构建产物
dist/
# 桌面打包产物（Tauri release 安装包在 desktop-tauri/src-tauri/target/ 下）
/release*/
# 美术审阅截图产物(tests/e2e/capture.spec.ts 生成,仅本地用于逐张观察)
.review-shots/
__pycache__/
*.py[cod]

# 运行期文件（配置、PID、Token、日志、朋友生成结果）
/runtime/

# services 运行时编译产物（npm run build:runtime 从 .ts 生成，不入库）
services/**/*.js
services/**/*.d.ts
# TypeScript is the source of truth; build:runtime produces these legacy entrypoints.
/server.js
/server/**/*.js
/routes/**/*.js
/scripts/**/*.js
/scripts/**/*.mjs
/tools/*.js
/assets/theme-bootstrap.js
/docs/guides/prompts/*.js
/eslint.config.mjs
/poc/audit-stability-apply.cjs
/poc/live2d-compare/probe.mjs
/poc/live2d-compare/probe2.mjs
/poc/live2d-compare/shot.mjs
/poc/live2d-compare/probe-types.js
workspace/

# 旧版运行文件（启动时会自动迁移到 runtime/）
friend_outputs/

# Claude Code 本地配置
.claude/
.gateway_token
tunnel.log
.gateway_pid
.gateway_port
.gateway_config.json
.tunnel_pid

# Dual-pose skeleton images (generated)
assets/dual-poses/*.png

# 角色 4 视角参考图（2026-08-29 已迁出项目 → AI 工作区 CharacterReferences，
# 与样张 SceneShowcase 同模式；此条目保留防误回放，勿重新放回项目 assets）
assets/character-references/
# Creator packages stay local; only import recipes and attribution metadata are tracked.
assets/live2d-candidates/

# 预压产物（npm run precompress 生成，随构建重新产生）
*.br
*.gz

# 场景审计一次性导出（scripts/maintenance 审计脚本产物，不入库）
/_audit_scenes.jsonl

# AI agent skills：默认忽略本机/第三方安装，只显式放行项目自有 skill。
# 新增共享 skill 时在此加入对应白名单，避免误提交个人设计工具和缓存。
.agents/*
!.agents/skills/
.agents/skills/*
!.agents/skills/studio-prompt-craft/
!.agents/skills/studio-prompt-craft/**
skills-lock.json

# 临时 UI 截图（Gemini 视觉复核用，不入库）
scripts/maintenance/ui-shots/

# Tauri generated artifacts
desktop-tauri/src-tauri/target/
desktop-tauri/src-tauri/resources/
desktop-tauri/src-tauri/binaries/
desktop-tauri/src-tauri/gen/
desktop-tauri/web/
desktop-tauri/native-live2d/target/
desktop-tauri/native-live2d/*.png
poc/**/target/
poc/**/src-tauri/gen/
poc/**/*.png

# pnpm env artifact (project uses npm)
pnpm-lock.yaml
pnpm-workspace.yaml

# Scene audit workdir (2026-08-26/27 full story-prompt audit + rewrite deltas)
.audit-work/

# 场景/热门角色/蓝图聚合产物（scripts/lib/scene-store.js、popular-store.js、blueprint-store.js 生成，不入库）：
# Git 只版本控制语义源（data/scenes/ 组分片、data/popular/*.json、data/blueprints/*.json、curation.json），
# 产物由网关启动 / scenes:build / blueprints:build 自愈重建，见 scripts/lib/ensure-data-build.js
/data/scenes.json
/data/scenes-nene.json
/data/scenes-natsume.json
/data/scenes-shared.json
/data/scenes-core.json
/data/scenes-index.json
/data/popular-characters.json
/data/scene-blueprints.json
/data/character-reference-standards.json
/data/character-reference-view.json

# scripts/archive：一次性历史脚本（占 scripts 目录 44%，2026-08-29 审计出库）。
# 不再跟踪；需要时从 git 历史（origin 保留完整历史）取回。
scripts/archive/

# AI 会话工作记忆目录（.workbuddy），个人工具状态不入库（2026-08-29 审计 P2 顺带收口）
.workbuddy/

# Local development caches and UI verification artifacts
.cache/
# Derived native installer template, bitmap and UI previews.
desktop-tauri/src-tauri/installer/generated/
